---
memory_type: daily
date: 2026-08-06
updated_at: 2026-08-06
---

# 2026-08-06

## Events

<!-- fact-id: event.2026-08-06.workspace-migrated-to-d-drive -->
- The active Commerce Ops workspace was safely copied from
  `C:\Users\PC\Documents\New project2` to
  `D:\Projects\commerce-ops` without deleting or renaming the C-drive source.
  The frozen migration snapshot matched at 125,373 files and 34,619,645,503
  bytes; both copies had branch `codex/vue-mainline-integration`, HEAD
  `89ccd7ebc0e80c6c19079c3b4d0b7e6bdae0992f`, and the same 236-entry Git
  worktree-status SHA-256. The 3,514,261,504-byte formal SQLite database matched
  SHA-256 `910849bdcb7568a53025266dc80d9533ef34c4aef26648ff4d8bdc4813f54cb8`
  and passed `PRAGMA quick_check` on D. Git object verification, portable-path
  checking, the memory audit, and Doctor passed; Doctor's port warnings were
  expected because the migrated services were already listening. The D-drive
  main service and scheduler were active and HTTP probes on ports 3101 and 4173
  returned 200. Three superseded generated frontend assets encountered during
  concurrent source updates were preserved outside the repository under
  `D:\Projects\commerce-ops-migration-safety-20260806-1156`. A later
  deletion-time audit found that four Codex task hosts and one active Phase 3A
  runtime had continued to use the C-drive copy after the frozen snapshot. Its
  14 content-differing source files and 29 missing Git checkpoint files were
  merged into D; no C-only non-Git file remained, which reconciled the later
  37-file/410,504-byte snapshot-count increase. After the user's separate
  deletion confirmation, every old-current-directory process was stopped, the
  C-drive contents were purged, and `C:\Users\PC\Documents\New project2` was
  converted in place to a directory junction targeting
  `D:\Projects\commerce-ops`. Junction file-hash verification passed, both HTTP
  probes still returned 200, and the formal D-drive SQLite database again passed
  `PRAGMA quick_check`.

<!-- fact-id: event.2026-08-06.postgresql-incremental-sync-completed -->
- SQLite-to-PostgreSQL Shadow incremental synchronization completed without
  switching production or modifying formal SQLite. The first controlled batch
  `62d92fe3-bfce-41d5-ada0-2d728769cee9` synchronized 47/47 dependency-closed
  Product, Sales, Inventory, Task, and Audit tables: 629,254 rows examined,
  390,928 inserted, 204,562 updated, and zero table failures. Independent final
  validation `85146b36-9e93-45ef-8dbc-0e189468416b` found zero count, business,
  or 42-sample differences. Pause/resume and read-only status were exercised;
  final Shadow stage is `READY`, while `is_switch_ready=false`. Current-snapshot
  six-domain provider parity passed, controlled SQLite/PostgreSQL startup probes
  passed, full tests passed 911/911, Vue Build passed, and Doctor kept formal
  SQLite integrity `ok`. Canonical evidence and remaining cutover gates are in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-INCREMENTAL-SYNC-FINAL-20260806.md`.

<!-- fact-id: event.2026-08-06.postgresql-provider-phase2-completed -->
- PostgreSQL Provider Abstraction Phase 2 completed without switching
  production, migrating a database, changing business rules, moving file
  assets, or creating a business Agent. A fail-closed factory and
  provider-aware application composition now expose stable Product, Sales,
  Inventory, Task, Audit, AI Context, and Agent Monitoring repositories for
  SQLite or the read-only `commerce_ops_shadow` PostgreSQL database. Full
  identity, core-field, and aggregate parity passed for 18,347 products,
  79,768 order headers, 115,868 order lines, 20,049 latest inventory rows, 252
  tasks, and 42,031 audit events. Controlled SQLite and Shadow startup probes
  loaded Foundation, six Contexts, `sales.daily-report@2.1.0`, and Monitoring
  with zero writes and zero external calls. The full test suite passed
  905/905, Vue Build passed, and Doctor reported all checks OK. Formal SQLite
  remains the only production provider at migration 024; scheduler, file,
  synchronous audit-service, Price Control, and fulfillment write paths remain
  explicit SQLite boundaries for a future approved phase. See
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PROVIDER-PHASE2-20260806.md`.

<!-- fact-id: event.2026-08-06.price-control-change-first-view -->
- The formal Price Control page now opens the SKU price-change view by default,
  matching the demo preview instead of opening current prices first. It displays
  country, category, SKU, Chinese name, platform/shop/price type, old and new
  price, direction, source batch, and detection time; users can copy one row,
  selected rows, or the current page as newline-separated deterministic texts.
  The summary now shows affected SKU count. Live read-only verification found
  508,990 stored price-point changes affecting 18,305 SKUs, with 50 selectable
  rows on the first page; the hourly schedule was already enabled and its latest
  run had succeeded. No synchronization or DingTalk send was triggered by this
  UI task. Full tests passed 906/906 and Vue Build passed.

<!-- fact-id: event.2026-08-06.price-control-null-semantics-diagnosis -->
- Read-only tracing established that Price Control currently treats every null
  one of the 15 platform/shop/price columns in an approved source row as an
  explicit price removal. This is unsafe because approved applications are
  platform-scoped partial updates: batch `PC2026CYHWglX` contains 738 approved
  SKU rows with all six Shopee price columns populated while every Lazada and
  TikTok price column is SQL `NULL`. A representative uploaded SKU
  `T5KK0031024` therefore became TikTok campaign `1175429.00 -> no price`, even
  though the null indicates that TikTok was outside this Shopee application,
  not a confirmed deletion. Stored changes currently include 326,596 `NEW` and
  132,301 `REMOVED` events, so null-boundary events make up about 90.2% of the
  508,990 events and should not be treated as reliable price changes until the
  comparison rule is corrected. No data, schedule, task, or notification state
  was changed during this diagnosis.

<!-- fact-id: event.2026-08-06.price-control-null-semantics-repaired -->
- After explicit user approval, the hourly Price Control schedule was paused,
  formal SQLite was backed up to
  `storage/backups/commerce-ops-before-price-null-repair-20260806-103549.sqlite`
  (3,113,541,632 bytes, SHA-256
  `12f1168b0b32dd4ad8ad0cd099b5cd762d3f8f010b704ae86173d4f44062062c`,
  integrity `ok`), and migration 025 was formally applied. Synchronization now
  ignores null price dimensions, carries the prior non-null current price
  forward, creates `NEW` only for absent-to-numeric values, and reserves
  `REMOVED` for a future explicit source deletion marker. All 508,990 historical
  events remain stored: 132,301 false removals and 95,748 false reappearances
  were marked `INVALID`, leaving 280,941 valid events. The current-price baseline
  was rebuilt from latest non-null snapshots to 617,667 price points with zero
  missing, mismatched, or orphaned rows; five invalid-only Foundation review
  tasks were auditably cancelled. The representative
  `ID/T5KK0031024/TIKTOK/STANDARD/CAMPAIGN` price was restored to 1,175,429.00.
  Operation Audit contains the repair totals. The main service and scheduler
  were restarted healthy, source MySQL probe succeeded, and the hourly schedule
  was restored with the enabled `控价提醒机器人`; it notifies only when valid new
  changes are found. Core full tests passed 913/913, final affected tests passed
  17/17, and the Vue production build passed.

<!-- fact-id: event.2026-08-06.price-control-hourly-aligned -->
- At the user's request, the persisted hourly Price Control schedule now aligns
  to whole clock hours instead of calculating the next run from the actual
  completion/claim time. Enabling between hours selects the next `xx:00`, and a
  delayed poll still schedules the following run at the next aligned hour, so
  delays do not accumulate. The main service and scheduler were restarted
  healthy, the dedicated Price Control DingTalk robot and notification rules
  were preserved, and the formal next run was persisted as 2026-08-06 12:00:00
  Asia/Shanghai. The affected Price Control test suite passed 10/10.

<!-- fact-id: event.2026-08-06.price-control-adjustment-workflow-candidate -->
- A candidate Price Control adjustment workflow is implemented but not yet
  activated in the formal database. It groups valid changes by synchronization
  run, defaults the page to the latest change round, filters by round and
  adjusted/unadjusted status, copies every valid change in the selected round,
  and lets an operator save an adjusted/unadjusted disposition plus a remark on
  each change. The disposition is also recorded as Foundation task evidence and
  an Operation Audit event. Candidate migration 026 adds the required columns
  and index; formal SQLite remains at migration 025, the running services were
  not restarted, and the hourly Price Control schedule was not changed. The
  focused backend/frontend/Foundation/Audit tests passed 48/48, the focused
  Price Control tests passed 10/10, and the Vue production build passed. The
  unrelated file lifecycle/review worktree currently has async service changes
  with synchronous tests, so the repository-wide suite is not green and was
  intentionally left outside this feature boundary.

<!-- fact-id: event.2026-08-06.price-control-adjustment-workflow-activated -->
- After explicit user approval, the 3,449,044,992-byte pre-026 SQLite backup was
  retained at
  `storage/backups/commerce-ops-before-price-adjustment-026-20260806-114827.sqlite`,
  formal migration 026 was applied, and the adjustment workflow became ready in
  the live API. All 280,941 valid historical events were initialized as
  unadjusted. The latest-change round contains 21,298 changes across 6,642 SKUs,
  while the previous-change round contains 208,101 changes across 35,249 SKUs.
  The first observed whole-hour run started at 2026-08-06 12:01:31 and completed
  successfully at 12:02:03 Asia/Shanghai with zero new changes, so DingTalk was
  correctly skipped and the next run remained aligned to 13:00. The formal
  SQLite database is healthy at migration 026.

<!-- fact-id: event.2026-08-06.price-control-module-restored-and-rerun -->
- The Price Control change-module context and live runtime were restored and
  independently reverified. Formal SQLite remained at migration 026, the live
  API reported the adjustment workflow ready and the external MySQL source
  connected through a read-only transaction. Manual incremental run
  `51b2b463-bbf4-421b-8098-b7b67001bef3` completed successfully at 13:46
  Asia/Shanghai after reading 104 approved batches, 114,822 source rows, and
  573,950 price points. It applied zero new batches and produced zero price
  changes, tasks, or DingTalk notifications. Existing state remained 508,990
  stored events, 280,941 valid unadjusted events, and 617,667 current price
  points. SQLite `PRAGMA quick_check` returned `ok`, HTTP health returned 200,
  and the focused Price Control/Vue contract tests passed 22/22.

<!-- fact-id: event.2026-08-06.price-control-zero-change-source-audit -->
- An independent read-only source audit at 15:23 Asia/Shanghai confirmed that
  the day's zero-change Price Control result was supported by the source data,
  not by a failed sync: across every approval status the source contained zero
  rows or batches created, submitted, approved, or marked updated on 2026-08-06.
  The latest `CA` approval remained 2026-08-05 17:23:54; the latest `BA`
  submission remained 2026-08-05 22:06:03. The latest read-only check at 21:01
  confirmed that all eight hourly runs from 14:00 through 21:00 succeeded, each
  rechecking 104 recent approved batches, 114,822 source rows, and 573,950
  non-null price points with zero changed fingerprints. The
  248 locally tracked batches versus 5,298 approved source batches reflects the
  designed current-baseline plus recent-window policy rather than a failed
  import of today's data. A remaining source-contract blind spot was made
  explicit: all 7,085,439 `CA` rows have null `apply_update_time`, and the
  source lacks approval/time indexes, so the hourly recent-window scan cannot
  independently rule out an in-place mutation to an old approved batch whose
  business timestamps do not change. No such mutation was observed and no
  database, schedule, task, or notification state was changed by the audit.

<!-- fact-id: event.2026-08-06.price-control-shop-registry-activated -->
- After explicit user approval, a consistent 3,514,294,272-byte pre-027 SQLite
  backup was retained at
  `storage/backups/commerce-ops-before-shop-registry-027-20260806-200712.sqlite`
  with `quick_check=ok` and SHA-256
  `5FF0E7D03BB059E108EDE0FC1AC5A2874804722B2750504B739B301FB5A76250`.
  Migration 027 was formally applied and a live encrypted Mabang account
  session imported 139 authorized shops with provider country evidence:
  Lazada 79 and Shopee 60 across ID, MY, PH, SG, TH, TW, and VN. All 139 have
  active Mabang Listing bindings, 69 matched growth shops exactly, and all
  currently retain `UNKNOWN` Standard/Mall type because the provider list does
  not prove that attribute. Formal SQLite passed integrity and foreign-key
  checks after the import; no product price was changed.

<!-- fact-id: event.2026-08-06.price-control-repricing-candidate-ready -->
- The guarded Price Control repricing workflow was implemented end to end and
  was subsequently activated in formal SQLite under the staged approval
  described below. Migration 028 adds immutable
  provider diff items and the `PREVIEW_READY / CONFIRMING / EXECUTING /
  EXECUTION_UNKNOWN / SUCCEEDED / PARTIAL / FAILED / EXPIRED / CANCELLED`
  plan state machine. The latest valid round is enforced; each source change
  requires explicit same-platform, same-country shop selection. The service
  builds natural-language instructions, strictly validates the AI parse, then
  obtains and validates an actual Mabang Listing diff. Only an exact SKU match
  or bounded `source SKU + S1…S9` virtual match is accepted and both identities
  are disclosed. Preview tokens stay server-side; actual diffs default to zero
  selected. Execution requires selected actual diff IDs, matching fingerprint,
  exact confirmation text `确认同步到店铺`, and acknowledgement for selected
  `UNKNOWN` shop types. Confirmation revalidates the latest round, source
  changes, shops, common account and required price-field capabilities before
  an atomic claim. Shared Mabang account operations are serialized so a
  concurrent account switch cannot cross preview or execution scope. Provider
  command indexes must be explicit for multi-country plans; expired previews,
  duplicate listing/variation/field targets, non-numeric live values and
  non-positive control targets fail closed. Network ambiguity, a missing job ID,
  or accepted-provider/local-persistence ambiguity is never resubmitted. Only
  explicit item-level provider success counts; missing terminal results remain
  unknown, and a source change is adjusted only when all planned and originally
  assigned shop diffs have success evidence. The adapter boundary permits
  future official marketplace APIs under the same safety contract. A read-only
  formal binding audit confirmed that all 79 active Lazada shops and all 60
  active Shopee shops currently advertise both `price` and `special_price`.
  Repricing focused tests passed 25/25, the combined shop/change/repricing
  suite passed 37/37, the full Node suite passed 998/998, six targeted
  Mabang Listing provider preview/selection/readback tests passed, Vue type
  checking and an isolated production build passed, and desktop plus 390x844
  mock browser checks had no console errors. Browser QA caught and fixed a
  frontend button-state gap: the
  execution button now stays disabled until selection, exact text and any
  required `UNKNOWN` acknowledgement are all present. The final execution
  button was never clicked. After the user explicitly approved the preview
  phase, formal SQLite was consistently backed up to
  `storage/backups/commerce-ops-before-repricing-028-20260806-214913.sqlite`
  (3,514,494,976 bytes, SHA-256
  `EB8EC078ADA84FA819CF251A8EEEDBA7EB3CAD87045E07B3A8F490E1338413F7`,
  `quick_check=ok`, zero foreign-key violations), migration 028 was applied at
  2026-08-06 21:49 Asia/Shanghai, and the main service plus scheduler were
  restarted healthy with `repricing.workflowReady=true`. The first authorized
  real Mabang preview attempt used one latest-round TW/Shopee campaign-price
  change and its only active same-country shop, but returned HTTP 500 before AI
  parsing or provider diff generation because the post-restart Mabang account
  connection did not become active. After the user confirmed the memory and
  approved repair plus one retry, the Foundation bridge was changed to preserve
  bounded `MABANG_*` codes, status and messages while continuing to hide other
  connector failures. Its focused Foundation/repricing regression passed
  34/34. The encrypted saved account then connected successfully and returned
  the expected live shop scope of Lazada 79, Shopee 60 and TikTok 0. The one
  approved retry reached AI parsing and failed closed with the newly visible
  `MABANG_LISTING_REQUEST_FAILED`: Commerce Ops AI Gateway rejected the Python
  command-parser Prompt because its registered SHA-256 was stale. The intended
  Python TW support had added `TW/taiwan` aliases and `TW` to the allowed country
  enum, changing the runtime Prompt digest from the registered value to
  `e686adc2d59bc5273b3e72f9550087195b4d3dce1ca116923d9eec4e08b4f1a6`.
  The Gateway registry was synchronized to that digest and a cross-file
  regression now verifies both Python Prompt digests after Windows newline
  normalization. Node Foundation/AI/repricing tests passed 40/40 and Python AI/
  provider tests passed 38/38. The repaired service was restarted with retained
  diagnostic logs; health, `repricing.workflowReady`, and the Mabang connection
  are all ready, with the same 79/60/0 shop scope. No preview was retried after
  the Prompt fix because the approved retry had already been consumed.
  Fail-closed behavior held throughout: there are zero repricing plans, items,
  selections, confirmations, execution jobs, or price-execution audit events;
  the source change remains `VALID / UNADJUSTED`, and the Mabang execution audit
  file was not changed. The user then confirmed this memory and approved one
  further real preview without price execution. Preflight reconfirmed the same
  latest valid round, `VALID / UNADJUSTED` source change, unique active
  TW/Shopee shop, `special_price` capability, connected idle Mabang session,
  and zero-plan baseline. The request passed account connection and the repaired
  AI Profile gate; Operation Audit recorded a successful DeepSeek
  `parse_listing_commands` call. The real Mabang preview then returned the
  explicit business error that the selected shop scope had no matching
  modifiable Shopee online product for SKU `P4AA1866860`, so the outer API
  returned 409 and persisted no preview. The service and Mabang session remain
  healthy and connected. Final verification again found zero repricing plans,
  items, selections, confirmations, execution jobs, or execution audit events;
  the source change remains `VALID / UNADJUSTED`, and the Mabang execution audit
  file is unchanged. This single-preview authorization is exhausted. A safer
  next attempt should first use read-only live-listing discovery to select a
  latest-round SKU that is proven online in an authorized shop, then obtain a
  separate explicit approval for one preview; price execution still requires a
  later independent confirmation.

<!-- fact-id: event.2026-08-06.postgresql-phase3a-in-progress-preserved -->
- PostgreSQL Production Readiness Phase 3A remains an in-progress candidate and
  has not switched production. Its C-drive task completed the six-domain write
  boundary audit, Scheduler/Task/Audit Provider work, Fulfillment dual-Provider
  repository, local Storage Provider boundary, Price Control Provider
  adaptation, Shadow migration 005, controlled Shadow write/read/cleanup, both
  provider startup probes, six-domain parity, and focused suites of 114/114,
  51/51, 66/66, and 59/59. The task stopped before resolving the remaining
  clean-path timeout, obtaining a final repository-wide green run, running the
  final Build and Doctor, writing the readiness report, or reassessing
  `is_switch_ready`; those gates remain open and `is_switch_ready` remains false.
  All 14 Phase 3A delta files and its Git checkpoints were preserved in the
  D-drive workspace before the C-drive contents were removed.

<!-- fact-id: event.2026-08-06.postgresql-phase3a-completed -->
- PostgreSQL Production Readiness Phase 3A was recovered and completed without
  switching production or changing formal SQLite. The clean-path timeout and
  PostgreSQL driver-isolation contract were fixed; `lib/storage` is no longer
  hidden by runtime-data ignore/copy rules; provider parity now automatically
  selects the latest successful pinned incremental snapshot; PostgreSQL JSONB
  export metadata and integer compatibility flags were corrected. Shadow
  migration 006 added the Price Control validity and adjustment fields from
  formal SQLite migrations 025/026. A controlled PostgreSQL contract passed
  Scheduler task/run/event and lease, export metadata, lifecycle/review
  metadata, Audit, Foundation Task/event, Price Control change/adjustment, and
  Fulfillment Agent Run writes, then verified complete cleanup with zero
  external calls or real business actions. Six-domain parity and both read-only
  startup probes passed, full tests passed 921/921, full Build passed, and
  Doctor kept formal SQLite integrity `ok` at migration 026. Phase 3A readiness
  is true, but production `is_switch_ready` remains false pending DELETE/
  tombstones, a separate writable PostgreSQL rehearsal, complete migration
  scope, refreshed synchronization, backup/restore, rollback rehearsal, and
  explicit cutover approval. Canonical evidence is in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3A-READINESS-20260806.md`.

<!-- fact-id: event.2026-08-06.postgresql-phase3b-rehearsal-ready -->
- PostgreSQL Phase 3B recovered to an execution-ready but not-yet-executed
  rehearsal. Read-only inspection confirmed that
  `commerce_ops_migration_test` is stale with 38 `app` tables, no `ai_shadow`
  or `shadow_meta` tables, and all six current Shadow migrations pending. A
  guarded runner now defaults to a read-only plan and requires both `--apply`
  and exact `--confirm-database=commerce_ops_migration_test` before atomically
  rebuilding only the three test schemas. The approved path will run F4
  compatibility, eight domain write boundaries, rollback, unique, JSONB,
  timestamptz, pagination, UPSERT, cleanup, latency, and no-external-call
  runtime safety checks. Guard tests passed 5/5, repository tests passed
  926/926, Build passed, and repeated plans proved the test schema stayed
  unchanged. Production remains SQLite, Shadow was not touched, and Phase 3B
  execution awaits explicit user confirmation. Exact application-role runtime
  parity remains a later dedicated-staging gate. Canonical evidence is in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3B-REHEARSAL-READY-20260806.md`.

<!-- fact-id: event.2026-08-06.postgresql-f3-scale-delete-policy-ready -->
- PostgreSQL migration preparation closed two further code-level gates without
  modifying PostgreSQL data. The legacy F3 whole-table array/sort path was
  replaced with bounded SQLite iterator batches, batched PostgreSQL inserts,
  PostgreSQL cursor validation, and order-independent duplicate-sensitive
  streaming digests. Snapshot-aware UUID classification now preserves real
  namespace/hash/platform identifiers as text and propagates compatible FK
  types; the current Price Control expression index and all SQLite views are
  converted explicitly. A formal read-only scan passed for 3,875,103 rows in
  15,546 batches across 88 tables and 15 views with peak RSS 269.4 MiB; current
  DDL generation passed for 306 indexes and 215 verified UUID columns. F3 now
  also requires exact test-database apply confirmation. Incremental sync hard
  deletes now have a versioned policy: default `BLOCK`, full-reconcile exact-key
  `DETECT`, and separately gated/confirmed child-first transactional `APPLY` to
  `commerce_ops_shadow` only. The delete plan ran read-only and no delete mode
  was executed. Full tests passed 936/936 and Build passed. The migration test
  database remained unchanged at 38 legacy `app` tables, production remained
  SQLite, and Shadow data was not modified. Evidence is in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3B-REHEARSAL-READY-20260806.md` and
  `docs/design/COMMERCE-OPS-INCREMENTAL-SYNC-DESIGN.md`.

<!-- fact-id: event.2026-08-06.postgresql-phase3c-readiness-audited -->
- PostgreSQL Phase 3C gained a repeatable read-only operational checker and a
  guarded operations runbook. The configured `commerce_ops.app` candidate was
  inspected as `commerce_migrator` with `default_transaction_read_only=on` on
  PostgreSQL 18.4. Twelve of 21 gates passed: production remains SQLite, both
  roles are least-privilege, database/schema access is correct, WAL level is
  `replica`, data checksums are on, and `pg_dump`, `pg_restore`, and
  `pg_basebackup` 18.4 are available. Nine gates remain blocked: TLS, WAL
  archiving, slow-query logging, I/O timing, `pg_stat_statements`, encrypted
  backup/retention evidence, a restore rehearsal, monitoring evidence, and a
  capacity baseline. The candidate schema remains empty and no database or
  infrastructure setting was changed. Full tests passed 939/939 and Build
  passed. Phase 3C is `NOT_READY` and `is_switch_ready` remains false. Evidence
  is in `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3C-READINESS-20260806.md` and
  `docs/design/COMMERCE-OPS-POSTGRESQL-PHASE3C-OPERATIONS-RUNBOOK.md`.

<!-- fact-id: event.2026-08-06.postgresql-phase3b-rehearsal-executed -->
- After the user explicitly confirmed rebuilding only
  `commerce_ops_migration_test`, the PostgreSQL Phase 3B rehearsal passed. It
  replaced the stale 38-table residue with 112 `app` tables, 15 views, 5
  `ai_shadow` tables, and 6 `shadow_meta` tables; migrations 001-006 have
  matching checksums. F4 passed nine Repository modules and full CRUD,
  transaction, constraint-error, value, and type parity; all eight controlled
  domain write boundaries passed and cleaned up; the advanced 105-row contract
  passed stable pagination, UPSERT, unique rejection, rollback, JSONB, and
  timestamptz checks. The runner now temporarily grants database-level CREATE
  only for the exact test-schema reset and verifies its revocation; the final
  migrator privilege is false and `commerce_app` still cannot connect to the
  test database. F4 explicitly preserves boolean semantics for the Shadow
  compatibility schema's 26 integer-backed flag columns. A fresh formal SQLite
  snapshot passed integrity and zero-FK-violation checks while normalizing
  3,875,110 rows in 15,547 batches across 88 tables and 15 views. Full tests
  passed 940/940, Build passed, Doctor passed with only expected occupied-port
  warnings, and the repeated Phase 3C read-only audit remained 12/21. Production
  SQLite, `commerce_ops`, and `commerce_ops_shadow` were not modified;
  `is_switch_ready` remains false. Canonical evidence is in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3B-REHEARSAL-READY-20260806.md`.

<!-- fact-id: event.2026-08-06.postgresql-phase3c-staging-rehearsal-completed -->
- After explicit user approval, PostgreSQL Phase 3C completed a production-like
  rehearsal without switching production. The independent
  `commerce_ops_staging` database was created from the completed Phase 3B
  schema-only migration-test database, then cleared and populated from new
  read-only SQLite online-backup snapshots; it has a distinct database OID and
  never reused `commerce_ops_shadow`. Its 112 `app` tables/15 views, 5
  `ai_shadow` tables, and 6 `shadow_meta` tables retain migrations 001-006. The
  initial 47-table load inserted 1,325,642 scoped rows, while final incremental
  synchronization captured concurrent source activity and validated 1,325,650
  exact source keys with zero target-only candidates, zero count differences,
  and 42/42 deterministic samples. Dedicated role `commerce_staging_app` is
  writable only for DML: database/schema CREATE, TEMPORARY, relation ownership,
  role membership, and CREATE/ALTER/DROP probes were all absent or denied with
  SQLSTATE 42501. Product, Sales, Inventory, Task, Audit, Agent Run, and Tool
  Invocation writes passed; Product and scheduled-task soft delete/restore,
  controlled Sales/Inventory/Audit hard delete, and Agent/Tool cascade delete
  passed with complete fixture cleanup. A process-scoped PostgreSQL config
  switch started the real server on a dynamic loopback port and returned HTTP
  200 while the existing port-3101 SQLite service remained healthy. A 131 MB
  AES-256-GCM custom backup was restored into the independent
  `commerce_ops_staging_restore_20260806` database with matching 112/5/6 object
  counts, all six migration checksums, and business counts; local monitoring and
  latency samples also passed as simulations. The formal production checker
  remains 12/21 because TLS, WAL archive, slow-query logging, I/O timing,
  `pg_stat_statements`, production-grade encrypted backup/restore, alerting,
  and production-shaped capacity evidence are not approved on an actual
  deployment. Full tests passed 948/948, Build passed, Doctor passed with only
  expected occupied-port warnings, and `is_switch_ready` remains false.
  Canonical evidence is in
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3C-READINESS-20260806.md`.

<!-- fact-id: event.2026-08-06.c-drive-cleanup-confirmed -->
- After explicit user approval, the confirmed C-drive cleanup permanently
  removed all 142 Commerce Ops development/test temporary targets, the npm and
  pip caches, seven stale application updater caches, and all 23 `.exe`, `.msi`,
  and `.msix` installers from Downloads. C-drive free space increased from
  38.68 GiB to 92.67 GiB, a net recovery of about 53.99 GiB. One 63.7 MiB
  ChatPlus updater file was deliberately retained because its ACL denied access
  while ChatPlusAI was running; no ownership or application state was forced.
  The C-path workspace junction still targets `D:\Projects\commerce-ops`, no
  process command line referenced the old C path, both HTTP services returned
  200, Doctor passed, and formal SQLite remained healthy at migration 026 with
  `PRAGMA quick_check` equal to `ok`.

## Handoff

- Use `D:\Projects\commerce-ops` as the canonical active workspace.
  `C:\Users\PC\Documents\New project2` is now only a directory junction to that
  same D-drive workspace; no rollback copy remains on C.
- Keep `DATABASE_PROVIDER=sqlite` for production.
- PostgreSQL production selection remains guarded. Shadow read validation still
  requires `POSTGRES_SHADOW_MODE=true`; the new writable staging path requires
  `POSTGRES_STAGING_MODE=true`, the exact `commerce_ops_staging` confirmation,
  and the dedicated staging application role. Neither mode is a production
  write target.
- Follow the production migration roadmap before any provider switch. Phase 3A,
  Phase 3B, Phase 3C, and the independent Phase 3D full-source rehearsal are
  complete. The explicitly approved formal candidate `commerce_ops` has now
  been initialized and warmed from the complete 92-table source scope; its
  encrypted backup and automated restore rehearsal passed. Final writer
  freeze, final full-source synchronization, and explicit switch approval
  remain open; `is_switch_ready=false` by design.
- Phase 3C now passes 21/21 operational checks with zero blocker IDs. Encrypted
  backup, automated restore verification, monitoring coverage, capacity, TLS,
  encrypted WAL archiving, slow-query logging, I/O timing, and
  `pg_stat_statements` are all verified. The infrastructure prerequisites to
  enter Phase 3D are satisfied, and the timed Phase 3D cutover/rollback
  rehearsal has passed. Production remains on SQLite while the initialized
  formal candidate waits for the final confirmed maintenance window: freeze
  the exact writers, perform the final zero-difference synchronization, then
  persist the separately approved provider switch.
- Price Control runs its persisted 60-minute whole-hour schedule with the
  dedicated `控价提醒机器人`; the next due time is persisted in
  `price_control_automation_settings`. It sends only for newly detected valid
  changes or configured failures, and skips notifications when no changes are
  found. The formal page defaults to valid change records and retains current
  prices as a secondary tab; invalid historical events remain directly
  traceable and are not physically deleted.
- Price Control null semantics are now authoritative: unmaintained null fields
  carry forward the previous non-null value and never produce a removal.
  Explicit removals remain unsupported until the source adds a deletion marker.
  Formal SQLite is at migration 028 with 139 authorized shops registered, and
  the guarded repricing runtime is healthy. Its first approved live-preview
  attempt failed closed at post-restart Mabang account connection before any
  provider diff, plan, confirmation, or write was created. Reconnect diagnosis
  and a new explicit approval are required before another preview; price
  execution requires a later, separate confirmation.

<!-- fact-id: event.2026-08-06.lazada-oauth-quick-tunnel-ready -->
- A standalone Lazada Open Platform OAuth development adapter was added under
  `integrations/lazada-oauth` without changing the existing port-3101 business
  server. It binds only to `127.0.0.1:8977`, implements the official
  HMAC-SHA256 signing contract, and now supports three application slots for the
  user's 60-store plan. App 1 retains `/lazada/callback`; App 2 and App 3 use
  `/lazada/apps/app-2/callback` and `/lazada/apps/app-3/callback`. OAuth state,
  authorization tickets, events, and tokens are isolated by `app_id`; store
  tokens use `(app_id, shop_id)` as the SQLite key so later authorizations cannot
  overwrite earlier stores. SQLite token values are AES-256-GCM encrypted with
  a generated key kept in the Git-ignored `.env`, while per-store `.env` token
  mirrors preserve the local secret-storage requirement. A localhost-only
  `/lazada/manager` page exposes non-secret app/store/expiry metadata. Dedicated
  tests pass 7/7, including multi-app isolation, encrypted storage, wrong-app
  state rejection, and manager redaction. The user confirmed App 1 Callback URL,
  App Key, and App Secret are configured; App 2 and App 3 credentials remain to
  be filled. The first real seller callback did not bind because its OAuth state
  was invalid/expired and the original Quick Tunnel had stopped. A fresh Quick
  Tunnel and OAuth service were started, the new App 1 callback was persisted to
  `.env`, and local/public health plus callback/config matching probes pass. The
  first successful real authorization then exposed a zero-millisecond timeout
  regression in the per-app token exchange path; the shared 20-second fallback
  was restored and covered by a new regression test, bringing the suite to 8/8.
  App 1 successfully exchanged and stored an active encrypted token for MY shop
  `300448752107`, expiring 2026-09-05T10:06:22.282Z. The per-store `.env` access
  and refresh mirrors are present, the SQLite encrypted record is present, and
  the legacy plaintext token table remains empty. A signed, read-only production
  call to Malaysia endpoint `/seller/get` returned HTTP 200 / Lazada code 0 for
  the same seller, with status `ACTIVE`, confirming the complete API call chain.
  The current
  `trycloudflare.com` host remains temporary; after any tunnel restart all three
  Lazada app Callback URL settings must be updated.

<!-- fact-id: event.2026-08-06.postgresql-production-hardening-awaiting-admin-restart -->
- Commerce Ops PostgreSQL Production Infrastructure Hardening implemented all
  nine requested controls without switching production or touching SQLite.
  Production-grade AES-256-GCM logical backup now uses a separately protected
  key, SHA-256 manifests, no retained plaintext, 30-day retention, and excludes
  infrastructure extensions. An actual 104,975,086-byte staging backup passed.
  Automated isolated restore verification passed in 111.092 seconds with
  matching 112/5/6 relation counts, all six migration hashes, seven selected
  business counts, zero invalid foreign keys/indexes, and automatic temporary
  database cleanup; evidence is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-RESTORE-REHEARSAL-20260806091933.json`.
  Monitoring now uses least-privilege `commerce_monitor` plus `pg_monitor` and
  covers connections, locks, archiver, disk, backup/restore freshness, slow
  queries, I/O, temp files, deadlocks, and checkpoints. The staging capacity
  baseline passed at 9,787.127 operations/second, p95 6.488 ms, zero errors and
  lock waits, 3% connection use, and 41.887% disk free; evidence is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-CAPACITY-BASELINE-20260806092800.json`.
  Formal readiness advanced from 12/21 to 16/21. The four evidence gates are
  cleared; the remaining TLS, WAL archive, 500 ms slow-query log,
  `track_io_timing`, and `pg_stat_statements` preload settings are fully staged
  but require one administrator restart of `postgresql-x64-18`. The current
  Codex process was denied Windows service-control access; config rollback
  worked and the live PostgreSQL instance remained available. Resume by running
  `scripts/postgresql-production-hardening.ps1 -Apply -ConfirmInstance
  commerce_ops_local_pg18` from an Administrator PowerShell, then run the
  hardening verifier, monitor, and Phase 3C readiness checker. Full tests passed
  954/954, Build and Doctor passed, SQLite integrity is `ok`, and the existing
  health endpoint returned `{"ok":true}`. Current checklist is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PRODUCTION-READINESS-CHECKLIST-20260806.md`;
  `is_switch_ready` remains false and Phase 3D is not yet eligible.
  The user's first Administrator PowerShell attempt then revealed a Windows-
  specific validation defect: PostgreSQL refuses `postgres.exe -C` when the OS
  process is elevated. Because PowerShell treated its stderr as terminating,
  the attempt stopped before restart and temporarily left HBA as `hostssl`
  while the live server still had TLS off. Codex immediately restored HBA to
  `host`, reconfirmed a non-TLS PostgreSQL connection, removed the elevated
  `postgres.exe` probe, and replaced it with a read-only `pg_file_settings`
  validator that accepts only the two expected restart-deferred settings.
  Direct validation now passes 12 settings, the corrected stage-only flow
  passes, and the same Administrator command is ready to retry.

<!-- fact-id: event.2026-08-06.postgresql-production-hardening-completed -->
- The user reran the corrected hardening script from Administrator PowerShell;
  it returned `APPLIED`, restarted `postgresql-x64-18` to `Running`, preserved
  `DATABASE_PROVIDER=sqlite`, and reported no SQLite touch. Post-restart
  hardening verification passed: a CA-validated application connection used
  TLS 1.3 with `TLS_AES_256_GCM_SHA384`, all loopback HBA rules require
  `hostssl`, and no setting remains pending restart. Forced WAL switch archived
  segment `00000001000000050000004C` with AES-256-GCM; `pg_stat_archiver`
  recorded one success and zero failures, and decrypt/SHA-256 comparison with
  the live WAL segment passed. A marked 550 ms query appeared in the 500 ms
  slow-query log, `track_io_timing` returned real read time, and
  `pg_stat_statements` is preloaded and installed in `public` for both candidate
  and staging. The least-privilege production monitor returned `PASS` with no
  alerts. The formal Phase 3C checker now reports 21/21 PASS, `blocked=0`, and
  no blocker IDs while intentionally retaining `isSwitchReady=false`. Focused
  PostgreSQL regression passed 16/16, Build and Doctor passed, PostgreSQL is
  running, SQLite integrity is `ok`, and the formal SQLite health endpoint
  remains `{"ok":true}`. Final evidence is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-HARDENING-VERIFICATION-20260806111356.json`
  and the updated Production Readiness Checklist. Phase 3D prerequisites are
  met; no production database switch is authorized.

<!-- fact-id: event.2026-08-06.platform-connector-center-lazada-live -->
- Commerce Ops now has an extensible Node.js Platform Connector Layer and
  Commerce API Gateway. The implementation adds the BaseConnector contract,
  connector registry, AES-256-GCM token cipher, unified Platform/Shop/
  ShopAuthorization/ApiRequestLog repository, proactive grouped token refresh,
  bounded read retries, fail-closed provider writes, normalized HTTP Gateway
  routes, and `gateway_only` Agent Tool Registry entries. Lazada is the first
  complete connector with shared OAuth/signing, country-specific API clients,
  seller, orders, products, and inventory operations; Shopee, TikTok Shop, and
  Mabang have stable extension seams. The connector control plane is stored in
  the existing OAuth SQLite WAL database, not the formal Commerce Ops business
  database, so no production database migration or provider switch occurred.
  Existing encrypted Lazada authorization migrated to the four connector
  tables; live SQLite/WAL inspection found no plaintext token and the legacy
  plaintext table has zero rows. A real read-only Malaysia validation succeeded
  for shop metadata, 10 of 130 recent-window orders, 10 of 130 products, and 42
  SKU inventory projections from the first 10 products; all four requests have
  successful connector audit records and provider request IDs. Product and
  inventory writes remain disabled. Focused OAuth and Connector tests pass
  16/16, the full repository suite passes 970/970, and the production build
  passes. The already-running port-3101 main server and port-8977 OAuth process
  predate the final code changes; an attempted narrow OAuth restart was rejected
  before execution by the terminal safety policy, so both services remain
  undisturbed and need one normal restart to load the new Gateway route and
  automatic future-authorization bridge.

<!-- fact-id: event.2026-08-06.lazada-second-shop-authorized -->
- Lazada App 1 completed a second real seller authorization through the current
  Cloudflare Callback. The local OAuth service validated the one-time state,
  exchanged and encrypted the token, and increased the App 1 store count from
  one to two. The Connector control plane synchronized the new Malaysia shop
  `Elephantt`, seller ID `300153639083`, short code `MY4N9V2YUI`; a signed
  read-only `/seller/get` verification returned `ACTIVE` and verified status.
  The authorization is active and expires on 2026-09-05T12:35:13.829Z. No OAuth
  code, state, access token, or refresh token is retained in memory.

<!-- fact-id: event.2026-08-06.postgresql-phase3d-rehearsal-passed -->
- PostgreSQL Phase 3D completed a guarded full-source cutover and rollback
  rehearsal in the independent `commerce_ops_cutover_rehearsal` database while
  production remained on SQLite. A pinned online-backup snapshot passed
  integrity and zero-FK checks and synchronized 90/90 source tables, 15/15
  views, 1,564 columns, and 3,875,411 rows. Full-row/key digests, bidirectional
  primary-key differences, 151 validated foreign keys, 312 source-table
  indexes, ten business totals, 42 deterministic samples, and Agent
  observability projections all matched. The restricted `commerce_app` role,
  Product/Sales/Inventory/Task/Audit/Agent Run/Tool Invocation writes, eight
  Repository write contracts, process-scoped PostgreSQL health, and pinned
  SQLite rollback startup all passed. Three successful reconcile batches took
  104 ms, 3,941 ms, and 4,754 ms; the small-sample p95 was 4,754 ms, while the
  final end-to-end rehearsal took 722,143 ms. Concurrent SQLite migration 027
  was detected and covered by PostgreSQL migration 007. Development migration
  028 is present but not applied to formal SQLite; PostgreSQL migration 008 was
  prepared and passed an isolated Phase 3B rebuild with all eight migration
  checksums. Hardening was reverified with TLS 1.3, encrypted WAL archival,
  slow-query evidence, I/O timing, and `pg_stat_statements`; full tests passed
  976/976, Build passed, and Doctor reported SQLite integrity `ok` with only
  expected occupied-port warnings. The formal candidate `commerce_ops` remains
  empty, no provider switch was made, and `is_switch_ready=false` pending active
  development completion, explicit formal-candidate initialization/warm-sync
  approval, final writer freeze/full sync, and separate switch approval.
  Canonical operator guidance and evidence are in
  `docs/design/COMMERCE-OPS-POSTGRESQL-PHASE3D-CUTOVER-RUNBOOK.md` and
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PHASE3D-READINESS-20260806.md`.

<!-- fact-id: event.2026-08-06.lazada-authorization-progress -->
- Lazada authorization now has 20 connected shops under App 1 and 20 under
  App 2, for 40 distinct Connector-managed shops: 19 Malaysia, nine Thailand,
  and 12 Philippines. Both App 1 and App 2 have reached their planned 20-shop
  capacity. App 3 has not yet been created/configured, and the user explicitly
  paused further Lazada authorization until it is ready. The latest
  five App 2 Callback exchanges all succeeded and added the Philippines shops
  `WoodenMALL`, `Radiant Retreat Home Furnishings.PH`, `colo mall`,
  `LACCOHUG MALL PH`, and `HOPE HOME`. All five are stored under `app-2` with
  active encrypted authorizations and passed signed `/seller/get` verification
  with active and provider-verified status. The preceding 15 App 2 shops remain
  active. None matches the user's still-missing Malaysia target shops `MOMOCO`
  or `Perabot Besi`, so the 19-shop target roster remains at 17/19. The earlier
  compatibility-sync fix continues to preserve provider-verified shop names
  and merged metadata. This progress record contains no OAuth code, state,
  access token, or refresh token and should be updated rather than duplicated
  as more stores are authorized.

<!-- fact-id: event.2026-08-06.platform-api-connections-page -->
- Commerce Ops Vue now includes a governance page at `/platform-connections`
  named `平台 API 接入`. It reads the existing Commerce API Gateway status,
  platform, and shop endpoints and displays only non-sensitive platform,
  application, seller, country, authorization-expiry, and current-callability
  metadata. The page provides search, platform/country/application/callability
  filters, 25-row pagination, desktop tables, mobile cards, loading/error
  recovery, and a navigation entry under `治理`. Live browser verification
  showed 40 connected and currently callable Lazada shops across MY, PH, and
  TH, split between App 1 and App 2; App 3 remains unconfigured. No backend,
  database, provider-write, or token-storage change was made, and secrets or
  token values are never returned to the UI. TypeScript/Vue checking passed,
  related tests passed 24/24, the production Vue build passed after retrying a
  transient Windows output-file normalization lock, browser search filtering
  passed, the console had zero errors, and the 375 px layout had no horizontal
  overflow.

<!-- fact-id: event.2026-08-06.current-architecture-preview-generated -->
- A current-state Commerce Ops architecture preview was generated as editable
  SVG and 3840x3520 PNG under `outputs/architecture/`. Compared with the older
  architecture sketch, it now shows the active Vue 3 workbench, AI Agent
  Foundation 1.4.1 Runtime/Context/Tool boundary, task/approval/audit
  governance, the Commerce API Gateway and Platform Connector Registry, the
  live 40-shop Lazada Connector and separate encrypted Connector control
  plane, the local OAuth Callback sidecar, unified business data domains, and
  the guarded Database Provider abstraction. The diagram distinguishes current
  production SQLite from PostgreSQL Shadow/Staging/Cutover rehearsal and marks
  the formal provider switch as blocked; Shopee, TikTok, and standard Mabang
  Connectors are shown as extension targets rather than completed capability.
  SVG parsing and PNG rendering validation passed. No application behavior,
  provider selection, database, external API, token, or business data was
  changed while creating the preview.

<!-- fact-id: event.2026-08-06.postgresql-production-candidate-ready-for-final-cutover -->
- After explicit user authorization, the formal PostgreSQL candidate
  `commerce_ops` was initialized and refreshed from a pinned formal SQLite
  snapshot without changing the persistent provider. The latest complete run
  synchronized 92/92 source tables, 15 views, 3,875,429 rows and 1,616 columns;
  all full-row digests and bidirectional primary-key differences matched,
  156 foreign keys were validated, 319 indexes and all views matched, ten
  business totals and 42 deterministic samples passed, and the restricted
  `commerce_app` role plus Product, Sales, Inventory, Task, Audit, Agent Run,
  Tool Invocation and Repository write paths passed with fixture cleanup. A
  process-scoped candidate service and the exact formal production mode both
  returned healthy while persistent production remained SQLite. The candidate
  AES-256-GCM backup
  `commerce-ops-commerce_ops-20260806151154.dump.aes256gcm` was restored into an
  isolated temporary database with matching schema, migrations and business
  counts; the restore target was automatically removed. Phase 3C revalidation
  passes 21/21, the latest hardening proof confirms TLS 1.3, encrypted WAL,
  slow-query logging, I/O timing and `pg_stat_statements`, full tests pass
  1017/1017, Build passes, and Doctor keeps SQLite integrity `ok`. A new guarded
  cutover command defaults to plan mode and requires four exact confirmation
  tokens before it can stop only the identified main/scheduler writers, retain
  the final SQLite backup, perform the final full refresh and zero-difference
  validation, create a final encrypted candidate backup, mark the exact
  snapshot switch-ready, atomically persist PostgreSQL production mode, and
  validate main health plus the scheduler lease. The current preflight has no
  active scheduled, Price Control, repricing, Foundation, Growth, image or
  lifecycle work and has sufficient disk space. No technical preparation
  blocker remains, but production is still SQLite and `is_switch_ready=false`
  until the user gives the final freeze/switch confirmation. Reverse
  PostgreSQL-to-SQLite synchronization is not implemented. Canonical readiness
  evidence is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-FINAL-PRECUTOVER-READINESS-20260806.md`.
