---
memory_type: daily
date: 2026-08-07
updated_at: 2026-08-08
---

# 2026-08-07

## Events

<!-- fact-id: event.2026-08-07.postgresql-production-cutover-completed -->
- After the user confirmed the memory write-back and explicitly authorized the
  irreversible maintenance window, Commerce Ops completed its formal
  SQLite-to-PostgreSQL production cutover. The exact SQLite main and scheduler
  writers were already fully stopped and all known active-work counters were
  zero. A retained 3,514,544,128-byte SQLite backup passed integrity and zero-
  foreign-key checks with SHA-256
  `df5fe80166e7d150b741e5f5253a1e9d2146cfa7ccf95255bd588f9b70cd979f`.
  The same pinned source synchronized 92 tables, 15 views, 3,875,432 rows and
  1,616 columns into `commerce_ops`; the final reconcile inserted zero,
  updated only one scheduler-lease row, deleted zero and skipped 3,875,431
  unchanged rows. Both full validations passed with zero row/key/digest/view/
  business/sample differences, 156 validated foreign keys, 319 indexes, ten
  business totals and 42 deterministic samples. The final AES-256-GCM backup
  `commerce-ops-commerce_ops-20260806172534.dump.aes256gcm` was created with no
  retained plaintext. The exact validated snapshot was marked
  `is_switch_ready=true`, persistent `DATABASE_PROVIDER` was atomically changed
  to `postgres` under the `commerce_ops / FORMAL_CUTOVER` guard, and the main
  service plus scheduler restarted using `commerce_app`. HTTP health is 200,
  the scheduler lease is active with zero pending runs, Agent Observability is
  ready with zero active runs, and Daily Report Agent 2.1 history remains
  queryable. The first production monitor snapshot passed with 4% connection
  use, no blocked sessions, long transactions, waiting locks, deadlocks or
  alerts. PostgreSQL-only Audit and lease writes have begun, so silent rollback
  to SQLite is now unsafe because reverse synchronization does not exist. The
  canonical report is
  `docs/reports/COMMERCE-OPS-POSTGRESQL-PRODUCTION-CUTOVER-FINAL-20260807.md`.

<!-- fact-id: event.2026-08-07.postgresql-post-cutover-revalidation -->
- A post-cutover read-only revalidation matched the retained final SQLite
  snapshot SHA-256 to the production migration ledger. The latest full
  reconcile and validation remain successful for all 92 source tables and
  3,875,432 source rows, with zero differing tables, zero failed samples, 163
  currently validated application foreign keys, and zero invalid indexes.
  Production runtime and infrastructure monitoring passed against
  `commerce_ops`; health, Product, Sales, Inventory, Audit, and Agent APIs
  returned HTTP 200. The Growth Radar V2 task-list API is not currently
  healthy: its PostgreSQL path calls `SUBSTR` on the `paid_at` timestamp and
  fails with SQLSTATE `42883`. The underlying scheduler repository remains
  readable and healthy, so this is a post-cutover query-compatibility defect,
  not evidence of a migration data difference.

<!-- fact-id: event.2026-08-07.postgresql-post-cutover-capacity -->
- The live `commerce_ops` database is 3,637,958,335 bytes, 1.508% of the
  241,286,770,688-byte D: volume. The volume has 61,209,899,008 bytes (25.368%)
  free, leaving only 888,206,336 bytes before the configured 25% free-space
  warning threshold. All PostgreSQL databases occupy 13,100,704,630 logical
  bytes; the physical PostgreSQL data directory is 14,179,283,087 bytes,
  encrypted WAL archives are 21,307,132,900 bytes, PostgreSQL backup storage is
  22,263,931,544 bytes, and retained Commerce Ops SQLite backups are
  25,304,206,486 bytes. No cleanup or deletion was performed.

<!-- fact-id: event.2026-08-07.postgresql-live-growth-and-memory-check -->
- At 09:25 Asia/Shanghai, after a new source import recorded at 09:05, the live
  `commerce_ops` database had grown to 4,082,087,615 bytes. D: free space was
  59,854,745,600 bytes (24.806%), below the configured 25% warning threshold.
  The production monitor still returned `PASS` because its alert logic checks
  the 15% critical disk threshold but does not currently emit the configured
  25% warning. PostgreSQL had 128 MB `shared_buffers`; summed PostgreSQL process
  working sets were about 1.20 GB (an upper bound because shared pages can be
  counted more than once), and the three Commerce Ops Node processes used about
  0.89 GB working set. The machine had about 11.87 GB RAM free. No cleanup or
  runtime configuration change was performed.

<!-- fact-id: event.2026-08-07.safe-disk-cleanup -->
- With the user's authorization to clean D: space, a scoped cleanup removed
  only three completed-migration temporary directories under the workspace:
  `tmp/postgresql-phase3c`, `tmp/postgresql-incremental-sync`, and
  `tmp/postgresql-shadow-phase1`. About 31.65 GB of physical space was
  recovered; D: free space increased from about 63.38 GB to 95.02 GB (39.382%).
  The production `commerce_ops` runtime check remained `PASS`, HTTP health
  remained 200, and both the retained final SQLite cutover snapshot and final
  encrypted PostgreSQL backup were confirmed present. No production database,
  WAL archive, historical backup, personal file, provider setting, or runtime
  configuration was deleted or changed.

<!-- fact-id: event.2026-08-07.pandh-ready-to-ship-execution -->
- After the user explicitly authorized the real bulk action, the Lazada
  Connector and Commerce API Gateway gained `get_order_items` and guarded
  `ready_to_ship` capabilities plus corresponding HTTP routes. The default
  global write gate remains disabled, POST calls are not automatically
  retried, and tests cover the connector payload, fail-closed gate, audit, and
  HTTP routing. A read-only preflight uniquely resolved the PANDH Philippines
  shop and found nine packed orders containing ten unique eligible packages;
  one already-confirmed item in a mixed-status order was excluded. The first
  provider request was rejected during mandatory-parameter validation and did
  not change any package. After correcting the documented `readyToShipReq`
  wrapper, Lazada returned package-level success for all ten packages. Readback
  confirmed zero remaining packed orders and every targeted item in
  `ready_to_ship`; the excluded confirmed item was unchanged. The focused
  Platform Connector suite passed 12 of 12 tests.

<!-- fact-id: event.2026-08-07.lazada-authorization-package-pushed -->
- A sanitized Lazada authorization-center package was committed as
  `16693ab` on branch `codex/lazada-authorization-package` and pushed to the
  public GitHub `origin`. The package contains the reusable multi-app OAuth,
  encrypted token control plane, Platform Connector/Gateway, Cloudflare Tunnel
  helpers, guarded ReadyToShip support, tests, architecture documentation, an
  environment-variable template, and an aggregate snapshot of 40 active shop
  authorizations split evenly across Apps 1 and 2. Because the repository is
  public, the commit deliberately excludes `.env`, App Keys and Secrets,
  access/refresh tokens, encryption keys, OAuth codes/states, the encrypted
  authorization database, live tunnel values, shop/seller identities, token
  expiry details, customer data, and order data. OAuth tests passed 8 of 8 and
  Platform Connector tests passed 12 of 12 before the push; the remote branch
  SHA was verified equal to the local commit.

<!-- fact-id: event.2026-08-07.lazada-report-api-capability-audit -->
- A read-only audit used the active `DreamHug.th` Thailand authorization on
  Lazada app-2 and the official Open Platform API catalog to map nine Seller
  Center exports. Reverse-order listing, finance transaction details, account
  transactions for Sponsored Solutions top-ups, seller metrics, and legacy
  Sponsored Discovery campaign/product reports are documented and accepted by
  the current app. Exact public Open API equivalents were not found for
  Sponsored Max Store, Sponsored Max Product, Sponsored Max Supernova, Super
  Affiliate income, or the Business Advisor dashboard export; the legacy ad
  reports are only partial substitutes and must not be presented as those newer
  reports. The 2026-08-06 probes returned zero matching reverse, finance,
  top-up, and campaign rows for the test shop without exposing business data.
  Reverse-order date filters also exposed a connector signing defect: official
  time-parameter names contain uppercase characters, while the current signer
  uses locale-aware key sorting; bytewise sorting succeeded in a diagnostic
  request, so the failure is local signature canonicalization rather than API
  permission.

<!-- fact-id: event.2026-08-07.lazada-customer-service-api-permission-audit -->
- Lazada's official Open Platform catalog exposes Instant Messaging APIs for
  session/message reads, replies, read-state synchronization, opening recent-
  order conversations, and recalling eligible messages, plus separate product
  review reply, reverse-order communication history, and Seller Center message
  APIs. Read-only `/im/session/list` probes against active Thailand shops on
  both app-1 and app-2 returned `InsufficientPermission`; no session or message
  content was read or exposed. The existing applications therefore need the
  Instant Messaging API permission approved before Commerce Ops can integrate
  buyer chat.

<!-- fact-id: event.2026-08-07.product-package-database-sync-activated -->
- Product Center replaced the prior manual package baseline with a read-only
  database sync from `AI_Project_A.product_package`. Before mutation, the
  encrypted production backup
  `commerce-ops-commerce_ops-20260807024724.dump.aes256gcm` was verified with
  SHA-256
  `eef8a1d0e30e96873b90aaa1c3570a44e9bb87ebb4da8254563cde1f56a38683`;
  no plaintext dump was retained. The old product-package and image records
  were cleared, active product image directories were recreated empty, and
  the previous image files were moved to the recoverable backup
  `D:\CommerceOpsBackups\product-center-reset\20260807025738`. PostgreSQL
  migrations 009 through 011 added sync runs, latest-only package storage,
  catalog reference indexes, and the transaction-scoped sync workspace. The
  optimized initial run `c1403049-08e6-484c-b05b-6a4506d13c3f` committed
  119,245 source rows and 119,245 initial change markers; every stable source
  key is unique and a live product detail exposes all 62 source fields. A
  second manual run and the scheduler's same-day 09:00 catch-up both returned
  `NO_CHANGES` with 119,245 unchanged rows and zero new, updated, removed, or
  field-change rows. Production now exposes 75,797 active SKU projections,
  while Product Center image/media tables and image directories remain empty.
  The main service and scheduler restarted successfully, HTTP health returned
  200, the product-package status/list/detail/change APIs passed live checks,
  the final product/package/provider/Vue suite passed 58 of 58 tests, the Vue
  production build passed, and the PostgreSQL production runtime check passed.

<!-- fact-id: event.2026-08-07.product-center-selectable-columns -->
- A full production audit confirmed that all 119,245 latest product-package
  rows contain exactly all 62 source keys, with zero incomplete rows. Product
  Center now returns those raw values in each paginated SKU summary and offers
  67 persistent main-table choices: the five original summary columns and all
  62 database fields. After the user supplied the canonical table screenshot,
  the default header order was restored to image, SKU/product, country/main
  SKU, category/specification, status, data status, updated time, and
  operations. Image and SKU/product remain fixed at the start, operations is
  fixed at the end, and selected database fields are inserted before it. The
  user screenshot then showed that the package-change table still occupied the
  first visible table position; the real DOM order was corrected so SKU master
  data now precedes the package-sync change panel. The
  Vue migration omission was also corrected by restoring the top-level Mabang
  sync and exact-match entries, the row image manager, the detail image
  section, and persistent detail/edit/image buttons; the original detail and
  edit handlers plus Mabang endpoints remain in use.
  PostgreSQL migration 012 added the
  `latest_import_row_id` lookup index. Live preference save/read/restore and a
  62-field product response passed, the focused suites passed 59 of 59 tests,
  the Vue production build passed, production runtime validation passed, and
  the main service, scheduler, and Mabang image service returned to healthy
  listening state after restart.

<!-- fact-id: event.2026-08-07.product-package-review-workbook -->
- A review workbook was generated from the production latest-only product
  package snapshot. It records the 119,245-row/62-field acquisition contract,
  documents every source field with real sample values, and includes 100 real
  canonical product rows across all 62 source fields. All three sheets were
  rendered for visual inspection and the workbook contains no formula errors.

<!-- fact-id: event.2026-08-07.lazada-app1-capacity-and-ziniao-authorization-queue -->
- The user confirmed that Lazada approved app-1 for up to 60 authorized sellers
  and that the additional seller whitelist entries were submitted. The current
  Connector database started this batch with 20 active app-1 shops, so the
  supplied 39-shop Excel batch could bring app-1 to 59 shops. All 39 workbook rows were uniquely
  matched to accessible ZiNiao browser environments, with no direct overlap
  against the existing 40 Connector shops. ZiNiao CLI 1.0.7 and its local ZClaw
  bridge passed the local health check. A fresh free Cloudflare quick tunnel
  was started and the local `.env` callback values were updated; the public
  endpoint returned HTTP 200, and the user updated app-1 to the new callback.
  The full 39-shop queue was scanned one shop at a time. Eleven shops were
  authorized successfully: all eight Singapore targets plus
  `VS0024-Miki Space Home`, `VS0029-Ibed STORE`, and `VS0012-HomeSense` from
  Vietnam. Their tokens were exchanged and encrypted, and app-1 increased from
  20 to 31 active shops with zero pending codes. The other 28 shops were not
  saved during this automated pass: 10 encountered reCAPTCHA and were skipped, 13 had rejected saved
  credentials, three had no prefilled credentials, and two had an unusable
  seller-login page/button. The database check returned 31 app-1 rows, all in
  active status. No CAPTCHA was bypassed, and authorization codes, OAuth
  states, passwords, and tokens were not written to memory. When the user chose
  to retry the failed shops manually, the prior free quick tunnel was no longer
  running. A new quick tunnel was started successfully and its public app
  status endpoint returned HTTP 200; manual OAuth retries are waiting for the
  user to update app-1's callback to the newly generated HTTPS callback before
  issuing the next one-time authorization link. After that callback update,
  the user manually completed `VS0075-BMZAR.VN`; its token was exchanged and
  encrypted successfully, increasing app-1 to 32 active shops and reducing the
  unresolved batch to 27 shops. The next one-time manual link was issued for
  `VS0039-HIMO Store`. The user then completed `VS0039-HIMO Store`; its token
  was saved successfully, increasing app-1 to 33 active shops and reducing the
  unresolved batch to 26. The restarted OAuth service now reports the current
  quick-tunnel callback, so the reusable app-1 launcher `/lazada/auth` is valid
  for the remaining manual authorizations. Four more manual callbacks were
  exchanged successfully and matched by returned seller ID to `VS0011-DAMIK`,
  `YS0067-COUNTRY HOUSE`, `YS0097-BMZAR`, and `YS0218-NINI Toko.ID`. The
  database now has 37 active app-1 shops, zero pending codes, and 22 unresolved
  shops from the original 39-shop batch. A later four-callback batch was also
  exchanged successfully and matched to `BS0514-Seemon mall`,
  `BS0489-T-YUANSY`, `BS0814-Sabaai Sleep`, and `BS0425-S-NAIDE`. The database
  then reached 41 active app-1 shops with zero pending codes, leaving 18 shops
  unresolved from the original batch. A subsequent 12-callback manual batch
  was fully successful and matched to six Vietnam shops (`VS0040-canva`,
  `VS0064-MINII House`, `VS0028-BOHO Officeial store`,
  `VS0098-SUNNY BIERE MALL`, `VS0045-ROCOCO MALL`, and
  `VS0049-YOYOO MALL`), four Indonesia shops (`YS0306-Peru.Mah`,
  `YS0074-BMZAR ID`, `YS0305-Sustainable Zenith`, and
  `YS0290-XUXUMall.ID`), and two Thailand shops (`BS0951-Sanuk Living` and
  `BS0603-CFTHOME`). The database reached 53 active app-1 shops with zero
  pending codes, leaving six shops unresolved from the original batch. Five
  more callbacks were then saved and matched to `VS0027-KUCA MALL`,
  `BS0635-Cosyy homes`, `YS0076-KUCA.MALL.ID`,
  `YS0510-Kursi Beruntung`, and `YS0034-STON HOUSE`. The database reached 58
  active app-1 shops with zero pending codes; only `YS0073-Freemax.Mall.id`
  remains unresolved from the original 39-shop batch.

<!-- fact-id: event.2026-08-07.liaoliao-ai-assistant-phase1 -->
- Commerce Ops added the standalone Python module
  `integrations/liaoliao-ai-assistant` for phase-one ChatPlusAI (乐聊) reply
  assistance. Browser and production-bundle evidence confirmed that
  `mai.zhisuitech.com` is a React SPA with `#/login` and
  `#/workbench/conversation`, readable login controls, and workbench-owned
  conversation/message response paths. The implementation saves Playwright
  storage state including IndexedDB, passively observes the page's responses,
  scrolls and clicks unread conversation rows with configurable DOM fallbacks,
  deduplicates shops/customers/conversations/messages in a dedicated SQLite
  database, generates OpenAI-compatible LLM suggestions, provides a local
  FastAPI review page and local processed marker, and records rotating and
  database audit logs. A follow-up added visible `liaoliao assist` operation:
  it fills a suggestion only when the reply editor is empty, waits for the
  operator to modify or send it, and contains no Enter, Send-button, or message
  endpoint action. The optional review page moved to port 8876 after confirming
  that port 8765 is the Mabang Python service. Ten automated tests pass; a
  real local Chromium test filled the draft while recording zero Send clicks;
  the configured DeepSeek model returned a successful anonymous test reply;
  dependency and wheel checks pass, with the assistant, selectors, migration,
  template, and static asset included. The operator completed the real login
  and the Session was saved. Authenticated calibration found 13 rendered
  `data-key="convItem"` rows, the real reply textarea, and the workbench's
  `advanceQueryConversationList`, `queryConversation`, and `syncMessage`
  responses. A completed live collection run persisted 92 conversations and
  307 messages and generated 100 suggestions; the later database snapshot held
  50 shops, 118 customers, 330 messages, and 132 ready suggestions without
  writing customer PII to memory. Suggestion generation now uses an atomic
  claim to prevent duplicate workers. Port 8876 and the visible `assist`
  process are running; `assist` has not filled a historical draft and is
  waiting for a new unread message.

<!-- fact-id: event.2026-08-07.liaoliao-assistant-queue-context -->
- The 乐聊 assistant was upgraded from a blocking one-draft workflow to a
  durable SQLite task queue. After filling an empty reply editor it immediately
  switches to the next conversation; an existing draft delays only its own
  conversation, and newer messages supersede older queued messages from the
  same conversation. A new context snapshot captures the visible right-side
  order, logistics, and product panel before generation. The prompt also loads
  optional global, platform, and exact-shop policies from `knowledge/`, which
  remains empty until approved policy content is supplied. A reversible live
  test confirmed that a draft survives switching away and back and was then
  cleared without sending; the extracted panel contained 958 characters and
  matched order, logistics, and product signals. Migration
  `002_assistant_queue_and_context` is applied, 12 automated tests and wheel/
  dependency checks pass, port 8876 is healthy, the new `assist` process is
  running with an empty queue, and no batch `collect` process is running.

<!-- fact-id: event.2026-08-07.liaoliao-quality-review-optimization -->
- The local 乐聊 assistant was upgraded to package version 0.4.0 without
  deploying it into the main Commerce Ops application. SQLite migration
  `003_quality_feedback_and_review` adds structured suggestion metadata,
  adopted-reply feedback, and a durable human-review send-action queue. The
  generator now structures the visible order/logistics/product panel, applies
  versioned global/platform/region/product/shop/intent knowledge precedence,
  retrieves prior human-adopted examples, emits intent/risk/fact metadata, and
  runs a second LLM quality-review pass. Historical suggestions without the new
  quality status cannot be approved for sending until regenerated. The local
  `/?view=review` flow supports editing, feedback capture, regeneration and a
  dedicated approval shortcut. A Send can occur only from an explicit local
  human approval protected by a per-process CSRF token; the browser worker
  rechecks the latest inbound message, suggestion hash, exact conversation and
  editor content, never uses Enter, and never retries an ambiguous result. New
  inbound messages invalidate older pending approvals. A pre-migration online
  backup `backups/liaoliao-pre-v040-20260807-201630.db` passed integrity checks
  with 883 retained messages; the upgraded live database also reports
  `integrity_check=ok`. Twenty-two automated tests, Python compilation,
  dependency checks and the 0.4.0 wheel build pass. An anonymous two-stage LLM
  test and one existing-message regeneration produced quality-checked replies;
  no real customer message was sent, `review_actions` and sent-action counts
  remained zero, and no customer PII was written to memory. Runtime hardening
  now isolates missing reply editors and modal-intercepted clicks to the current
  conversation instead of terminating monitoring. Port 8876, the visible
  Playwright browser and `assist` are running after a full retry-cycle check.

<!-- fact-id: event.2026-08-07.shopee-token-broker-sync -->
- With the user's explicit request to retain current Shopee authorizations for
  future data access, Commerce Ops added an internal token-broker client,
  atomic shop/token synchronizer, encrypted Connector cache, guarded CLI, and
  connector refresh seam. A consistent pre-write SQLite backup
  `backups/platform-connectors-before-shopee-tokens-20260807-210059.sqlite`
  passed `integrity_check=ok`. The live synchronization stored 56 visible
  Shopee shops and 42 active encrypted access-token authorizations in the
  existing Connector SQLite control plane; 14 unbound shops retain metadata
  without authorization. All 42 ciphertext rows use the AES-256-GCM `v1`
  format, decrypt successfully through the repository, share application ID
  `2013594`, and have no API-key/access-token plaintext or secret fields in the
  database bytes or metadata. The Git-ignored token-broker environment file is
  ACL-restricted to the current Windows user, SYSTEM, and Administrators, and
  no secret appears in tracked files. Connector and Shopee tests pass 15/15.
  The Shopee connector is registered for broker-backed token renewal, but the
  platform remains `planned`: direct Shopee resource reads stay fail-closed
  until the application `partner_key`, signing, normalization, and production
  read validation are added. No token-broker PUT, Shopee provider write, main
  service restart, or PostgreSQL change was performed.

<!-- fact-id: event.2026-08-07.shopee-command-relay-validation -->
- The user supplied the authoritative Shopee command-relay contract, which
  supersedes the earlier assumption that Commerce Ops needs the application's
  `partner_key`. The Mac mini endpoint `POST /api/shopee/call` performs provider
  signing and enforces the caller's organization shop scope without returning
  partner or token credentials. A live read-only call for authorized shop
  `1618749121` to `/api/v2/shop/get_shop_info` returned HTTP 200 with shop name
  `Vinco MALL`, region `TH`, status `NORMAL`, no provider business error, and no
  credential fields in the response. The relay itself does not block provider
  writes, so it must not be exposed as a generic Agent/business tool; Commerce
  Ops integration requires explicit read-only path allowlists and retains its
  existing write gates. No write call or runtime activation was performed.

<!-- fact-id: event.2026-08-07.lazada-shopee-platform-api-branch-pushed -->
- Commerce Ops completed the Shopee business-data connector and pushed commit
  `fdbde54` on branch `codex/lazada-shopee-platform-api` to GitHub; the remote
  branch SHA equals the local commit. Lazada retains local encrypted OAuth and
  signing, while Shopee uses Gateway authorization mode `delegated`: the
  Connector never decrypts a local Shopee token and maps five normalized read
  capabilities to six fixed relay operations that always issue provider GET
  requests. Arbitrary provider paths, authorization paths, request bodies, and
  Shopee writes are unavailable, even when the Gateway global write switch is
  enabled. Gateway validation succeeded against active shops in TH, PH, MY,
  SG, TW, and VN; the Vinco MALL validation returned one recent order, two
  products with ten normalized SKUs, and four inventory records without a
  provider write. The focused Lazada/Shopee/Gateway suite passed 19 of 19.
  The full repository suite passed 1,033 of 1,037; its four failures are
  pre-existing dirty-worktree expectations for PostgreSQL readiness, Growth
  Radar migration ordering, Phase 3B migration ordering, and a Product Center
  manual-override fixture, not files in this commit. The pushed branch contains
  sanitized code, tests, documentation, and environment templates only; real
  relay keys, provider credentials, tokens, databases, backups, internal
  addresses, and business/customer records were excluded.

<!-- fact-id: event.2026-08-07.shopee-relay-key-rotated-local -->
- The user rotated the Shopee internal API key after the prior key returned
  HTTP 403. The new value was written only to the Git-ignored, ACL-restricted
  local Shopee environment file and was not printed, committed, or written to
  memory. A fresh token-service check returned 56 visible shops with 42 bound,
  and the scoped relay returned provider status `NORMAL` for the Vinco MALL
  shop-information read. No provider write was called.

<!-- fact-id: event.2026-08-07.customer-service-ai-center-v1-design -->
- The LiaoLiao customer-service target architecture was revised to V1.1 in
  `docs/design/COMMERCE-OPS-CUSTOMER-SERVICE-AI-CENTER-V1.md`, and the shared
  Product Knowledge architecture was separated into
  `docs/design/COMMERCE-OPS-PRODUCT-KNOWLEDGE-CENTER-V1.md`. Customer Service
  now owns only conversations, references, Context snapshots, suggestions,
  review and controlled browser actions; it consumes existing order, inventory,
  shop configuration and product-package truth through versioned Facades.
  Product knowledge belongs to Product Domain and publishes immutable SUPPORT
  and LISTING consumer views from the same approved Release. The design also
  separates deterministic fact lookup from knowledge retrieval and prevents
  `product_ai_contents` or adopted replies from becoming unreviewed knowledge.
  No production migration, deployment or customer send occurred.

<!-- fact-id: event.2026-08-07.unified-data-foundation-candidate -->
- Commerce Ops completed a production-read-only unified-data audit and a V1
  candidate architecture for Mabang orders, Mabang inventory, the database-
  synchronized product package, approved price control, shop detail and
  Platform Connector data. The code now centralizes source identities and
  fixes Sales/Assortment's obsolete `company_product_center` filter; a live
  read-only repository check returns all 119,245 authoritative product-package
  rows instead of zero. Shop Repository PostgreSQL placeholder/JSONB handling
  and Growth Readiness timestamp SQL were also corrected and verified against
  production reads. The audit found healthy validated database foreign keys
  but empty product identity mappings, all 80,500 order headers without a
  canonical shop, all 116,700 current order lines and 190,066 inventory rows
  without a canonical product, 139 shop records without Connector identity,
  currency or control shop type, 91,950 current prices without a matching
  product, and Connector application/identity tables still absent. The
  candidate migration adds dataset/module/lineage/quality governance, eight
  canonical views, confirmed external shop identities, non-secret API
  application profiles and strongly validated account-shop bindings. It passed
  an isolated create/apply/validate/drop rehearsal with production writes zero;
  45 focused contract/regression tests pass. No production migration, identity
  backfill, token move, table cleanup, service restart or external write was
  performed; formal application remains a separately confirmed step.

<!-- fact-id: event.2026-08-07.sixth-center-shop-api-authorization-workbook -->
- A new review workbook was generated from the user's 139-row Sixth Center
  shop roster without overwriting the source file. It reconciles 79 Lazada and
  60 Shopee rows against the live Commerce Ops Connector and the current
  company Shopee Token Broker inventory, adds platform seller/shop IDs,
  Lazada short codes where available, and authorization status. The result has
  116 authorized rows, 21 unauthorized or out-of-scope rows, and two explicit
  identity conflicts requiring review. Store type is populated only where the
  source roster explicitly identified C or Mall; the integrated provider
  metadata and the non-discriminating Mabang shop-type value were not used to
  guess missing types. The exported workbook passed post-export Artifact Tool
  import, formula-error, table-range, and visual checks.

## Handoff

- Production database provider is PostgreSQL, exact database `commerce_ops`.
- Keep `POSTGRES_PRODUCTION_MODE=true`, confirmation database `commerce_ops`,
  and confirmation scope `FORMAL_CUTOVER`; Shadow, staging, rehearsal and
  candidate modes remain false.
- Do not resume SQLite synchronization or change the provider back to SQLite.
  For an incident, freeze PostgreSQL writers and use a separately approved
  recovery/reconciliation procedure.
- The final SQLite backup is retained under `storage/backups/` and the final
  encrypted PostgreSQL artifact under `D:\PostgreSQLBackups\logical\`.
- Production runtime, backup, restore-plan, hardening-plan and monitor commands
  now recognize the exact post-cutover operational context. Monitor disk
  headroom: 61.24 GB / 25.38% free at the first post-cutover snapshot.
