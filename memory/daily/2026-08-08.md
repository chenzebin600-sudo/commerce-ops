---
memory_type: daily
date: 2026-08-08
updated_at: 2026-08-08
---

# 2026-08-08

## Events

<!-- fact-id: event.2026-08-08.s-naide-lazada-public-advertising-data-export -->
- A read-only, formula-driven Lazada advertising workbook was exported for the
  Thailand shop `BS0425 / S-NAIDE` to
  `outputs/lazada-ads-bs0425-20260808/BS0425-S-NAIDE_Lazada广告全量数据_截至20260808.xlsx`.
  The export contains all data obtainable in this run from Lazada's documented
  public Sponsored Discovery scope (`bizCode=sponsoredSearch`): 3,141 daily
  rows for 2018-01-01 through 2026-08-07, 21 current-day hourly rows, one
  Campaign and its detail, 69 historical item-level ad groups, and 5,172
  keyword rows. The ad-group endpoint's 90-day query-duration limit was handled
  with consecutive windows and exact-ID deduplication. It also includes 3,640
  `Payment` account-transaction rows for 2018-01-01 through 2026-08-08, clearly
  labeled as funding/payment ledger activity rather than actual ad spend. The
  workbook has 10 sheets, dashboard formulas and charts; post-export import
  verified all sheets and preserved long IDs, and both formula-error scans
  found zero matches. Sponsored Max / 全站推广 is explicitly excluded because
  Lazada's current public API documentation provides no corresponding report
  endpoint or documented `bizCode`; a Seller Center Sponsored Max export is
  still required to merge that product's data. No token, secret, provider
  write, or production-database write was retained or performed.

<!-- fact-id: event.2026-08-08.s-naide-sponsored-solutions-api-permission-validation -->
- Lazada's current public Sponsored Solutions documentation was checked against
  the `BS0425 / S-NAIDE` Thailand shop. The documented report surface remains
  the Sponsored Discovery `sponsoredSearch` scope: seller/solution overview and
  metric trend, campaign performance, and campaign ad-group performance where
  an ad group binds a Lazada `itemId` (listing/item grain rather than a child
  SKU variant). Live read-only calls through the shop's existing app-1
  authorization returned transport code `0` and business success for
  `getReportOverviewMetric`, `getReportOverview`, `searchCampaignList` and
  `searchAdgroupList`; the tested window returned one campaign and zero
  ad-group rows. This proves the shop and app can call the documented Sponsored
  Discovery reporting APIs. It does not prove Sponsored Max reporting access:
  the public API detail still supplies no documented Sponsored Max `bizCode` or
  corresponding endpoint, while the Seller Center screenshot is explicitly the
  Sponsored Max overview. No token, secret, provider write, advertising detail,
  or application code change was retained.

<!-- fact-id: event.2026-08-08.s-naide-account-transactions-payment-api-validation -->
- A live read-only validation confirmed that the existing Lazada app-1
  authorization for `BS0425 / S-NAIDE` can call
  `POST /finance/transaction/accountTransactions/query`. Filtering
  `transaction_type=Payment` for 2026-08-01 through 2026-08-08 returned
  provider code `0`, success `true`, and 21 rows; every row was type `Payment`
  and subtype `Sponsored Solutions Top-up`, matching the Seller Center
  `Finance > My Balance > Balance Transactions > Payment` / advertising top-up
  data path. A second read-only call using the screenshot's full 2026-02-08
  through 2026-08-08 range also succeeded and reported 1,256 matching Payment
  rows, with pagination required beyond the first 100. The endpoint supports
  required `yyyyMMdd` start/end filters and returns transaction type, subtype,
  transaction time, amount, currency, and transaction number. No token,
  secret, provider write, or application code change was made.

<!-- fact-id: event.2026-08-08.s-naide-payment-total-20260727-20260807 -->
- A complete paginated read-only Lazada account-transaction calculation for
  `BS0425 / S-NAIDE`, using inclusive dates 2026-07-27 through 2026-08-07 and
  `transaction_type=Payment`, returned 37 THB rows. All rows were subtype
  `Sponsored Solutions Top-up`, all amounts were negative, the signed ledger
  total was THB -3,959.00, and the corresponding outgoing payment total was
  THB 3,959.00. No transaction identifiers, token, secret, provider write, or
  application code change was retained.

<!-- fact-id: event.2026-08-08.s-naide-total-advertising-related-expense-20260727-20260807 -->
- Applying the user-confirmed Lazada advertising-related expense rule to
  `BS0425 / S-NAIDE` for the same inclusive 2026-07-27 through 2026-08-07
  Thailand date range produced a signed advertising-ledger amount of THB
  -3,959.00 and a signed matched Finance Details amount of THB -1,680.67.
  Finance Details were fetched through padded UTC windows and then strictly
  filtered by Thailand-local transaction date. A subsequent exact-identity
  audit found 100 rows repeated across adjacent inclusive request windows: 55
  on 2026-07-31 and 45 on 2026-08-04. Removing only those cross-window copies
  produced 428 unique rows with a maximum date of 2026-08-07 and zero
  2026-08-08 rows. The matched expense API fee names were `Sponsored
  Affiliates` (THB -1,809.62) and `Sponsored Affiliates Refund` (THB +128.95).
  `Premium Package` (THB -7,308.24) and
  `Reverse - Premium Package` (THB +471.76) were present but excluded after the
  user clarified that these are platform program-package rows belonging to the
  profit module. Other user-listed expense categories had no returned row in
  this range. The deduplicated combined signed amount is THB -5,639.67, so the
  displayed positive shop expense value is THB 5,639.67. No
  transaction identifiers, token, secret, provider write, or application code
  change was retained.

<!-- fact-id: event.2026-08-08.s-naide-store-bill-workbook-20260727-20260807 -->
- A formula-driven Lazada store-bill workbook was exported for `BS0425 /
  S-NAIDE` covering the inclusive 2026-07-27 through 2026-08-07 Thailand date
  range. The first export was found to include 25 Finance rows dated 2026-08-08
  because an API UTC boundary had been treated as a Thailand-local day
  boundary. The date-corrected workbook then exposed a second export defect:
  100 exact rows were repeated across adjacent inclusive API windows, including
  one `Sponsored Affiliates` THB -213.37 row on 2026-08-04. The duplicated row
  had the same transaction number, order number, statement and amount in both
  source windows, matching the user's one-row manual export evidence. The
  workbook at
  `outputs/019fe085-b514-7041-b828-86c02bd210fb/lazada-s-naide-bill/BS0425-S-NAIDE_店铺账单_20260727-20260807_已校正.xlsx`
  is therefore also superseded and must not be used for this period. A final
  deduplicated workbook has not yet been generated. No provider or
  production-database write was performed.

<!-- fact-id: event.2026-08-08.manage-profit-fee-dashboard-skill-installed -->
- The user-provided `manage-profit-fee-dashboard` Codex skill was installed
  from the local ZIP into the personal Codex skills directory. The installed
  package contains its metadata, calculation and data-schema references, and
  the dependency-free starter dashboard. Its required files and declared skill
  name were validated, and the starter `app.js` passed `node --check`.

<!-- fact-id: event.2026-08-08.lazada-finance-api-s-naide-validation -->
- A read-only live validation confirmed that Commerce Ops' existing Lazada
  app-1 authorization resolves `BS0425 / S-NAIDE` to the verified active
  Thailand seller and can call `GET /finance/transaction/details/get`. For the
  Seller Center screenshot range 2026-07-31 through 2026-08-07, four paginated
  calls returned 304 finance-detail rows. After correctly parsing comma-
  formatted amount strings, the API's positive, negative, and net sums matched
  the screenshot's Revenue, Deductions, and Total Amount exactly. The response
  directly supplies the profit-dashboard's required fee name, amount, order
  number, statement, order-item, SKU, tax, paid-status, and transaction fields;
  shop short code is supplied by `GET /seller/get`. No token, secret, provider
  write, or application code change was made.

<!-- fact-id: event.2026-08-08.s-naide-mabang-finance-order-linkage-audit -->
- A production-read-only audit of `BS0425 / S-NAIDE` found nine Mabang order
  headers and nine current order lines in PostgreSQL, all from source query
  dates 2026-08-05 and 2026-08-06. The nine raw Mabang transaction identifiers
  are distinct, nonblank 16-digit text values with no scientific notation, and
  none is marked as an evaluation order. The Lazada finance window from
  2026-07-31 through 2026-08-07 contains 304 fee-detail rows for 52 distinct
  `order_no` values. Applying the dashboard's exact linkage rule
  `finance.order_no = Mabang 交易编号` matched one distinct order, represented
  by ten finance-detail rows; 51 finance orders have no match in the currently
  retained Mabang scope, while eight of the nine retained Mabang transactions
  have no finance transaction in that finance-date window. This is a source-
  coverage limitation rather than proof of missing upstream orders because
  finance transaction date and Mabang payment date are different semantics.
  The canonical shop name also normalizes to `s naide` while the Mabang source
  mapping normalizes to `s-naide`; all nine order headers still have a null
  canonical shop ID, so future canonical-ID automation needs that mapping
  issue resolved. No database, provider, external write, or application code
  change was made.

<!-- fact-id: event.2026-08-08.shop-directory-platform-connections-sync -->
- The Platform Connections module was aligned with the latest managed-shop
  workbook and deployed as a canonical PostgreSQL shop directory plus a live
  Connector authorization projection. Production migrations 013 and 014 were
  applied only after a verified encrypted full backup with no retained
  plaintext. The final import contains 139 unique shop rows with no duplicate
  directory rows. An initial name-normalization mismatch created duplicate
  candidates; it was detected immediately, proved unambiguous with read-only
  checks, transactionally merged into the original rows, and the importer was
  corrected before the final idempotent rerun.

<!-- fact-id: event.2026-08-08.shop-directory-platform-connections-validation -->
- The deployed Platform Connections API reports 139 shops: 116 authorized and
  callable, 21 not authorized, and 2 identity conflicts requiring review.
  Lazada status is projected from the encrypted Connector control plane, while
  Shopee status is read from the delegated Token Broker through a short cache.
  The module now supports both human creation and idempotent spreadsheet/API
  system sync, and it never copies provider tokens into PostgreSQL. Focused
  backend/API/UI tests passed 20 of 20, frontend TypeScript and production-build
  checks passed, and the live service on port 3101 returned the expected totals.

<!-- fact-id: event.2026-08-08.sales-assortment-target-list-price-50 -->
- Sales and Assortment was upgraded from the unavailable product-package 45%
  tier price to the user-approved 50% target-profit list-price estimate. The
  product-package source viewer now ends with `50%目标利润标价`, calculated from
  local cost, and all CNY operating metrics calculate the corresponding price
  from CNY cost. Contract version `SALES-ASSORTMENT-1.4.0`, formula metadata,
  UI copy, price coverage, tests, and production assets were updated. Large
  product-package and inventory snapshot reads were serialized to avoid the
  PostgreSQL statement timeout observed on a cold dashboard load. Focused
  backend tests passed 29 of 29, Vue type checks and production build passed,
  the restarted local service returned 100% derived-price coverage across
  119,245 package rows, and browser validation found the new final column and
  no console warnings or errors.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-first-batch -->
- The first shared product-knowledge standardization batch was generated from
  the user-supplied local workbooks and four country directory trees without
  changing source files or production data. Hash-aware source registration,
  read-only Product Core identity/category matching, common-versus-country
  scope handling, evidence lineage, conflict detection, risk flags, category
  indexes, JSONL assets, and an 11-sheet review workbook were produced. The
  batch contains 18,463 Product Core candidates, 3,241 Product Knowledge
  claims, 4,724 accessory relations, 91 support-policy candidates, 172 support
  playbooks, 943 same-scope conflicts, 26 explicit country differences, and
  1,187 media candidates. All are review, mapping, or source-read candidates;
  none is approved for an AI Knowledge Release.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-source-read-gap -->
- Product Core matching across 15,833 extracted source SKUs resolved 13,450 to
  an exact stock-SKU model, found 1,352 exact stock SKUs whose current Product
  Core row lacks a main SKU, and left 1,031 unmatched. The 160 DingTalk shortcut
  sources were registered with country, source-category, and document-role
  metadata but their bodies remain unread because this environment has no DWS
  CLI or DWS MCP capability; they are fail-closed and cannot be retrieved for
  reply generation until that connector is available or the documents are
  exported locally.

<!-- fact-id: event.2026-08-08.inventory-semantic-separation-and-audit-status -->
- Growth Radar now parses and persists Mabang `未发货量` and
  `分仓调拨未发货量` as separate nullable inventory facts, with SQLite and
  PostgreSQL migration definitions. The unified data audit retains its
  existing missing counters and separately reports confirmed and unconfirmed
  order-line amount and days-of-supply counts. Actual-XLSX parser, SQLite
  persistence, audit-summary, Growth Radar foundation/backend, and V2 migration
  regressions passed 133 of 133 focused tests; no production migration was
  executed.

<!-- fact-id: event.2026-08-08.s-naide-goods-payment-mabang-export -->
- For `BS0425 / S-NAIDE`, the Lazada finance range 2026-07-31 through
  2026-08-07 contained 304 detail rows. Filtering the exact API fee name
  `Item Price Credit` (the Seller Center display name `货款`) produced 54 fee
  rows for 51 distinct order numbers. All 51 identifiers were found in the
  available official Mabang order export, yielding 55 product-detail rows and
  zero missing or unexpected orders. A local workbook was delivered at
  `outputs/manage-profit-fee-dashboard-20260808-bs0425/BS0425-S-NAIDE_马帮订单_货款_20260731-20260807.xlsx`
  with summary, selected-order and 62-column Mabang-detail sheets. Re-import
  verification confirmed 51 requested and matched text identifiers, 55 detail
  rows, no scientific-notation identifiers, and no formula errors. The four
  optional freight-cost fields absent from the current Mabang export template
  were retained as blank rather than guessed or converted to zero. Temporary
  customer-detail source files, intermediate JSON and QA previews were removed;
  no provider or production-database write was performed by this export task.

<!-- fact-id: event.2026-08-08.sales-amount-semantic-safety-fix -->
- The Sales and Assortment repository now carries order-line amount, amount
  status, and order currency into the service. Contract
  `SALES-ASSORTMENT-1.4.1` retains the prior target-profit-price values as
  explicitly estimated compatibility fields and publishes actual sales only
  when all included line amounts are confirmed and use one currency; missing,
  unconfirmed, partial, and mixed-currency coverage remains non-publishable
  with explicit status and counts. Deterministic reports and AI instructions
  call the modeled values standardized estimates instead of sales or GMV. The
  active Vue dashboard, overview, trend, and ranking labels now consume that
  distinction and expose actual sales as unavailable until its gate passes.
  Focused Sales, unified-data, and Daily Report Agent tests passed 29 of 29;
  no production data was changed.

<!-- fact-id: event.2026-08-08.s-naide-thailand-profit-fee-basic-model -->
- The `manage-profit-fee-dashboard` rules were applied to `BS0425 / S-NAIDE`
  for Lazada Thailand transaction dates 2026-07-31 through 2026-08-07. The
  calculation scope is the 51 transactions selected by the `货款` fee, with
  298 related finance-detail rows, 55 Mabang product-detail rows, and 39
  involved Thailand product-package SKUs. All 55 cost lines matched. Five SKUs
  had different costs across Thailand warehouses, so the calculation used the
  exact Mabang order warehouse `泰国TZ-AD仓-1308`, which resolved every
  ambiguity without guessing. Formula-driven results are THB 55,288.90 list
  revenue, THB 38,981.65 received revenue, and THB 29,337.427 total cost, with
  46.94% list margin, 24.74% received margin, and 17.44% list-to-received
  margin. The seven-sheet workbook is at
  `outputs/manage-profit-fee-dashboard-20260808-bs0425-profit/BS0425-S-NAIDE_泰国利润费用基础数据_20260731-20260807.xlsx`;
  all 14 workbook checks passed, re-import found no formula errors, and all 55
  sixteen-digit transaction identifiers remained exact text. No production
  data or provider state was changed, and temporary files containing customer
  details were removed after verification.

<!-- fact-id: event.2026-08-08.unified-field-mapping-v2 -->
- The six requested source families were completed as a machine-readable
  crosswalk covering 241 unique source fields with no missing canonical target
  and no duplicate source registration, plus six versioned identity/
  relationship rules. Shared NFKC normalizers, Asia/Shanghai business dates,
  explicit product/warehouse/shop/Connector key namespaces, DRAFT-only
  activation gates, and fail-closed shop strong-ID/country checks were added.
  Production-read-only preview at 01:40 CST found 39 entirely absent historical
  order fields, 2,227 unmatched latest inventory-product candidates, 13,812
  unmatched control-price product keys, 9 missing shop Connector bindings, and
  zero formally API-executable price-shop assignments. Candidate migration 014
  passed an isolated PostgreSQL rehearsal with no fact, view, module-binding, or
  backfill change and nine governance constraint checks; the isolated database
  was removed. Mapping CSVs, design, gap preview, and rehearsal report were
  generated. Production writes remained zero, and focused backend/UI tests and
  Vue TypeScript checks passed after the final label migration.

<!-- fact-id: event.2026-08-08.customer-service-control-plane-phase1 -->
- The first main-system Customer Service control-plane slice was implemented
  without applying a production migration or touching a real LiaoLiao account.
  Additive SQLite migration 033 and PostgreSQL Shadow migration 016 define
  encrypted accounts, workers, events, conversations, messages, panel/context
  snapshots, suggestions, evidence, reviews, commands, and send-action audit.
  Internal worker routes use a dedicated fail-closed token; event and message
  identity are idempotent, and a new message stales only the same
  conversation's prior suggestion and cancels its old draft command without
  blocking other conversations. The Vue workbench provides account setup,
  queue and message views, status/evidence placeholders, and auditable handled
  state. The existing Python/Playwright assistant now has an optional fail-open
  central bridge that never uploads credentials or browser Session state.

<!-- fact-id: event.2026-08-08.customer-service-phase1-validation -->
- Customer Service and shared-knowledge focused validation passed 20/20 Node
  tests, 27/27 Python LiaoLiao edge tests, Vue TypeScript checking and an
  isolated production build. The full repository suite reached the Customer
  Service tests successfully and retained two unrelated failures: the
  PostgreSQL readiness document does not yet list concurrent Profit tables,
  and the existing Product Center manual-override clearing test fails. The
  normal Vue output directory was held by the running local preview, so the
  final build was verified in a separate output directory without stopping the
  user's process. Production Customer Service/Knowledge migrations, worker
  activation, knowledge import and real-account validation remain unexecuted.

<!-- fact-id: event.2026-08-08.customer-service-central-reply-agent -->
- The main Customer Service control plane now includes encrypted exact browser
  routing, an immutable `customer_service` Context registration, an
  AgentRuntime-created Reply Agent, a central suggestion runner and bounded
  fill-only browser commands. Each conversation progresses independently; a
  newer inbound message cancels only that conversation's old suggestion and
  command. Generation records model, prompt version, token usage, evidence,
  confidence, risk and quality metadata without logging raw customer text.
  `FILL_DRAFT` explicitly sets `automaticSend:false`; high-risk drafts remain
  in Commerce Ops and the Playwright edge never clicks Send for central
  commands.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-runtime-scaffold -->
- Shared Product Knowledge migration 035 and PostgreSQL Shadow migration 018,
  provider-neutral repository/service/read API, digest-confirmed importer and
  published-only Customer Service resolver were implemented. The first package
  was revalidated in plan-only mode at 29,007 candidates with digest
  `1bc094f8f9458a978c2c9102bb36ce9ed5f119b16743ccbc176140bc5003881c`.
  Its 11-sheet review workbook was rebuilt with the bundled spreadsheet runtime,
  every sheet was visually inspected, and the formula-error scan returned zero.
  No production import, approval or Release publication occurred.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-governance-workbench -->
- Product Knowledge Phase 1 governance was implemented without applying a
  production migration or importing candidates. Migration 035 / Shadow 018 now
  pins product claims, accessory relations, Customer Service policies, and
  Customer Service playbooks to typed release membership. The repository,
  service, API, environment gates, Vue Product Knowledge workbench, Customer
  Service Context and Reply Agent prompt enforce per-record review, published-
  release-only retrieval, reviewer/publisher separation, high-risk compliance
  acknowledgement, and no bulk auto-approval. Plan-only import revalidated
  digest `1bc094f8f9458a978c2c9102bb36ce9ed5f119b16743ccbc176140bc5003881c`
  and 29,007 candidates with `productionMutationPerformed=false`. Focused Node
  validation passed 38/38, the LiaoLiao Python edge passed 27/27, Vue type and
  isolated production builds passed, and the full repository passed 1,088 of
  1,089 tests; the sole failure remains the unrelated pre-existing Product
  Center manual-override clearing case.

<!-- fact-id: event.2026-08-08.customer-service-no-send-rollout-hardening -->
- The Customer Service rollout path was hardened without applying migrations,
  importing candidates, logging into a real LiaoLiao account, or sending a
  customer message. New accounts are server-forced to observe-only even if the
  client requests a higher mode. Promotion is sequential and evidence-gated:
  one observed message is required before suggest-only, and one generated plus
  human-accepted/edited suggestion before draft-fill; downgrades remain open.
  The main Vue workbench supports editing,
  accepting, rejecting and manually queuing a fill while high-risk fills need
  explicit acknowledgement. Global AI and Draft Fill defaults are off. Exact
  confirmed shop plus exact Mabang order identity can now read authoritative
  Lazada/Shopee order-item logistics through the existing Platform Gateway;
  provider errors, mismatches and ambiguous identities remain unavailable and
  LiaoLiao panel logistics remains non-authoritative. The Python Edge validates
  the shared `CS_FILL_DRAFT_V1` account, message and safety contract, and the
  new Fleet supervisor isolates each account's process, Session, SQLite, logs
  and Worker ID while forcing legacy local programmatic sending off. Central
  account leases now exclude duplicate Workers before browser startup and scope
  event, command and result traffic to the exact account and lease token. A
  deterministic post-model gate rejects forged evidence references, unsupported
  logistics/order/stock assertions, unknown tracking identifiers, risky
  promises and low-confidence auto-fill; the Vue workbench exposes the gate
  reason, threshold, active leases and human review metrics. The final
  `npm run verify:customer-service:no-send` passed 39 Node/policy tests, 21
  targeted Python safety/Fleet tests, Fleet validation and Vue type checking;
  the complete Edge suite passed 35/35. Full-repository validation passed 1,102
  of 1,103 tests; the sole failure remains the unrelated existing Product
  Center manual-override clear case. Shared Knowledge plan-only
  validation again confirmed 29,007 candidates and digest
  `1bc094f8f9458a978c2c9102bb36ce9ed5f119b16743ccbc176140bc5003881c`
  with `productionMutationPerformed=false`.

<!-- fact-id: event.2026-08-08.customer-service-quality-feedback-closure -->
- Customer Service quality feedback now requires a controlled reason for every
  UI edit or rejection and persists a non-sensitive normalized edit ratio,
  original/final length and metric version. Suggestions persist intent, effective
  risk, country, confirmed shop, Product Model/SKU, category, provider, model,
  Prompt and actual input/output/total Token usage. The read-only quality API and
  Vue workbench aggregate confidence, low-threshold blocks, accept/edit/reject,
  edit size and Token totals by country, category, intent, risk, account, shop or
  model. `CS_REPLY_EVALUATION_V1` adds an offline JSONL replay that performs no
  model call; all three anonymous examples and 12 assertions passed. The final
  no-send verifier passed, and the full repository passed 1,102 of 1,103 tests;
  only the existing unrelated Product Center manual-override clear case failed.
  The running service on port 3101 still reports `MIGRATION_REQUIRED`; no formal
  database, knowledge release or real LiaoLiao state was changed.

<!-- fact-id: event.2026-08-08.customer-service-rollout-readiness-contract -->
- Customer Service rollout now uses one shared transition evaluator for both
  backend enforcement and the Vue account workbench. Each account exposes its
  current 1-of-3 stage, inbound-message, generated-suggestion and reviewed-
  suggestion counts, machine-readable blockers and next eligible mode. AI
  suggestion activation additionally requires a configured Reply Agent, ready
  Product Knowledge schema and at least one reviewed published SUPPORT Release;
  importing candidates alone is insufficient. `CS_DEPLOYMENT_READINESS_V1` and
  `npm run check:customer-service:rollout` provide read-only observe, suggest
  and draft preflights over schema, identity protection, no-send, global gates,
  knowledge publication and per-account evidence. The no-send verifier passed
  42/42 Node/policy tests, 3/3 offline cases with 12/12 assertions, 21/21 Python
  Edge tests, Fleet validation and Vue type checking. The full repository passed
  1,105 of 1,106 tests; only the pre-existing unrelated Product Center manual-
  override clearing case failed. The current port-3101 observe preflight remains
  correctly blocked by missing Customer Service migration and unexpectedly
  enabled global AI/Draft Fill flags. No migration, knowledge import/publication,
  service restart, real browser login or customer-message send occurred.

<!-- fact-id: event.2026-08-08.customer-service-read-only-ui-fixture -->
- Customer Service now has an anonymous loopback-only UI fixture with three
  synthetic rollout states. It accepts GET requests only and rejects every
  write with `QA_FIXTURE_READ_ONLY`; browser QA confirmed the rejected advance
  leaves the account at observe-only and surfaces an account-level error. At
  1280x720 the rollout cards, blockers, disabled controls, permanent no-send
  strip and scrollable account dialog rendered without horizontal overflow.
  Responsive rules were source-checked because the in-app browser did not
  expose viewport emulation. The no-send verifier passed 43/43 Node/policy
  tests, 3/3 replay cases with 12/12 assertions, 21/21 Python Edge tests, Fleet
  validation and Vue type checking. No database, model, real LiaoLiao Session
  or customer message was touched. The running port-3101 observe preflight
  still exits 2 for missing Customer Service schema and AI/Draft Fill being on.

<!-- fact-id: event.2026-08-08.customer-service-observed-human-send-attribution -->
- The Customer Service completion audit found and closed a gap after draft fill:
  central now embeds a protected draft-content digest in `CS_FILL_DRAFT_V1`, the
  Edge returns it after filling, and a later observed LiaoLiao outbound message
  is linked to the AI suggestion only on an exact digest match. Matched sends are
  audited as `SEND_OBSERVED/MATCHED_AI_DRAFT`; different human content is stored
  as `HUMAN_OR_UNMATCHED` with no suggestion attribution. The workbench can show
  that the human send was observed while explicitly stating that the system did
  not click Send. Older V1 commands without the telemetry digest remain fillable.
  The no-send verifier passed 43/43 Node/policy tests, 3/3 replay cases with
  12/12 assertions, 23/23 Python Edge tests, Fleet validation and Vue type
  checking. No formal database, model, real Session or customer message changed.

<!-- fact-id: event.2026-08-08.customer-service-response-quality-observability -->
- Customer Service now records the first response latency from the first inbound
  after the prior outbound to the next observed outbound, excluding negative,
  out-of-order and over-seven-day intervals. Status aggregates the latest 5,000
  observed sends into outbound total, exact AI-draft match total/share and first-
  response P50/P95/sample size. The Vue workbench rendered anonymous fixture
  values 6 observed, 4 exact matches (67%), P50 42 seconds and P95 3 minutes 5
  seconds at 1280x720 without horizontal overflow. The complete no-send verifier
  remained green at 43/43 Node/policy, 3/3 replay cases with 12/12 assertions,
  23/23 Python Edge, Fleet and Vue checks. Temporary QA ports were closed; no
  formal database, model, real LiaoLiao Session or customer message was touched.

<!-- fact-id: event.2026-08-08.customer-service-explicit-handling-observability -->
- Customer Service mark-handled is now idempotent and records the bounded time
  from the first inbound observation in the current episode to the human action.
  Status exposes the current HANDLED share among OPEN/HANDLED conversations plus
  the latest 5,000 handling samples' P50/P95, while the contract explicitly says
  this is an operator-completion proxy rather than proof the customer issue was
  resolved. Anonymous browser QA rendered 3 handled, 75% current handling share,
  P50 4 minutes and P95 12 minutes without horizontal overflow. The no-send
  verifier stayed green at 43/43 Node/policy, 3/3 replay cases with 12/12
  assertions, 23/23 Python Edge, Fleet and Vue checks. Temporary ports were
  closed; no formal database, model, Session or customer message was touched.

<!-- fact-id: event.2026-08-08.platform-api-shop-authority-implementation -->
- The user superseded the earlier shop-directory source rule: Platform API /
  Connector is now the authority for the technical shop set, identity,
  platform, country, provider code/name, status and authorization.
  Platform Connections now lists the Gateway set directly, overlays local
  manager/category/control-shop-type fields only through strong IDs, excludes
  registry-only rows from the API configuration list, and exposes API-only
  rows without a prior PostgreSQL projection. Manual technical-shop creation
  is disabled at the Platform API endpoint and UI; explicit projection sync
  refuses uploaded rows, copies only a non-secret allowlist, requires UI
  confirmation, and leaves GET read-only. Site default currency is derived by
  `SHOP_SITE_DEFAULT_CURRENCY_V1` and explicitly is not order-settlement
  currency.

<!-- fact-id: event.2026-08-08.platform-api-shop-authority-validation -->
- The 02:08 CST production-read-only recheck still found 134 Connector shops,
  139 registry rows, 130 valid strong-ID links, 9 registry-only rows, 4 API-only
  rows, 2 identity reviews, 139 missing stored currencies and 102 unknown
  control shop types. Formal Platform Gateway bindings remain 0. Price Control
  now filters, previews and confirms only `CONFIRMED` shop identities and
  rechecks identity immediately before execution. Shop/Connector/Foundation/UI
  tests passed 35/35, repricing tests passed 27/27, Vue TypeScript checks and
  the production build passed, and this task performed no production write.

<!-- fact-id: event.2026-08-08.order-domain-full-reset-observed -->
- During the final read-only recheck, the current production order tables were
  found empty. Audit event `sales_assortment.orders.full_reset` at 02:06 CST,
  actor `codex`, records a successful reset from 80,500 headers, 116,706 lines,
  151,245 raw rows and 578,401 inventory links to zero, with reason
  `responsibility_category_scope_changed`. The recorded pre-reset dump exists
  at `storage/backups/order-reset/commerce_ops_before_order_reset_20260808014229.dump`;
  its verified SHA-256 is
  `B1B91BB8952C480248C597576D59544B10EBEDA3DF86588BE4199B4A40B6D2E4`.
  This was an intermediate empty-state observation from another concurrent
  task. It was later superseded by the current-scope reimport recorded below;
  the dump remains the recovery point for the pre-reset state.

<!-- fact-id: event.2026-08-08.order-domain-current-scope-reimport -->
- With the user's explicit approval, the active order domain was fully reset
  after the responsible category scope changed, then rebuilt from the latest
  2026-07-10 through 2026-08-08 Mabang export. Primary batch
  `7120146d-2657-48fa-953d-a568faad6947` imported 16,043 raw rows, 12,189
  order headers and 14,455 valid lines while rejecting 1,588 duplicate or
  invalid rows. A later user/API-triggered Profit sync added a current-category
  missing-order reconciliation batch, not an old-category batch; retaining it
  leaves 12,195 headers, 14,459 current lines, 21,837 raw evidence rows, 90
  current shops and 3,762 SKUs. Legacy tool/lighting shop matches are zero.
  The refreshed Sales and Assortment trend has nonzero quantity for every day
  from 2026-07-10 through 2026-08-07; August 1 through 7 quantities are 407,
  446, 442, 475, 434, 432 and 394. August 8 local early-morning orders are
  present in PostgreSQL but the current UTC day-key places them under August 7.
  Reset/import/validation audit events were retained, the one-off maintenance
  index was removed, and the verified pre-reset dump remains recoverable.

<!-- fact-id: event.2026-08-08.sales-assortment-deepseek-manual-refresh -->
- Sales and Assortment no longer invokes DeepSeek when the Vue page opens,
  dashboard data reloads, filters change, or imports complete. Page load now
  uses a read-only latest-analysis endpoint; only the existing manual
  `重新分析` action invokes DeepSeek and replaces the one active displayed
  result. The latest result persists across service restarts in a checksummed,
  versioned local-storage envelope with a recovery backup, and its internal
  directory is excluded from file-lifecycle quarantine scans. Previously
  generated results were only held in process memory, so no durable historical
  result existed at rollout; the first manual analysis will create the retained
  result. Focused backend, frontend-contract and lifecycle tests passed 62 of
  62, Vue TypeScript and production build passed, and the restarted live API
  returned a successful null read without invoking DeepSeek or creating a
  persistence file.

<!-- fact-id: event.2026-08-08.lazada-profit-module-production-deployment -->
- The production Profit Analysis module was implemented and deployed for all
  callable Lazada shops with transaction-date, country and shop filters; six
  KPI cards; fixed six-country totals; canonical shop overlays; mixed-currency
  protection; and explicit partial-data states. PostgreSQL migration 017 was
  first rehearsed in isolation, then applied alone after an encrypted full
  `commerce_ops` backup with no retained plaintext. The active service on port
  3101 exposes the new API and Vue route. Finance transaction facts are cached
  once and calculation snapshots reuse existing Mabang and product-package
  data instead of copying either shared domain.

<!-- fact-id: event.2026-08-08.lazada-profit-module-initial-run -->
- Corrected production run `01e6846c-2d94-4e10-8e8a-e740040653ce` covered
  2026-07-27 through 2026-08-07 and finished in about 39 seconds after caches
  were warm: all 78 Lazada shops returned finance successfully, 1,948 goods-
  payment orders were selected, 70 shops were complete, 8 were partial, and 0
  failed. The partial state is intentional: 29 upstream Mabang order numbers
  remain unresolved across ID, MY, PH and SG, and one MY cost line is ambiguous;
  no missing value was guessed or published as zero. TH and VN are fully
  complete; cross-country money remains non-additive because currencies differ.

<!-- fact-id: event.2026-08-08.lazada-profit-duplicate-unit-reconciliation -->
- Spreadsheet-to-database reconciliation found that the general Growth Radar
  importer had rejected two legitimate repeated S-NAIDE Mabang product-unit
  rows as byte-identical source rows. Profit cost was therefore initially low
  by `2 × THB 540.617 = THB 1,081.234` for 2026-07-31 through 2026-08-07. The
  Profit repository now restores only rejected rows whose row hash matches a
  valid row in the same authoritative batch. Real replay returned 55 product
  lines and exactly reconciled the prior workbook: THB 55,288.90 list revenue,
  THB 38,981.65 received revenue, THB 29,337.427 cost, and 46.94%, 24.74% and
  17.44% margins. Nine Profit tests, 24 Gateway/Mabang tests, 14 Vue workspace/
  clean-start tests, TypeScript checks, production build, API filtering and
  browser interaction/console validation passed.

<!-- fact-id: event.2026-08-08.customer-service-frontend-recovered -->
- The Commerce Ops service on port 3101 was healthy while its Vue UI was blank
  because `public/vue-preview/index.html` referenced build assets that were no
  longer present. `npm run build:vue` restored the full production asset set.
  A second runtime failure on the Customer Service route was fixed by
  normalizing the intentionally partial `MIGRATION_REQUIRED` status response
  and guarding optional collection access. All 23 HTML-referenced assets now
  return HTTP 200, the Customer Service page renders its migration notice,
  queues, evidence panel and account entry with no browser console error, and
  the no-send verification passed 43 Node tests plus 23 Python tests. No
  database migration, model call, real LiaoLiao action or customer send was
  performed.

<!-- fact-id: event.2026-08-08.customer-service-account-creation-gate-diagnosed -->
- The Customer Service account dialog and form were confirmed operational, but
  account creation is blocked by the production `commerce_ops` database: a
  read-only application-role query found no `app.cs_*` or Product Knowledge
  tables, and the service reports `MIGRATION_REQUIRED`. The Vue dialog now
  explains that deployment gate in Chinese, disables its fields and Create
  action while the schema is unavailable, and keeps API errors visible inside
  the dialog instead of behind it. Browser verification found the gate visible,
  the form disabled and no console errors; the full no-send verifier passed 43
  Node/policy and 23 Python tests. No production migration, account row, model
  call, real LiaoLiao action or customer send was performed. Making account
  creation functional requires an explicitly approved, backed-up production
  application of the additive Customer Service migration before a page refresh.

<!-- fact-id: event.2026-08-08.lazada-profit-partial-country-aggregation -->
- Lazada Profit country and selection aggregation no longer becomes entirely
  unavailable when one shop is partial. List revenue, received revenue and
  total cost now independently sum every shop that has that exact value;
  profit rates use only shops that have the corresponding revenue and cost
  inputs, and the API/UI expose `已统计 X/Y 家` coverage for every displayed
  aggregate. The completeness tag remains a separate quality warning and no
  unknown value is converted to zero. Production verification reconciled the
  ID, MY, PH and SG country totals exactly to their non-null shop rows; 10/10
  Profit tests, 16/16 Vue/clean-start tests, the Vue production build, live API
  checks and browser console/layout validation passed. The port-3101 service
  was reloaded without changing database data.

<!-- fact-id: event.2026-08-08.sales-assortment-actual-sales-order-grain-fix -->
- The Sales and Assortment actual-sales gate was completed at order grain,
  superseding the same-day line-level safety guard. The apparent “待确认” was
  not missing source data: `商品总金额` is an order-level value repeated on
  every SKU line, so summing it would overstate multi-line orders. Contract
  `SALES-ASSORTMENT-1.5.0` now counts `订单核算金额（人民币）` once per canonical
  order header and derives CNY only from original amount times a positive
  exchange rate when the direct field is absent. Partial category/SKU matches
  are excluded without allocation, missing amounts are never zero-filled, and
  safe confirmed orders remain visible as an explicitly partial subtotal with
  order coverage. Live port-3101 verification returned CNY 64,203.44 for
  2026-08-07 with 302/303 orders safely included, and CNY 1,855,611.13 for the
  latest 30-day window with 10,835/10,837 orders included; four orders used the
  deterministic exchange-rate fallback. Focused Sales tests passed 22/22,
  parser tests 2/2, Daily Report tests 5/5, and the Vue production build passed.
  The full suite passed 1,118/1,119; its sole failure is the unrelated existing
  Product Center manual-override-clear expectation. No production database row
  was changed or backfilled.

<!-- fact-id: event.2026-08-08.lazada-profit-cny-country-summary -->
- Lazada Profit country preview now shows a per-country CNY-equivalent column
  and a bottom all-country CNY summary. Rule `PRODUCT_PACKAGE_COUNTRY_FX-1.0.0`
  resolves the current product-package `国家汇率` and rate direction, treats
  reciprocal rounding-equivalent rows as consistent, and leaves genuinely
  missing or conflicting rates unavailable. Each shop amount is converted
  before aggregation and margins are recomputed with the existing per-metric
  partial-data rules. The 2026-07-27 through 2026-08-07 production run matched
  all 6 country rates and returned CNY 243,221.570771 list revenue, CNY
  176,312.837794 received revenue and CNY 120,129.845551 total cost, with
  50.60888507%, 31.86551413% and 23.09951048% margins across 1,948 orders.
  A short-lived in-process rate cache reduced warm dashboard responses to 322
  ms then 76 ms. Profit tests passed 12/12, Vue/clean-start tests passed 16/16,
  the Vue production build passed, and live browser validation found no console
  errors or page-level horizontal overflow. No database schema or data changed.

<!-- fact-id: event.2026-08-08.sales-assortment-anomaly-platform-period-labels -->
- Sales and Assortment anomaly tables now show each store together with its
  normalized platform as compact secondary metadata. The anomaly heading also
  displays the exact comparison windows returned by the dashboard contract,
  rather than leaving “本期” and “上期” implicit. With the current latest order
  day of 2026-08-07, the live mappings are 2026-08-07 versus 2026-08-06 for the
  one-day comparison, 2026-08-05 through 2026-08-07 versus 2026-08-02 through
  2026-08-04 for three days, and 2026-08-01 through 2026-08-07 versus
  2026-07-25 through 2026-07-31 for seven days. All 22 focused Sales tests and
  the Vue TypeScript/production build passed; the built asset contains the new
  labels. No database or external system data changed.

<!-- fact-id: event.2026-08-08.lazada-profit-date-filter-diagnosis -->
- Profit's visible date control currently looks up a calculation run whose
  `date_from` and `date_to` exactly equal the selected range. Production has a
  2026-07-27 through 2026-08-07 run, while a read-only request for its
  2026-08-01 through 2026-08-07 subrange returns no run even though the finance
  facts are cached. A true date filter should decouple data synchronization from
  range calculation: query covered transaction-date facts locally, recompute
  the selected range with versioned Mabang/product-cost evidence, and fetch only
  uncovered shop-day gaps in the background. No code, database or external
  platform state changed during this diagnosis.

<!-- fact-id: event.2026-08-08.customer-service-local-onboarding-and-016-deployment -->
- Customer Service now provides a real three-step LiaoLiao account onboarding
  flow in the main Vue workbench: account metadata, human login in a visible
  isolated Chromium, and automatic transition to continuous monitoring. The
  central UI never collects passwords, Cookies, OTPs or Sessions; each account
  receives independent local Session/SQLite/log paths, login is serialized,
  monitoring defaults to four concurrent accounts with a hard maximum of
  twelve, and all accounts start in `OBSERVE_ONLY`. The local Worker credential
  is automatically generated/reused without API disclosure, Session saves are
  atomic, and the no-send verifier passed 52/52 Node checks, 32/32 selected
  Python safety checks, 3/3 deterministic replay cases, Fleet validation and
  Vue type checking. Full Python passed 46/46; the full Node suite passed
  1,133/1,134 with only the pre-existing unrelated Product Center
  manual-override-clear expectation failing.
- PostgreSQL Shadow migration 016 was rehearsed successfully in
  `commerce_ops_migration_test` after a guarded empty Customer Service schema
  replacement, then production `commerce_ops` received an AES-256-GCM full
  backup with no retained plaintext and no co-located key. Backup artifact
  `commerce-ops-commerce_ops-20260808033843.dump.aes256gcm` has encrypted
  SHA-256 `1f2466f748170d0f16394f8b0fea44f4aae4fe6c7ae25375c83c37534f2d3fb2`.
  Migration 016 was then applied alone with 15 tables, 9 indexes and 31 foreign
  keys; migration 018 was not applied. The restarted port-3101 service reports
  `CONTROL_PLANE_READY`, zero accounts, `automaticSendEnabled=false`, AI and
  Draft Fill disabled, and Product Knowledge not yet ready. No real LiaoLiao
  login, model call or customer send occurred.

<!-- fact-id: event.2026-08-08.sales-assortment-cold-load-timeout-fix -->
- The Sales and Assortment page failure was reproduced as PostgreSQL error
  `57014`: the cold product-package query transferred all 119,245 normalized
  JSON payloads, about 312 MB, and took about 46 seconds against a 30-second
  statement timeout. The dashboard repository now projects only the identity,
  product, category, cost and exchange fields actually used by Sales and
  Assortment while the paginated source viewer retains full source evidence.
  The projected query completed within the same production timeout in about
  9.9 seconds. After reloading port 3101, a true cold page-load simulation ran
  the dashboard and both estimate-trend requests concurrently; all three
  returned HTTP 200 in about 19-22 seconds across two cold runs. Focused Sales and unified-data tests
  passed 27/27. No database row, schema or external system data changed.

<!-- fact-id: event.2026-08-08.lazada-profit-date-filter-incremental-sync -->
- Both Profit date-filter phases are deployed. The default range is the latest
  seven complete Shanghai business dates. Any shop-day range covered by prior
  successful finance reads is recalculated locally from cached Lazada facts;
  uncovered ranges expose exact coverage and one explicit action that fetches
  only missing shop/date intervals. A server runner enabled by default at 09:30
  Asia/Shanghai creates the previous-day snapshot and skips an already complete
  date. Production proved 2026-08-01 through 2026-08-07 as a cached preview with
  546/546 shop-days, 1,111 goods-payment orders, 6 countries and CNY
  151,851.142036 list revenue. The uncovered 2026-08-08 date reported 78 missing
  shop-days, retained the available range 2026-07-27 through 2026-08-07, and
  published no calculation. On startup the runner created exact snapshot
  `00783980-91c0-4b59-a6fb-d2e0e0de44a7` for 2026-08-07 with 149 orders and 78
  complete shops by reusing 78 cached shop-days and fetching 0 externally.
  Profit/date tests passed 16/16, Vue/clean-start tests passed 16/16, the Vue
  production build passed, and desktop plus 375px browser checks found no
  console errors or page-level horizontal overflow. No schema migration was
  required; the service was restarted and only the audited daily Profit
  snapshot was added to production data.

<!-- fact-id: event.2026-08-08.latest-product-package-country-volume-export -->
- The current production latest-only product-package snapshot was exported
  read-only with all 119,245 rows and all 62 source fields. Because the bundled
  XLSX engine exceeded its single-workbook memory ceiling at the full
  7.39-million-cell scale, the lossless columnar export was split into seven
  country workbooks and packaged together: ID 25,755, MY 16,769, PH 25,276, SG
  8,405, TH 17,648, TW 3,210, and VN 22,182 rows. Every volume reported exactly
  62 keys per row, passed first/last-row and formula-error checks, and all 21
  sheet previews passed visual review. The archive is
  `outputs/latest-product-package-20260808-112223/AI_Project_A_最新完整产品包_20260808_按国家分卷.zip`.
  The task triggered no source synchronization and performed no production
  database write.

<!-- fact-id: event.2026-08-08.price-control-stale-run-recovery-and-live-preview -->
- The user approved recovery of the 11:00 Price Control round, prevention of a
  recurrence, a fresh source synchronization, and one real provider preview
  with no confirmation or price write. Stale Price Control run
  `05e601f4-9cc8-4eab-911b-bc019c9b8961` and its paired Foundation source run
  `foundation:source-run:2fa31ad12c59467d3f1ed97376717f76` were atomically
  marked `FAILED` with `PRICE_CONTROL_STALE_RUN_RECOVERED`; Audit event
  `70933892-f82f-4598-9ec7-3f935b9225d9` records the recovery. The sync path now
  has a configurable MySQL per-query timeout, persisted heartbeats, and a
  guarded 30-minute stale-run recovery at both admission and scheduler polling.
- Fresh incremental sync `a237697a-8a5f-4ce4-b4ba-6c099cc4f913` succeeded with
  104 batches seen, 114,822 source rows, 573,950 price points and zero changes;
  the source table and business timestamps remained 2026-08-06 01:08:46 and
  2026-08-05 17:23:54 respectively. A local port collision on 8877 was resolved
  without stopping the existing service by moving the managed Mabang Listing
  bridge to 8878 and restarting only Commerce Ops. Read-only discovery then
  proved live Shopee listing SKU `T4AA2537533` in confirmed ID shop
  `ironPoetry.ID.Mall`. Real preview plan
  `2c5a377d-962b-449b-89ae-6b9f7e729831` was created as `PREVIEW_READY` for one
  unselected `special_price` difference from 1,557,000 to 1,683,000 and no
  execution job. The Mabang execution audit file stayed byte-identical, while
  Audit recorded one preview and zero confirmation/execution events. All 71
  focused Price Control, repricing, Mabang integration and env-isolation tests
  passed. No marketplace price write occurred.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-production-publication -->
- Product Knowledge Shadow migration 018 was rehearsed in
  `commerce_ops_migration_test` with the exact 13-table, 9-index and constraint/
  application-grant contract. Before production apply, a new AES-256-GCM full
  backup was created as
  `commerce-ops-commerce_ops-20260808050311.dump.aes256gcm`; plaintext was not
  retained, the key was not co-located, and the encrypted artifact SHA-256 is
  `54e969f895bd4a8f72c38879f6d808298000e4142480de2b5c039a923c7a9bf0`.
  Migration 018 was then applied to `commerce_ops` with frozen migration SHA-256
  `7c9244fafee77c847cde8d044b68f5f962c70dd19dabecdafbc85c821617f687`.
- Shared package digest
  `1bc094f8f9458a978c2c9102bb36ce9ed5f119b16743ccbc176140bc5003881c`
  imported 29,007 candidates into PostgreSQL. A controlled initial selection
  digest `f30c9c02f103e8ac661d2d1f435f33a94da832b798ec5c094b134e0e4a72c262`
  approved only 23 exact-mapped, conflict-free, normal-risk, customer-visible
  Malaysian installation claims. Separate reviewer and publisher identities
  published immutable `customer-service-knowledge` version 1 with 23 claims
  and zero accessories, policies or playbooks; the governance write gate was
  then disabled. The remaining candidates stay fail-closed in review, mapping,
  source-read or conflict states, including 943 conflicts and 160 unread source
  references.

<!-- fact-id: event.2026-08-08.customer-service-suggest-only-activated -->
- Customer Service Product Knowledge readiness is now true with one published
  `CUSTOMER_SERVICE` release. Central AI was enabled, while Draft Fill and
  automatic send remain disabled. The active LiaoLiao account passed its
  observation and dependency gates and was advanced from `OBSERVE_ONLY` to
  `SUGGEST_ONLY`; its isolated local Playwright Worker is online with the
  account lease active. Production generated 13 READY suggestions with no
  failed suggestion state during final verification. Product Knowledge audit
  contains 23 successful candidate reviews, one Release creation and one
  publication with no failed event. Focused Product Knowledge validation passed
  30/30; the no-send verifier passed 52/52 Node checks, 32/32 Python checks,
  deterministic reply replay and Vue type checking, and confirmed that no real
  customer message was sent.

<!-- fact-id: event.2026-08-08.customer-service-google-chrome-draft-fill-deployment -->
- Customer Service Draft Fill was deployed as a fill-only production capability.
  The local Worker now launches the installed stable Google Chrome through
  Playwright with an isolated temporary browser profile and the existing
  per-account storage-state Session; process-tree verification resolved the
  browser executable to `C:/Program Files/Google/Chrome/Application/chrome.exe`
  and not the Playwright-bundled Chromium. Central command polling now runs at
  every conversation boundary so one stale or busy conversation cannot delay
  later messages or Worker heartbeats. Human and automatic fill paths both
  recheck the global gate plus exact account `ACTIVE` and `DRAFT_FILL` state in
  the repository transaction and again before leasing; unauthorized pending or
  leased commands are canceled without reaching the browser. The Vue workbench
  adds `采纳建议（不填入）` for the required first review and disables fill until
  the account reaches `DRAFT_FILL`. The global Draft Fill gate is enabled, the
  real account and one primary lease are active, the Worker is online, and
  automatic send remains false. Offline no-send validation passed 57/57 Node
  checks, 40/40 Python Playwright/Fleet checks, deterministic reply replay and
  Vue type checking; the production Vue build and served customer-service chunk
  were also verified. The final runtime was started with persistent stdout/error
  log handles and remained stable beyond three minutes with continuous Worker
  heartbeats, lease renewal, the same Google Chrome executable and an empty
  service error log. No suggestion was accepted on the user's behalf and no real
  customer message was sent.

## Handoff

<!-- fact-id: event.2026-08-08.shop-directory-platform-connections-handoff -->
- New technical shops must enter through Platform API / Connector, not manual
  Platform Connections entry or uploaded sync rows. The API configuration list
  reads that authoritative set directly; explicit sync only materializes its
  non-secret projection. Two identity conflicts remain intentionally blocked,
  registry-only shops remain historical/non-API rows, and business enrichment
  still requires reviewed local input. No provider write or token disclosure
  occurred.

<!-- fact-id: event.2026-08-08.shared-product-knowledge-handoff -->
- Migration 018, the 29,007-candidate import and the deliberately narrow
  23-claim initial Customer Service Release are complete. Future Releases must
  retain per-record review and separate reviewer/publisher identities; do not
  bulk-approve the remaining package. Product Core mappings, same-scope
  conflicts, country differences, money/compensation, review-inducement,
  internal-operation, safety and media mappings still require business review.
  The next coverage step is reviewed TOP-product knowledge plus ingestion of
  the 160 pending DingTalk document bodies; the governance write gate should
  stay disabled except during a bounded publication window.

<!-- fact-id: event.2026-08-08.customer-service-control-plane-handoff -->
- Migrations 016 and 018 are deployed, the real LiaoLiao account is monitoring,
  central AI and the global Draft Fill gate are enabled, the isolated Worker is
  online in installed Google Chrome, and the account remains `SUGGEST_ONLY`.
  Production has 19 generated suggestions and zero ACCEPT/EDIT reviews, so the
  only remaining account-upgrade blocker is `CS_SUGGESTION_REVIEW_REQUIRED`.
  The current 18 READY suggestions are all below the 0.72 automatic-fill
  threshold and remain review-only; do not lower the threshold to force them
  through. The operator must genuinely accept or edit one suggestion without
  filling, then explicitly upgrade the account to `DRAFT_FILL`. Future
  low-risk suggestions that pass the evidence and confidence gate may then be
  filled into the exact LiaoLiao editor automatically; risky or insufficiently
  grounded suggestions stay manual. Humans still inspect and click Send, and
  automatic send remains off.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-income-api-validation -->
- Shopee's official Payment API now exposes Seller Center-aligned income
  overview and order-level detail reads, escrow list/detail reads, and
  asynchronous income-report and income-statement generation/download flows.
  Read-only relay validation for authorized MY shop `MS0698` (`Fine Nest`)
  succeeded: the overview exactly matched the supplied Seller Center screenshot
  at MYR 5,884.06 pending and MYR 28,630.52 released; released and pending
  detail pagination, a screenshot-order escrow breakdown, an XLSX income report,
  and a weekly PDF income statement all returned successfully. The generated
  documents reached Shopee status `DOWNLOADABLE`; no document link, credential,
  token, buyer identity, or order identifier was retained. The current Commerce
  Ops Shopee connector still allowlists only shop, order, product and inventory
  reads, so Payment APIs remain unavailable through the provider-neutral Gateway
  until an explicit read-only finance adapter and operation allowlist are added.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-income-report-export -->
- At the user's request, Shopee's official income-report flow generated and
  downloaded the released-income XLSX for MY shop `MS0698` (`Fine Nest`) covering
  2026-08-01 through 2026-08-07 in Malaysia local time. The original Shopee file
  was preserved without edits at
  `outputs/shopee-fine-nest-income-20260801-20260807/Fine-Nest-Shopee-Income-2026-08-01_to_2026-08-07.xlsx`.
  Workbook verification found five readable sheets (`Summary`, `Service Fee
  Details`, `Adjustment`, `Shipping Fee Discrepancy`, and `Income`), 323 income
  rows including the header, no formula-error cells, and a visually intact
  provider layout. No credential, download link, buyer identity, or order
  identifier was retained in memory.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-income-rule-calculation -->
- Applying the user-confirmed Shopee Malaysia-only Order-row income formulas to the Fine
  Nest 2026-08-01 through 2026-08-07 report used 154 `Order` rows and excluded
  166 `Sku` rows. The source components were MYR 6,121.66 total released,
  MYR -253.22 adjustment, MYR -70.00 AMS commission and MYR 0.00 Ads Escrow Top
  Up, producing MYR 5,938.44 real received income. The list-price components
  were MYR 8,404.32 Product Price and MYR -369.00 seller voucher with the other
  five defined components explicitly zero, producing MYR 8,035.32 list-price
  income. No workbook data was changed.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-my-cost-profit-calculation -->
- Fine Nest Malaysia Shopee cost matching for the 2026-08-01 through 2026-08-07
  Income report covered 154 distinct Order IDs. Current Mabang facts supplied
  168 order-SKU lines and 188 units for 153 orders; the remaining normal order
  was absent from both current and raw Mabang facts, so its one SKU and one unit
  were read through the authorized Shopee official order-detail API. Across 169
  order-SKU lines, 189 units and 53 distinct Malaysia SKUs, product-package
  `cost_local` matching had no missing or ambiguous cost and produced MYR
  4,540.0066 total cost. With MYR 8,035.32 list-price income and MYR 5,938.44
  real received income, the list-price, received, and list-to-received margins
  were 43.49936779%, 23.54883437%, and 17.40358069%. No buyer identity or order
  identifier was retained in memory.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-wallet-payment-validation -->
- Shopee Open Platform endpoint
  `GET /api/v2/payment/get_wallet_transaction_list` is the official source for
  Seller Center `My Balance` wallet transactions. Seller Balance Payment is
  selected with `transaction_tab_type=wallet_wallet_payment`; the request may
  also use `money_flow=MONEY_OUT` and `create_time_from` / `create_time_to`, with
  a maximum 15-day interval per call. A live read-only relay check for authorized
  MY shop `MS0698` (`Fine Nest`, platform shop ID `1776742367`) covering
  2026-08-01 through 2026-08-08 returned 11 matching rows, transaction type
  `SVS_PAYMENT_DEDUCT`, and MYR -950.40 in total, exactly matching the supplied
  Seller Center screenshot. The shop token and access state were valid at check
  time, proving the current app-shop authorization can call this Payment API.
  Commerce Ops does not yet expose this wallet endpoint through its Shopee
  Connector allowlist, so product integration remains separate from provider
  authorization. No credential, request ID, order ID, buyer identity, or row-level
  transaction content was retained.

<!-- fact-id: event.2026-08-08.shopee-profit-unified-module-and-skill -->
- Shopee profit support was added to the existing Profit Analysis module for
  MY, VN, TH, ID and PH with versioned country formulas, XLSX parsing, exact
  Mabang order/SKU/quantity matching, country product-package local cost,
  auditable snapshots and fail-closed missing/ambiguous evidence. The unified
  dashboard now filters by transaction date, country, platform and shop,
  expands countries into Lazada/Shopee rows, uses Shopee calendar months and
  Lazada complete weeks, exposes current/last/custom periods, supports manual
  Shopee workbook import, and runs each platform's current period once per
  Shanghai day. The reusable personal skill was installed and validated at
  `C:/Users/PC/.codex/skills/calculate-shopee-profit`. Confirmed workbook
  samples reproduced THB 4,030 list / THB 3,073 received / 2 orders; IDR
  790,167 list / IDR 595,858 received / 6 orders; and PHP 38,878 list / PHP
  30,430.36 received / 38 orders. Focused Profit/Shopee validation passed 28/28,
  Vue type checking and the production build passed, and browser QA confirmed
  the new period, KPI, expansion, filtering, snapshot and import layouts.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-income-report-production-chain -->
- The production Shopee finance adapter was corrected to the official XLSX
  Income Report contract: `release_time_from` / `release_time_to`, generated
  response `id`, lookup parameter `income_report_id`, numeric status `2`, and
  `file_link`. A live read-only end-to-end check for authorized MY shop MS0698
  (`Fine Nest`) covering 2026-08-01 through 2026-08-07 successfully generated,
  polled and safely downloaded an 876,756-byte XLSX with a valid ZIP header.
  No credential, file link, report ID, request ID, buyer identity or order ID
  was retained.

<!-- fact-id: event.2026-08-08.shopee-fine-nest-normal-expense-validation -->
- The user-confirmed Shopee normal-expense rule was validated for MY shop
  `MS0698` (`Fine Nest`) over 2026-08-01 through 2026-08-08. The official wallet
  transaction API supplied MYR -950.40 of Seller Balance Payment advertising
  spend, while the same-period official Income Report `Summary` supplied MYR
  -79.06 of `AMS Commission Fee` affiliate deduction and MYR 0.00 of
  `Ads Escrow Top Up Fee`. Exact decimal sign normalization produced a positive
  normal expense of MYR 1,029.46. The source workbook contained no formula-error
  cells. No credential, file link, report ID, request ID, buyer identity, order
  ID, or row-level transaction content was retained.

<!-- fact-id: event.2026-08-08.shopee-normal-expense-skill-packaged -->
- The installed personal `calculate-shopee-profit` skill was extended to cover
  the user-confirmed Shopee normal-expense metric. It now routes fee-total tasks
  to a dedicated reference contract, aligns Seller Balance Payment advertising
  spend with Income Report `AMS Commission Fee` and `Ads Escrow Top Up Fee` by
  one shop and identical transaction-date range, preserves refunds and source
  signs, and uses a dependency-free BigInt cents script instead of floating-point
  arithmetic. The script reproduced Fine Nest MYR 1,029.46, UI metadata was
  regenerated, and `quick_validate.py` passed. The installable package is
  `outputs/skills/calculate-shopee-profit-20260808.zip` (8,344 bytes; SHA-256
  `A35FF0CB26878D99934A793F0246046C8EFF47D227D1B46228ECDA0F86F051D4`).

<!-- fact-id: event.2026-08-08.price-control-multiplatform-detection-fix -->
- The Price Control difference pipeline was corrected after the DingTalk robot
  appeared to report only Lazada. The source contained Lazada, Shopee and
  TikTok; the visible bias came from taking the first 30 array entries, while a
  separate recent-batch bug had also treated absent previous values as large
  false `NEW` rounds. Production now enforces one latest approved batch per
  country/category, compares the full composite price identity, silently
  establishes missing baselines, suppresses older overlapping scopes and
  duplicate fingerprints, and rotates bounded DingTalk examples across the
  changed platforms. The natural 17:00 scheduled run
  `1509a39e-8816-467d-917d-eb4a75e2b220` succeeded from business timestamp
  2026-08-08 12:20:24, read 70 scopes / 81,674 rows / 412,454 price points,
  persisted 40,891 valid differences and sent DingTalk successfully. The
  platform totals were Lazada 5,396, Shopee 26,857 and TikTok 8,638; directions
  were 25,329 UP, 15,562 DOWN, zero NEW and zero REMOVED. Focused regression
  passed 58/58 and the Vue production build passed. No Mabang preview,
  marketplace API execution or price write occurred. Historical false `NEW`
  records remain untouched pending explicit cleanup approval; the known scope
  is 297,792 valid `NEW` events, including the 16:00 false round of 66,944, and
  143 Foundation tasks would become invalid-only if those records are
  invalidated.

<!-- fact-id: event.2026-08-08.shopee-profit-20260801-20260807-population -->
- Profit Analysis was populated from official Shopee Income Reports for
  transaction dates 2026-08-01 through 2026-08-07. All 44 callable authorized
  shops in the five confirmed rule countries were covered: ID 7, MY 15, PH 10,
  TH 6 and VN 6. Thirty shop snapshots are COMPLETE and fourteen are PARTIAL;
  the partial shops retain revenue and order counts while total cost and
  dependent margins remain unavailable where existing Mabang orders or
  product-package costs are incomplete. The five-country report contains 980
  distinct orders. Eight authorized SG shops and one TW shop remain visible but
  intentionally uncalculated because no user-confirmed profit contracts exist
  for those countries. No new Mabang import was executed after the user
  confirmed that Shopee profit must query existing Commerce Ops Mabang order
  facts only.

<!-- fact-id: event.2026-08-08.shopee-live-report-schema-hardening -->
- Live authorized-shop reports confirmed two additional fail-closed parsing
  cases: MY/VN may omit the entire Adjustment sheet when no adjustment exists,
  and a zero-order report may contain Summary only. Adjustment absence now
  contributes zero for MY/VN, while Summary-only files are accepted only when
  Total Released Amount is explicitly zero. Zero-order snapshots publish zero
  revenue, cost and orders with undefined percentage margins. Shopee sync now
  skips already covered shop/range snapshots, persists acquisition failures,
  uses single-store incremental retries when needed, and does not fail the
  entire revenue snapshot when existing Mabang evidence is incomplete.

<!-- fact-id: event.2026-08-08.shopee-previous-day-incremental-schedule -->
- The unified 09:30 Asia/Shanghai Profit scheduler now preserves Lazada's
  existing missing-gap synchronization while requesting Shopee only for the
  immediately preceding Shanghai business date. Shopee skips a shop-day when a
  wider successful snapshot already covers it, and calendar-month views combine
  a historical interval with later daily snapshots only when they form a
  contiguous, non-overlapping full-range chain; amounts and counts are summed
  and margins are recomputed. A read-only PostgreSQL check found 44 result
  windows for the existing 2026-08-01 through 2026-08-07 snapshot across 44
  unique supported shops. Focused Profit/Shopee tests passed 25/25. The full
  suite passed 1,168/1,169; its sole repeatable failure is the unrelated existing
  Product Center manual-name override clearing assertion. After explicit user
  authorization, the validated Commerce Ops `start-all` process tree was
  restarted and the new server and scheduler restored port 3101 with a healthy
  `/api/health` response. The restarted code also contains a separately added
  expense feature whose new `profit_expense_transactions` and
  `profit_shop_daily_expenses` tables are absent from production PostgreSQL, so
  Profit readiness is currently blocked pending explicit approval of a formal
  production migration. No migration or production database write was executed
  during the restart.

<!-- fact-id: event.2026-08-08.unified-profit-expense-integration -->
- The Lazada expense contract was packaged and validated as the installed
  personal skill `C:/Users/PC/.codex/skills/calculate-lazada-expense` and was
  integrated with the existing Shopee normal-expense contract in Profit
  Analysis. Both platforms now reuse the single
  `profit_finance_transactions` bill fact set; only advertising account/wallet
  rows use `profit_expense_transactions`, while versioned local-date results
  use `profit_shop_daily_expenses`. The dashboard exposes Expense at selection,
  country, platform and shop levels with the same date/platform/country/shop
  filters and CNY conversion path as Profit. Premium Package remains excluded
  from Lazada Expense. Shopee Summary-based expenses publish daily facts only
  from one-day statements, so a multi-day Summary is never assigned to an
  invented date. The S-NAIDE regression reproduced THB 5,639.67 and removed
  exactly one cross-window duplicate; the Fine Nest regression reproduced MYR
  1,029.46. Focused Profit, connector and PostgreSQL planning validation passed
  62/62 and the Vue type check and production build passed. The full suite's
  only remaining failure is the pre-existing unrelated Product Center manual-
  name override clearing assertion. PostgreSQL Shadow migration
  `019_profit_expense_module.sql` and its guarded production plan were added,
  but no formal production database migration was executed.

<!-- fact-id: event.2026-08-08.profit-expense-production-migration-applied -->
- After the user explicitly authorized the formal production change, guarded
  PostgreSQL migration `019_profit_expense_module.sql` was applied to database
  `commerce_ops` at 2026-08-08T11:00:32.375Z with its planned SHA-256 unchanged.
  Post-apply read-only validation through the `commerce_app` role found
  `app.profit_expense_transactions` and
  `app.profit_shop_daily_expenses`, both initialized with zero rows, and
  `ProfitRepository.isReady()` returned true. The production runtime check
  passed with zero validation writes and zero external calls. Re-running the
  guarded migrator returned `ALREADY_APPLIED`, confirming migration registry
  idempotency. No existing Finance Details bill row or historical profit result
  was copied, deleted or rewritten.

<!-- fact-id: event.2026-08-08.shopee-fs0713-ads-api-validation -->
- The current Shopee Open Platform V2 catalog exposes Ads performance at
  separate grains: `get_all_cpc_ads_daily_performance` for shop-day totals,
  `get_product_campaign_daily_performance` for campaign-day facts,
  `get_gms_campaign_performance` for a GMV Max campaign over a selected range,
  and `get_gms_item_performance` for item/listing performance over a selected
  range. The GMV Max reports include expense, broad GMV/ROAS, impressions and
  clicks; item results carry `item_id`, while title/image/link enrichment still
  requires Product data. The current Ads API catalog has no separate diagnosis
  endpoint matching Seller Center's `Check Details` column.
- Live read-only relay validation for PH shop `FS0713` (`DreamHug Beddings
  Store`, platform shop ID `1720506264`) found a current valid token. A shop-day
  CPC performance request for 2026-08-01 through 2026-08-07 returned seven
  daily rows, and a GMV Max item-performance request for the same range
  succeeded with 36 item reports. This proves the current Open Platform
  app-shop combination can call both shop-level Ads and GMV Max item-level Ads
  reads. Commerce Ops' provider-neutral Shopee Connector still does not
  allowlist Ads operations, so product integration remains separate from
  provider authorization. No credential, provider request ID or item-level
  business value was retained.
- A follow-up read-only All Ads List check found 53 ordinary product-ad
  campaigns for `FS0713`; `get_product_level_campaign_setting_info` returned
  their status, budget and item mappings, and product campaign performance
  returned campaign-day metrics. The live GMV Max campaign ID was not included
  in that ordinary campaign list. Shopee's separate GMV Max item-performance
  API explicitly returns only items with performance in the requested range,
  while `list_gms_user_deleted_item` returns only seller-removed item IDs. The
  current official Ads catalog therefore cannot guarantee a complete Seller
  Center-equivalent GMV Max `All Ads List` covering zero-performance, newly
  created, paused or otherwise non-returned items; Seller Center export remains
  necessary when exact UI-list completeness is required.

<!-- fact-id: event.2026-08-08.shopee-fs0713-link-ads-preview -->
- A read-only link-grain Shopee Ads preview was generated for PH shop `FS0713`
  over 2026-08-01 through 2026-08-07. The source reads covered all 53 ordinary
  product-ad campaign/item mappings and 36 GMV Max item performance reports,
  which merged into 69 unique listing links. Recomputed link-level totals were
  PHP 8,781.54 expense, PHP 62,420.00 broad GMV, ROAS 7.11, 91,621 impressions,
  3,478 clicks and 116 broad orders. The inline preview shows the 30 highest-
  expense links with product links and pagination, while explicitly labeling
  that GMV Max coverage includes only items with performance in the selected
  range. No provider credential, request ID or buyer/order identity was
  retained.

<!-- fact-id: event.2026-08-08.shopee-fs0713-link-ads-workbook -->
- A complete formula-driven Shopee Ads link-grain workbook was generated for PH
  shop `FS0713` (`DreamHug Beddings Store`) covering 2026-08-01 through
  2026-08-07. It contains 69 unique `item_id` links, all 53 ordinary product-ad
  campaign/item mappings, 371 ordinary campaign-day rows, 36 GMV Max item
  performance rows and zero seller-removed GMV Max items. Reconciled totals are
  PHP 8,781.54 expense, PHP 62,420.00 broad GMV, ROAS 7.1081, 91,621
  impressions, 3,478 clicks and 116 broad orders. The workbook at
  `outputs/019fe178-feff-74c3-a489-1b7ca7e733ca/FS0713-Shopee-Ads-Link-Level-20260801-20260807.xlsx`
  has separate link summary, ordinary campaign detail, GMV Max detail, ordinary
  daily detail, removed-item and methodology sheets. Formula-error scanning
  returned zero matches and every sheet passed rendered visual review. The
  workbook explicitly retains the API limitation that GMV Max item performance
  includes only items with performance in the selected range and is not a
  complete zero-performance Seller Center All Ads List. No provider write,
  credential, request ID or buyer/order identity was retained.

<!-- fact-id: event.2026-08-08.profit-expense-production-backfill-complete -->
- Production Profit Expense facts were fully backfilled at the exact existing
  Profit time grains: Lazada 2026-07-27 through 2026-08-07 covered 78 shops and
  936/936 shop-days; Shopee 2026-08-01 through 2026-08-07 covered 44 supported
  shops and 308/308 shop-days. Every scoped fact is `COMPLETE`, has a non-null
  signed total and expense value, and satisfies `expense_value =
  -(advertising_expense_signed + billing_expense_signed)` with zero date,
  classification or arithmetic mismatches. Lazada period totals by currency
  are IDR 17,261,477, MYR 2,708.87, PHP 11,491.40, SGD 731.63, THB 37,660.29
  and VND 8,952,592; Shopee totals are IDR 1,113,209, MYR 3,323.93, PHP 19,201,
  THB 15,321 and VND 9,961,118. Advertising source facts remained unique at
  744 Lazada rows and 467 Shopee rows. The shared Profit bill table was not
  copied: its existing Lazada 15,528 and Shopee 11,359 rows each remained
  unique by shop and source key.
- Backfill hardening preserves marketplace-local JavaScript date strings,
  resumes one pending Shopee report ID instead of regenerating it, bounds
  stalled statement acquisition, and runs Shopee statement retrieval serially
  by default for the current relay. One MY export variant with unreadable
  labels is accepted for daily Expense only under a strict Summary template
  fingerprint; unreadable Income or Adjustment content remains incomplete for
  Profit and never overwrites shared finance facts. Focused syntax and Profit
  regression validation passed 34/34. The verified `start-all` runtime was
  restarted with the completed code and restored the server listener on port
  3101. Temporary diagnostic and backfill scripts were removed; no credential,
  provider report/request ID, buyer identity or order identifier was retained.

<!-- fact-id: event.2026-08-08.profit-gmv-expense-rate-implementation -->
- Profit Analysis now implements source-backed GMV and expense rate for Lazada
  and Shopee. The Mabang parser projects exact original-currency
  `原始商品总金额` and `优惠金额（原始货币）` onto the canonical order header;
  imports update those components transactionally, and Profit dynamically
  aggregates one valid order header once by Shanghai payment date. Store rate
  is total expense divided by store GMV; country rate is total country expense
  divided by total country GMV; cross-country CNY totals convert both inputs
  per shop before recomputing. The Vue Profit workbench now exposes GMV,
  expense rate, source status, order coverage and day coverage at selection,
  country, platform and shop levels. The contract is documented in
  `docs/design/COMMERCE-OPS-PROFIT-GMV-EXPENSE-RATE-V1.md`.
- Read-only production evidence found 11,951 current canonical headers with
  exact authoritative raw rows: 11,943 are immediately backfillable, two have
  source conflicts and six have missing/unparseable components, which remain
  incomplete. For 2026-07-27 through 2026-08-07, Lazada has 1,955 valid orders
  with one missing discount component; Shopee has 2,241 valid orders with at
  least one conflicting original amount. Exact unique name fallback currently
  resolves 37 of 39 Lazada source-shop names and 44 of 51 Shopee source-shop
  names; ambiguous or unmatched names are not allocated.
- SQLite migration `037_profit_gmv_module.sql`, PostgreSQL migration
  `020_profit_gmv_module.sql`, and a guarded plan-only production migrator were
  added. The formal `commerce_ops` plan SHA-256 is
  `5e6f876de85929058261d18cf9a2b377c0b365789ea1151531b5624fd53e081e`.
  Migration 020 was not applied because this task did not include explicit
  production-migration confirmation. The existing elevated `start-all` process
  on port 3101 could not be restarted from the current process token, so the
  backend requires a controlled restart after migration; the rebuilt static
  Vue assets are already on disk. Focused Profit, Shopee, Growth Radar and GMV
  tests passed 97/97, provider/migration readiness tests passed 17/17, and Vue
  type checking plus production build passed. No production data was changed.

<!-- fact-id: event.2026-08-08.profit-gmv-production-migration-applied -->
- After the user explicitly authorized the formal production change, guarded
  PostgreSQL migration `020_profit_gmv_module.sql` was applied to database
  `commerce_ops` at 2026-08-08T15:26:44.478Z with SHA-256
  `5e6f876de85929058261d18cf9a2b377c0b365789ea1151531b5624fd53e081e`.
  A second guarded invocation returned `ALREADY_APPLIED`, and read-only
  validation through the least-privilege application role found all four GMV
  source columns plus `ProfitRepository.isReady()` and `isGmvReady()` true.
- The migration's strict raw-shop identity backfill left 244 otherwise unique
  canonical batch/order identities unpopulated because continuation rows did
  not carry the same raw shop text. The authorized rollout repair updated
  exactly those 244 `MISSING` headers by unique source batch plus order identity
  in one transaction; ambiguous identities and existing conflicts were not
  changed. Production now has 12,195 canonical order headers: 12,187 source
  confirmed, two conflicting and six genuinely missing or unparseable. Two
  confirmed headers derive negative GMV and remain invalid and non-publishable.
- For 2026-07-27 through 2026-08-07, Lazada has 1,954 confirmed GMV headers and
  one missing header; Shopee has 2,239 confirmed, one conflicting and one
  missing. The live Profit API reports S-NAIDE (`BS0425`) as complete for all
  12 days with THB 5,639.67 Expense, THB 84,865.08 GMV and a 6.64545417%
  expense rate. Country and cross-country aggregation use the same range and
  remain fail-closed where any required GMV evidence is partial.
- Commerce Ops was restarted as one `start-all` process with API and scheduler
  children. Port 3101 and `/api/health` remained healthy after restart, and the
  live Profit dashboard returned the migrated GMV and expense-rate fields.
  Focused date, Expense and GMV regression tests passed 11/11. Temporary
  validation and rollout-repair scripts were removed; no credential, buyer
  identity or order identifier was retained.
