# Plan: Shopee Discount 折扣价格匹配与活动续期
_Locked via grill — by Codex + 用户_

## Goal

在现有 Commerce Ops 模块化单体中新增 Shopee Discount 管理模块，以数据仓库“控价”为价格真源，按单国家、多店铺范围扫描全部在售商品（包括零库存商品），通过“国家 + SKU”匹配日常控价、活动价或大促价，并安全完成两类独立工作流：当前活动纠价、下一期 30 天活动续期。所有平台写入必须经过运营预览和人工确认；任务支持百万链接、千万变体规模下的持久化执行、分页、限流、幂等、断点恢复、部分成功、平台回读、审计与人工确认的恢复方案。

## Approach

1. **沿用当前系统边界和基础设施。**
   - 新前端页面进入 `frontend/commerce-ops-vue`，复用 Vue 3、TypeScript、Vue Router、Element Plus 和共享 API 客户端。
   - 后端作为 `server.mjs` 下的新领域模块接入，拆分为 API、服务、仓储、调度、数仓客户端和 Shopee Discount 适配器，不把业务继续堆进 `server.mjs`。
   - 复用现有 Commerce Data Access、Foundation 任务/租约/操作方案、通用审计、加密工具、钉钉机器人和 Shopee 内网中转服务；不创建独立微服务、独立前端或第二套通知系统。
   - 为 Foundation 扩充明确的 Shopee Discount 领域枚举；Foundation capability 仅描述集成账号能力，不把它冒充为用户权限。
   - Foundation 只承载任务外壳与计划摘要；领域表保存不可变分片明细，并按稳定排序计算每分片哈希及整体 Merkle root。整体根哈希写入 Foundation 方案并在每批执行前验证；批准后只允许将项目标记为跳过/阻断等安全缩减，不允许修改价格、替换或新增项目，任何需要修改的项目都进入新方案版本重新确认。
   - 全量生产以 PostgreSQL 为前置条件，先锁定分区、索引、批写、归档、容量和迁移方案；SQLite 仅允许开发及受控试点（默认最多 1 店/10 变体），不得开放全量写入。

2. **建立领域数据模型和长期审计链。**
   - 保存模块设置：加密数仓 Key、掩码、验证时间、控价目录权限、每日检查时间、统一钉钉机器人、写入总开关、国家/店铺白名单、批量与并发限制。
   - 保存活动身份：国家、店铺、Shopee `discount_id`、系统活动名、运营备注、开始/结束时间、价格策略、来源（系统创建/外部创建）和当前状态；系统识别只依赖保存的 `discount_id`。
   - 保存方案与版本：单国家范围、所选店铺、默认价格档位、店铺级/链接级覆盖、数仓查询水位、价格及批准时间快照、Shopee 商品/变体快照、规则版本、内容哈希、状态和过期时间。
   - 将大量方案项和执行项按店铺及分页分片保存，包含 `shop_id + item_id + model_id`、SKU、原价、目标价、价格来源、冲突/异常、执行状态、平台错误和回读结果；避免把千万项塞入单个 JSON 字段。
   - 保存每个写入批次的幂等键、游标/checkpoint、尝试次数、执行 epoch 和未知结果的回读状态；同一“店铺 + 方案批次”只允许一个目标活动。
   - 每个外部写先事务性落一条不可变 dispatch intent（operation UUID、目标、payload hash、epoch、DISPATCHED 状态）再发请求；任何已派发但未完成官方回读的操作均视为 UNKNOWN。接管者不得重新派发 UNKNOWN，只能回读或转人工；调用期间持续续租，旧 worker 的迟到响应只能作为协调证据。
   - 每次创建活动保存唯一 operation UUID、完整目标指纹（店铺、系统名、运营备注、时间窗、档位、方案根哈希）和平台标记。只有 Shopee API 明确支持幂等键或可可靠检索的唯一标记时才允许自动协调；否则采用 at-most-once 创建，同店同时间窗严格串行，任何响应丢失直接进入人工 UNKNOWN，绝不自动重建。
   - 非幂等端点在 worker、领域适配器、HTTP 客户端、主服务代理和内网 relay 全链路禁止自动重试；连接重置、超时和响应解析失败均进入 UNKNOWN。只有验证过平台幂等键语义的端点才可按同一键重试，并以端到端故障注入测试证明没有隐藏重试。
   - UNKNOWN 人工协调使用封闭且审计的状态机，只允许：`LINK_VERIFIED_OBJECT`（关联经回读确认的平台对象）、`CONFIRMED_NOT_SENT`（必须有官方/relay 的确定性证据证明未发送）、`ABANDONED`。禁止把原 intent 改回待派发；若确需重新创建，必须新 operation UUID、新一轮冲突回读、新预览和新批准。
   - 保存执行前快照和恢复方案，恢复前必须回读当前平台状态。
   - 活动/方案摘要、确认记录、状态事件和恢复历史长期保留；变体明细在线保留两年并支持归档/汇总；通用 HTTP 审计继续按现有 180 天策略。

3. **实现数据仓库控价客户端。**
   - 管理员一次配置并验证 `zndr_` Key，加密落库，运行时仅在内存解密；密钥不得进入方案、日志、通知或错误正文。
   - 使用控价产品和筛选条件 `平台=SHOPEE + 国家 + 大品类/SKU`，读取：库存SKU、国家、大品类、平台、控价状态、日常控价及批准时间、活动价及批准时间、大促价及批准时间、数据水位。
   - 明确区分“请求失败”和“成功但无记录/无选定档位”。请求失败时缓存只能展示，禁止确认或执行；成功但目标值缺失/为 0 时才启用 Shopee 原价 1% OFF 兜底。
   - “成功但无记录”必须先通过目录/schema 版本、分页完整性、行数/游标、控价状态语义和水位校验；按国家/大品类与最近成功基线比较缺失数量及比例。超过配置的异常阈值（上线前由样本锁定，默认 fail-closed）时整批阻断人工检查，不能大面积触发 1% OFF。
   - 价格或批准时间在确认后发生变化时，只暂停受影响 SKU 并要求重新确认，其他项继续。
   - 明确区分管道水位与业务批准时间：本次查询的源最新水位/同步水位距执行时超过 35 天属于硬阻断；单 SKU 价格批准时间超过 35 天仅标记陈旧，需显式二次确认后可放行。批次中旧批准时间占比超过 20%显示批次级警告。
   - 同一方案的数仓数据必须固定到可重复读取的同一水位/事务快照；如果数仓接口无法提供快照语义，则保存首个水位并在分页结束验证水位未变化，变化即整次扫描失效重来。

4. **扩展 Shopee Discount 适配器并锁定平台语义。**
   - 在现有 Shopee 中转协议上封装列店、分页读取全部在售商品与变体、读取活动列表/详情/商品促销状态、创建/更新/结束 Discount、添加/更新活动商品，以及写后回读。
   - 现有通用 Shopee 控制台必须服务端强制为只读路径/方法白名单，即使中转支持写入也不能通过 `/api/shopee-console/call` 提交写请求。Discount 写操作使用独立的服务端适配器凭证、固定 allowlist 端点和固定请求 schema，浏览器不能提供任意 API 路径或平台凭证。
   - 中转调用需支持明确的 GET/POST 方法、请求体大小限制、超时、请求 ID、平台错误分类和认证错误隔离；不得把通用“API 控制台”的任意路径代理直接当作后台写入接口。
   - 生产写入的主服务到 relay 传输必须使用可验证的 TLS/mTLS；若现有内网只能 HTTP，则每个固定 schema 请求必须由独立密钥签名（method、path、timestamp、nonce、body hash、request ID），relay 校验短时钟窗、nonce 唯一性和重放缓存并对响应签名。未配置或校验失败时 fail-closed，写端点不得启动；只读流量可按现有受控部署策略另行评估。
   - 在实施前以当前 Shopee Open API 文档及真实只读样例锁定各站点的时间边界、价格精度、分页、稳定游标能力、批量上限、速率限制、活动重叠约束和请求/响应字段；将这些作为站点能力配置而非散落常量。
   - 商品扫描优先使用稳定游标；无稳定快照时按 `shop_id + item_id + model_id` 去重，记录扫描开始/结束水位，结束后增量补扫，并在写入前逐批重验商品身份、状态、原价和活动归属。
   - “全部在售”按 Shopee 链接状态定义，包含零库存；下架、禁售、删除或审核失败排除。活动期间转为非在售时不主动移出，只停止纠价并记录状态；重新上架后重新进入检查。

5. **实现确定性价格决策引擎。**
   - 当前版本每个 `国家 + SKU` 只有一个目标价，同一 SKU 对应的所有正常变体使用相同目标价；多个变体可共用 SKU。系统仍须验证假设：SKU 仅去除首尾 Unicode 空白，不改变大小写、符号或前导零；数仓在同一水位内必须对 `国家 + SKU + 平台` 唯一，任何重复/冲突记录或规范化碰撞阻断该 SKU 的全部变体并要求人工处理。
   - 价格档位由运营明确选择：`DAILY=日常控价`、`EVENT=活动价`、`MEGA=大促价`；系统不根据活动名称或 8.8、9.9、Payday 日期猜测档位。
   - 覆盖优先级：链接级 > 店铺级 > 国家方案默认。覆盖只属于当前方案，到期失效，下一期重新选择。当前版本不支持单变体人工档位覆盖。
   - 所有金额在边界处由十进制字符串解析为整数最小货币单位（或等价定点 decimal），数据库和哈希均使用规范字符串/整数，禁止 Node 浮点运算。站点配置必须校验币种精度、价格上下限、允许步长和最低折扣规则。
   - 数仓有有效目标值时使用国家 + SKU 统一价；字段缺失/为 0/记录不存在时按每个变体自身 Shopee 原价计算 `floor(original_minor_units × 99 / 100)`，再按站点允许步长向下归一化，标记为“原价 1% OFF 兜底”。
   - 兜底归一化后必须再次验证大于 0、平台最小/最大价格、允许步长和折扣约束；任何不合法结果只跳过并告警，绝不提交。
   - 数仓目标价大于或等于 Shopee 原价时，只跳过该异常变体，提醒“原价基准落后，需人工删除并重新添加变体”，不自动重建变体、不改用兜底价。
   - 对当前折扣价与目标价按站点最小货币单位完全比较；相差一个最小单位也算不匹配。

6. **实现当前活动纠价工作流。**
   - 每天一次自动检查，并支持“立即重新检查”。每日检查读取全部在售商品、当前 Discount 活动、数仓控价、重叠活动与新上架商品。
   - 系统创建的活动自动带出保存的价格档位和覆盖；Shopee 后台外部创建的活动必须由运营先选择默认档位并可设置店铺/链接覆盖。
   - 发现价格差异、新上架待加入、陈旧控价、活动冲突、授权异常或变体原价异常时生成系统待办并通知统一钉钉群；不自动写入。
   - 新上架且无冲突的商品生成待加入项；距离当前活动结束不足 24 小时时不再补入当前活动，只进入下一期方案。
   - 重叠 Discount 活动阻断对应商品，展示原活动身份、起止时间与价格；系统不结束、不删除、不篡改原活动。数据库对同店目标时间窗设置互斥/排他锁，每个写批次紧邻写入前再次读取平台活动归属，并在平台支持时携带版本/前置条件。写后立即回读冲突状态；若平台不提供原子前置条件，则明确保留外部运营竞态，检测到冲突立即停止该店后续写入并转人工，绝不覆盖或自动重试。

7. **实现下一期活动草稿与续期工作流。**
   - “提前 24 小时”是运营期望的默认提醒，不是容量承诺。实施前用真实 API 限额建立容量公式和完成时间 SLO（扫描、创建、加商品、回读、退避均计入）；系统按所选范围的预计耗时在 T-24h 之前动态提前数日生成并要求确认。若剩余时间无法满足 SLO，拒绝该范围准入并要求拆分/缩小，不能承诺无缝完成。
   - T-24h、T-6h、T-1h 仍发送提醒。提醒实现为 UTC 持久化 due-job，保存店铺 IANA 时区、唯一通知键、发送结果和停机 catch-up 规则；草稿必须由运营人工确认，系统不能无人值守创建或改价。
   - 一个方案只覆盖一个国家，可选择该国家多家店铺；默认勾选已授权且正常运营的店铺。每店创建独立活动并保存独立 `discount_id` 和执行结果。
   - 默认商品范围是方案生成时店铺全部在售商品，包括零库存，不继承旧活动商品清单。
   - 正常下一期持续 30 天并与旧活动按店铺当地时区及 Shopee 允许边界无缝衔接；错过后从平台允许的最近时间开始，不回填历史空档并记录告警。
   - 运营可提前确认；系统立即预创建未来活动并分批加入商品，不等旧活动结束才执行。
   - 运营提前结束活动会让原续期草稿失效。8.8、9.9、Payday 等大促由运营明确新建方案和选档；大促结束后新建完整 30 天日常周期，不恢复旧活动剩余天数。
   - 活动名固定为 `PM-{国家}-{DAILY|EVENT|MEGA}-{开始日期}-{批次短码}`，另存运营备注。

8. **实现方案编辑、批量覆盖与人工确认。**
   - 支持页面搜索/勾选店铺、链接、item_id、SKU，也支持上传批量覆盖清单；清单字段至少为 `shop_id`、可选 `item_id/商品链接`、`price_tier`、备注。
   - 解析链接后必须回显店铺、item_id、SKU、变体数、最终档位和规则来源；无法匹配、重复或冲突规则不能静默忽略。
   - 沿用现有共享 `APP_ACCESS_TOKEN`，不新增用户账号/RBAC。模块开关（查看、编辑、执行、设置）是部署级能力和放量门禁，不宣称为个人权限；服务端禁止从请求体接受可信审计身份。
   - 生产启动必须显式选择并验证写安全模式：`trusted_single_role`（部署负责人以受管配置声明所有持钥者均获准写入，并记录部署证明）或 `separate_execute_identity`（可信反向代理身份/独立执行凭证）。缺失、无效或与监听方式冲突时，服务端强制真实写开关关闭并拒绝执行接口；不能依赖文档约定或运行人员记忆。
   - 在 `separate_execute_identity` 模式下，特权身份必须同时参与最终批准和执行；批准事件绑定该身份、方案 ID、Merkle root、策略哈希和有效期，执行时重新验证批准未过期且根哈希未漂移。共享 Token 只能生成/编辑预览，不能完成最终批准。
   - 数仓 Key、Shopee/relay 写凭证、写安全模式、真实写开关、国家/店铺白名单、安全上限、并发/批量限制和停写阈值属于安全敏感设置：`separate_execute_identity` 模式下只有特权身份可修改，共享 Token 仅能读取脱敏状态；变更写入专用审计并使未执行批准失效。
   - 确认时要求填写运营姓名、输入精确确认文字并提交方案哈希；姓名仅为人工声明，不宣称为可信登录身份。数据结构预留未来多级审批，但当前只需单名运营确认。
   - 执行前重查数仓价格/批准时间、活动冲突、商品状态和授权；变化项从已确认方案中隔离为待重新确认，不静默采用新值。

9. **实现大规模、幂等且可恢复的后台执行。**
   - 目标容量按单国家 1,000 店、100 万链接、1,000 万变体设计。生成和执行均为持久化后台任务，页面关闭或服务重启不丢失；分页流式读取、分片落库、受控并发、租约、checkpoint 和进度聚合。
   - 并发控制分全局、国家、店铺和接口端点四层，并根据 Shopee 实际限流动态退避；店铺失败隔离，不阻断其他店铺。
   - 租约采用 fencing：每次接管递增执行 epoch，批次通过数据库条件更新原子认领；每次外部写前验证 lease/epoch并在网络调用期间持续续租，租约丢失立即停止新派发。已持久化为 DISPATCHED 且未完成官方回读的 intent 一律进入 UNKNOWN；新 owner 只能协调回读，不能再次派发。旧 worker 的迟到响应不得直接推进状态。
   - 同一方案重复确认只能恢复原任务；超时或结果未知必须先回读 Shopee，再决定跳过、继续或人工处理，禁止盲目重试或创建重复活动。
   - 逐商品/变体执行，允许部分成功，不整批回滚；一个异常变体只跳过自身。已成功项不因后续失败回滚。
   - 每次写入后回读活动归属和折扣价，将结果分为成功、冲突、Shopee 拒绝、授权失败、未知待回读、跳过和待重新确认。
   - 授权异常按店铺隔离并发高优先级待办；重新授权后重新读取活动冲突、商品状态和数仓价格，再从原方案续跑。

10. **实现通知、待办、恢复与可观测性。**
    - 系统内待办是权威记录，钉钉只发送批次摘要、风险数量和系统入口，不包含完整价格表或密钥；当前所有任务统一发送到一个已配置机器人，配置模型预留未来国家/店铺路由。
    - 通知失败不改变业务任务结果，但需记录并形成可重试的通知状态。
    - 保存执行前每个变体的活动归属和价格快照；误操作时先回读当前状态，再生成“可恢复、已被再次修改、状态不明”清单，运营确认后分项恢复。
    - 不提供未经确认的一键回滚；新建错误活动也先检查现状，再由运营确认结束。
    - request ID 贯穿主服务、后台任务、数仓请求、Shopee 中转、写入批次、回读和钉钉通知；日志和审计统一脱敏。
    - 记录并监控队列等待时间、扫描/写入/回读吞吐、平台限流率、失败率、租约丢失/epoch 冲突、UNKNOWN 数量与年龄、授权失败、通知失败和预计完工时间；按国家/店铺/端点展示仪表盘，并为每项配置告警阈值与自动停写条件。

11. **测试、验收与逐级上线。**
    - 单元测试覆盖价格选择、覆盖优先级、1% OFF 向下取整、最小单位比较、陈旧预警、状态转换和幂等键。
    - 定义版本化审批 canonicalization 合同：唯一不可变 item key、重复键拒绝、字段类型与 UTF-8 编码、金额规范字符串、NULL/空值语义、叶节点字段集合、稳定排序、分片边界、树构造/奇数叶 padding 和根哈希版本；可变执行状态完全排除在哈希载荷之外，并以跨 Node/PostgreSQL 的 golden vectors 验证。
    - 合同测试覆盖数仓返回、Shopee Discount 请求/响应、分页、限流、平台错误和写后回读；使用固定 fixture，不在普通测试中真实写平台。
    - 集成测试覆盖草稿、人工确认、执行前漂移、部分成功、超时未知回读、服务重启续跑、授权恢复和恢复方案。
    - 规模测试使用生成数据验证 1,000 店/100 万链接/1,000 万变体下的内存上限、PostgreSQL 分区/索引/批写、数据库分页、归档与任务恢复，不要求一次性把所有明细加载进浏览器；同时用真实 API 限额做容量/SLO 演练。
    - 上线分四阶段：只读扫描；测试店最多 1 店/10 变体；国家白名单逐步扩到 10 店/100 店；完成真实限流、幂等、断点和回读验证后开放全部授权店铺。
    - 保留独立真实写总开关、国家/店铺白名单和单批上限，后端必须强制执行，不能只靠前端禁用。
    - 指定测试店铺连续完成 3 个涵盖创建、纠价和恢复的批次，且无重复活动、无未经确认写入、无错误价格成功写入，才允许扩大白名单。

## Key decisions & tradeoffs

- 数据仓库是目标价真源，但活动档位由运营明确选择；放弃自动猜测活动类型，换取确定性和可审计性。
- 同一国家 SKU 正常情况下同价；只有缺价兜底按链接/变体自身原价计算，因此允许跨店出现不同兜底价。
- 缺价使用原价 1% OFF，而查询失败禁止执行；两者必须严格区分。
- 目标价不低于原价时跳过异常变体并提醒人工重建，不阻断整个链接，也不自动修复商品结构。
- 当前活动纠价与下一期续期是两个独立方案，分别预览、确认和执行。
- 重叠活动由运营处理；系统不抢占、不自动结束其他活动。
- 允许部分成功，不整批回滚；未知结果必须回读，恢复也必须再次确认。
- 当前认证只能证明共享访问密钥，不具备用户身份；本期接受声明式运营姓名并明确其审计局限，避免为本模块顺带建设全站 IAM。
- 复用 Foundation 任务和操作方案作为控制面，但千万级明细进入不可变领域分片表，并用 Merkle root 将审批绑定到完整集合，避免巨大 JSON、单行热点和审批后替换。
- 全量生产使用 PostgreSQL；SQLite 只承载有硬上限的本地开发/试点。
- 代码按全量规模设计，真实写入按白名单逐级放量。

## Risks / open questions

- 实施前必须从当前 Shopee 官方文档和内网中转能力确认 Discount 写端点、HTTP 方法、字段、每次批量上限、速率限制、站点价格精度、时间边界、活动商品冲突语义和未来活动预创建限制；这些技术事实不得凭记忆实现。
- 当前 Shopee 健康客户端只封装 GET 调用，写入协议和安全 allowlist 需要扩展，并必须避免把任意路径通用代理变成后台写权限。
- 当前共享 Token 无法可信区分具体运营人员；声明姓名可追责性有限。真实写入仅适用于所有持钥者都属于获准写入范围的受控部署；否则必须先增加可信代理身份或独立执行凭证。未来个人身份系统上线后需要迁移 `created_by/approved_by`。
- 现有 Foundation `domain` 枚举不包含折扣领域；需选择正式扩展枚举或使用稳定的现有领域边界，不能用不准确的领域值蒙混。
- PostgreSQL 全量生产的分区键、明细归档、索引、批写大小、连接池和容量预算仍需在实现设计中以规模测试锁定；SQLite 不能作为全量回退。
- 数仓查询 API 的后台服务端集成方式、凭证授权范围和请求限流需在实现阶段确认；本次只验证了浏览器查询工具的可见字段与样例。
- 钉钉消息中的系统方案入口只有在部署地址对群成员可访问时才有效；本地/回环地址不得作为消息链接。

## Out of scope

- 变体倍率、固定加价或规格分档定价。
- 单变体人工价格档位覆盖。
- 根据日期或活动名自动识别 8.8、9.9、Payday 并自动选择档位。
- 未经人工确认的自动创建活动或自动改价。
- 自动结束、删除或篡改重叠活动。
- 自动删除并重新添加 Shopee 变体。
- 多级审批。
- 用户账号、个人身份认证和 RBAC。
- 跨国家单批方案。
- 未经确认的自动回滚或恢复。
- 用缓存控价在数仓不可用时执行写入。
