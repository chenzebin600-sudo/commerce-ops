# 单条产品 SKU 软删除基线决策

## 1. 决策状态

- 审计对象：菲律宾 `P2DD0020938`
- 当前状态：已确认保留软删除，接受为新正式基线
- 删除时间：2026-07-22 18:07:22（Asia/Shanghai）
- 操作来源：本机回环访问、`local_session`
- 审计结论：高度符合人工主动删除；虽未填写删除原因，用户现已明确确认保留
- 用户确认：2026-07-23 14:48:07（Asia/Shanghai）明确选择方案 A
- A2 合并状态：软删除决策阻断已解除，允许进入独立的 A2 合并流程
- 正式数据库：本节点保持只读，未恢复、未删除、未执行 checkpoint 或 VACUUM

## 2. 可识别产品信息

| 项目 | 值 |
|---|---|
| 国家 | 菲律宾 |
| SKU | `P2DD0020938` |
| 国家 + SKU | `菲律宾\|P2DD0020938` |
| 产品名称 | 1D-太阳能板8W |
| 主 SKU | `DD0938` |
| 款号 | `1D0116` |
| 款名 | 太阳能板 |
| 一级 / 二级类目 | 户外照明 / 家用照明 |
| 产品包状态 | 清仓商品 |
| 生命周期 | `CLEARANCE` |
| 销售规格 | 材质铝+玻璃+PET35*2 5*7cm；6V 8W线材带公头线长 5m；多晶硅电池片转换率18%以上 |
| 产品记录 ID | `f7290474-bcc5-4c8c-a6d1-13b49a37495b` |
| 当前来源行 ID | `7b2a4c43-c7e7-4e8f-8388-a17862dab28d` |
| 产品包行 ID | `742a9f4e-b3c3-477e-81e4-f97d5970cae7` |
| 最近来源批次 | `be63775d-71b6-46b3-9373-903dd88dfd09` |
| 来源行号 | 3 |

正式库中没有同一 SKU 的其他国家记录。款型模型中共有 4 个国家 SKU，其中 3 个仍正常，只有上述菲律宾 SKU 被软删除。

## 3. 证据来源

删除前证据使用只读一致性备份：

- `storage/backups/growth-radar-mainline-20260722013312/commerce-ops-before-013-014.sqlite`
- 备份时间早于删除时间
- SHA-256：`cc54113b2b6761033c72516b08954c76ba797b6d8426cdf85a6aa63c53c226b7`
- `PRAGMA integrity_check`：`ok`
- `PRAGMA foreign_key_check`：0

删除后证据来自静止状态下的正式 SQLite 只读连接，以及操作审计事件：

- 审计事件 ID：`d708da60-7166-4bb2-b055-8ddda497d90e`
- 审计动作：`product.deleted`
- 请求方法：`DELETE`
- 请求路径模式：`/api/product-center/products/{productId}`
- 执行结果：成功，HTTP 200
- 请求来源：本机回环地址
- 操作者类型：`local_session`
- 审计元数据仅包含产品 ID、国家、SKU 和软删除结果；未记录删除原因

## 4. 删除前后字段对比

| 字段 | 删除前 | 删除后 | 说明 |
|---|---|---|---|
| `revision` | 2 | 3 | 按软删除规则递增 |
| `updated_at` | 2026-07-21 17:44:17（Asia/Shanghai） | 2026-07-22 18:07:22（Asia/Shanghai） | 记录本次变更时间 |
| `deleted_at` | `NULL` | 2026-07-22 18:07:22（Asia/Shanghai） | 标记为已删除 |
| `deleted_by` | `NULL` | `local_session` | 记录操作来源 |
| `delete_reason` | `NULL` | `NULL` | 用户未填写删除原因 |
| `restored_at` | `NULL` | `NULL` | 尚未恢复 |
| `restored_by` | `NULL` | `NULL` | 尚未恢复 |
| `archived_at` | `NULL` | `NULL` | 未归档 |

产品名称、国家、SKU、主 SKU、款号、款名、规格、类目、产品包来源和生命周期等业务事实字段均未变化。删除动作只改变软删除状态和版本字段。

## 5. 关联影响

| 关联对象 | 当前数量 | 删除前后变化 | 实际影响 |
|---|---:|---|---|
| 产品包当前事实行 | 1 | 无变化 | 中台事实仍保留 |
| 历史导入记录 | 3 | 无变化 | 来源批次和历史可追溯 |
| 产品图片 | 1 张有效图片 | 无变化 | 文件和图片元数据仍保留 |
| 人工字段覆盖 | 1 个有效覆盖、2 条变更事件 | 无变化 | 人工产品名称仍保留 |
| Listing 草稿 | 0 | 无变化 | 没有需要归档的草稿 |
| Listing 发布记录 | 0 | 无变化 | 没有已发布 Listing 受影响 |
| 产品库存快照 | 2 条、1 个仓库 | 无变化 | 历史库存仍保留 |
| 产品成本快照 | 2 | 无变化 | 历史成本仍保留 |
| 包装档案 | 1 | 无变化 | 包装事实仍保留 |
| 生命周期 | 1 条 `CLEARANCE` | 无变化 | 生命周期事实仍保留 |
| Growth Radar 事实 | 0 | 无变化 | 当前无雷达订单、库存、观察或覆盖记录 |
| 马帮图片映射 | 正式库尚无 015 表 | 不适用 | 支线 B 尚未合并 |
| 导出文件 / 任务精确关联 | 0 | 无变化 | 无关联文件或任务受影响 |

软删除后：

1. 产品中心默认查询使用 `deleted_at IS NULL`，因此该 SKU 不再出现在正常产品列表中；拥有恢复权限时仍可在“已删除产品”中查询。
2. 产品记录本身没有物理删除，产品包事实、图片、人工覆盖、库存、成本、包装和生命周期历史都继续保留。
3. 当前没有 Listing 草稿或发布记录，因此没有 Listing 内容被隐藏或归档。
4. 当前没有 Growth Radar 订单或库存事实。产品库存快照仍完整保留，不存在外键孤立。
5. 支线 B 的马帮图片匹配代码只查询 `archived_at IS NULL AND deleted_at IS NULL` 的产品。若保留删除，未来图片采集不会为该菲律宾 SKU 创建新建议或关联；已有 1 张人工产品图片不受影响。

## 6. 是否属于人工意图

结论：**高度符合人工主动删除**。

依据：

- 请求来自本机回环地址和 `local_session`，发生在系统实际使用时间窗口内。
- 操作为明确的产品 DELETE 接口，不是导入、调度或后台清理动作。
- 前端删除按钮受 `product.delete` 能力控制。
- 点击删除后会打开专用确认弹窗，弹窗展示产品名称、SKU、国家、仓库、图片和人工修改状态，并要求再次点击“确认删除”。
- 操作返回 HTTP 200，审计记录明确标记 `product.deleted` 和 `soft_deleted`。
- 自动化测试使用独立临时数据库，没有发现测试进程写入正式库的证据。

限制：删除原因是选填项，本次为空；审计没有可识别的个人账号标识。技术证据本身不能替用户确认业务决定，但用户已在本任务中明确选择保留软删除。

## 7. 方案 A：保留软删除，接受为新正式基线

用户已于 2026-07-23 14:48:07（Asia/Shanghai）明确确认：

> A：保留软删除，接受当前状态为新基线

执行结果与边界：

1. 不修改当前 `product_skus` 记录。
2. 用户已明确确认“保留软删除”。
3. 将当前静止库状态记录为新的正式基线。
4. 使用现有一致性备份：`storage/backups/pre-g1b-merge-audit-20260722T102353Z/commerce-ops-before-g1b.sqlite`。
5. 该备份 SHA-256 为 `be3d47a7c97eb2a4d8a3f9e995ddce1f22677920097bf3ffa613d829484aad72`，完整性检查为 `ok`，外键异常为 0，最高迁移为 014。
6. 正式库和基线备份中的目标 SKU 均保持 `revision=3` 及相同软删除状态。
7. 软删除决策阻断已经解除，允许进入独立的 A2 合并流程。
8. A2 合并不等于生产启用；生产启用限制继续保留。

适用条件：该菲律宾 SKU 确实不应继续进入产品中心运营池，也不需要未来马帮图片建议。

## 8. 方案 B：恢复该 SKU

本方案仅设计，本节点不执行：

1. 保持服务停写并复核当前静止状态。
2. 启动受控主服务，通过现有 `POST /api/product-center/products/{id}/restore` 接口恢复，不直接执行手工 SQL。
3. 由产品服务清除 `deleted_at`、`deleted_by` 和 `delete_reason`，写入 `restored_at`、`restored_by`，并按系统规则递增 `revision`。
4. 确认产生成功的 `product.restored` 审计事件，且日志不包含凭证或隐私数据。
5. 验证产品默认查询重新显示该 SKU；图片、人工覆盖、Listing、库存、成本、包装、产品包来源和生命周期关联保持正确。
6. 验证支线 B 合并后该 SKU 可以重新参与马帮图片匹配。
7. 再次停止所有写入进程，进行不少于 10 分钟的静止观察。
8. 使用 SQLite Online Backup 创建新的合并前一致性备份，校验完整性、外键、迁移编号和核心计数。
9. 新基线通过后，再允许进入 A2 合并流程；生产启用限制仍继续保留。

适用条件：该 SKU 仍需在产品中心展示、进入运营池，或需要继续接收马帮图片建议。

## 9. 最终决策与后续边界

最终决策：**方案 A：保留软删除，接受当前状态为新基线**。

该决策已记录，不再等待软删除意图确认。方案 B 仅作为未来业务决定变化时的受审计恢复预案保留。

当前允许在新的独立节点中执行 A2 合并前检查与合并，但本节点不执行合并。支线 B 仍未合并，迁移 015 和 016 仍未创建或应用，生产启用仍保持阻断。
