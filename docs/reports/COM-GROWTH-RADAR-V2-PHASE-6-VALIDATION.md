# COM-GROWTH-RADAR-V2 阶段 6 复盘：验收

> 日期：2026-07-25（Asia/Shanghai）
> 指标合同：`GRV2-METRICS-1.0.1`
> 代码基线：`master` / `a8327c524764f89eda8127e32b4aa48e38c3fac6`

## 1. 做了什么

- 对迁移 019、确定性指标引擎、API 和前端工作区执行专项与全量回归。
- 使用正式 SQLite 的只读在线备份创建隔离副本，只在副本上应用迁移 019、生成分析并进行页面冒烟。
- 验证桌面端、430px 移动端、图表、配置表局部滚动、SKU 证据抽屉和“更新分析”幂等行为。
- 从前端保存国家映射和规则版本，并确认新 published 运行引用新配置。
- 验证失败刷新不会替换最新已发布结果，页面不会把不可用的国家或店铺覆盖显示为零。
- 检查浏览器控制台、SQLite 完整性、外键、便携路径和代码差异格式。

## 2. 自动测试

| 门禁 | 结果 |
|---|---:|
| COM-015 专项回归 | `63/63 PASS` |
| PostgreSQL readiness / e2 专项 | `8/8 PASS` |
| Growth Radar 全族 | `177/177 PASS` |
| 全量测试 | `693/693 PASS` |
| Build | `PASS` |
| Doctor | `PASS` |
| `git diff --check` | `PASS` |

Build 同时确认：

- 便携路径检查通过。
- 前端存在 `466` 个唯一元素 ID。
- 前端存在 `217` 个有效静态绑定。

## 3. 隔离数据库验证

隔离数据库位于系统临时目录，文件名为 `visual.sqlite`；报告不记录用户目录或本机绝对路径。

验证结果：

- 迁移链 `001` 至 `019` 成功应用。
- `integrity_check=ok`。
- 外键异常为 `0`。
- 最新库存批次包含 `20,026` 行，分析读取 `20,022` 行有效库存事实。
- 聚合生成 `10,327` 个全盘 SKU 指标。
- 发布 `7,695` 条确定性机会、风险或数据阻断信号。
- 分析运行 ID：`431786e8-c200-4544-bf89-133e36181f10`。
- 完整分析从 `2026-07-25T03:38:20.538Z` 到 `2026-07-25T03:38:23.923Z`，约 `3.4` 秒。

正式 SQLite 没有应用迁移 019，没有写入分析运行、指标或信号；正式库仍停留在迁移 018。

## 4. 页面验收

隔离演示地址：

`http://127.0.0.1:3194`

桌面端 `1280x720`：

- 页面无横向溢出。
- 驾驶舱、货盘分析、配置管理和 SKU 详情均可用。
- 信号结构、库存健康和类目销量图表均包含实际数据。
- SKU 详情显示规则、输入、阈值、公式和证据。

移动端 `430x900`：

- 页面无整体横向溢出。
- 顶部模式切换和“更新分析”按钮完整可见。
- 图表单列展示，规则配置表单单列展示。
- 宽配置表和历史表使用局部横向滚动，不挤压页面布局。

浏览器控制台错误：`0`。

## 5. 质量降级验证

空国家映射基线仍保持 fail-closed：

- 国家级分析不可用。
- 店铺跨源销售覆盖不可用。
- 店铺列表正确显示空态，不返回伪造的 `0%` 覆盖率。
- 全盘 SKU 分析仍可发布并正常展示。

该状态符合 `GRV2-METRICS-1.0.1` 的 fail-closed 要求。

在隔离数据库中通过前端确认国家映射后：

- 保存了 `25` 条确认映射和 `2` 条异常编码排除项。
- 页面明确提示配置需要重新分析。
- 新 published 运行引用新配置，国家级 SKU 分析恢复可用。
- 店铺事实为空时仍不伪造销售覆盖率。

## 6. 遇到的问题

- 旧 Growth Radar 根节点继承桌面最小宽度，首次在 430px 视口造成内容裁切。
- 已将工作区和两个子根节点约束为 `width: 100%`、`max-width: 100%`、`min-width: 0`，复测通过。
- 配置宽表首次在移动端继承内容最小宽度；已约束配置网格、区块和历史列，并复测为局部横向滚动。
- Doctor 发现广告端口已被既有进程占用并给出警告，但主应用、数据库隔离和完整性检查均通过。

## 7. 是否需要架构调整

不需要。当前实现符合已冻结设计：

- 只使用确定性规则。
- 每条信号保存规则版本、输入、阈值、公式和证据。
- 新分析失败时继续服务上一成功发布结果。
- 不推断在线 Listing 覆盖。
- 不自动执行经营动作。
