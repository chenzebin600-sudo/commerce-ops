# PostgreSQL WAL 生命周期与清理建议 V2

生成时间：2026-08-10 11:24 CST  
结论：**当前可安全删除 0 个文件；未执行 WAL 删除。**

## 当前 WAL 库存

| 项目 | 当前值 |
|---|---:|
| 加密 WAL 段 | 5,107 个 |
| WAL 段总大小 | 85,681,517,890 bytes（79.797 GiB） |
| 备份历史标记 | 2 个，合计 880 bytes |
| 最早段 | `00000001000000050000004C.aes256gcm` |
| 最新段 | `00000001000000190000003E.aes256gcm` |
| 最早时间 | 2026-08-06 19:13:55 CST |
| 最新时间 | 2026-08-10 10:42:45 CST |
| D 盘可用 | 33.805 GiB |
| 容量状态 | WARNING；50 GiB 告警，80 GiB严重告警 |

`check_wal.ps1` 本次输出 `deletionPerformed=false`。监控脚本只枚举和写证据，不包含删除逻辑。

## 当前保留的两个物理恢复点

| 恢复点 | 状态 | 创建时间 | WAL 范围 | 大小 |
|---|---|---|---|---:|
| `2026-08-10-unverified-attempt1` | `pg_verifybackup` 再次 PASS；缺少正式元数据，但物理文件有效 | 10:20:45 | `19/3C000028` → `19/3C000158` | 2.847 GiB |
| `2026-08-10` | VERIFIED；独立恢复 PASS | 10:33:48 | `19/3E000028` → `19/3E000158` | 2.847 GiB |

第一次 attempt 不是损坏或字节重复的失败副本。它是比正式备份更早的独立一致恢复点，因此在本轮“存在恢复价值则保留”的规则下不删除。只要它被保留，WAL 生命周期边界必须以它的起始段 `00000001000000190000003C` 为准，而不能只以正式备份的 `...3E` 为准。

## 7 天策略交集

- 本次 7 天截止点：2026-08-03 11:24:27 CST。
- 早于截止点：0 个、0 GiB。
- 早于最老保留恢复点 `...3C`：5,104 个、79.750 GiB。
- 位于 `...3C` 至正式恢复点 `...3E` 之前：2 个、0.031 GiB；为保护第一次恢复点后的连续恢复能力而保留。
- 同时满足“早于 7 天”与“早于最老保留恢复点”的严格交集：**0 个、0 GiB**。

当前安全删除范围为空，预计立即释放空间为 **0 GiB**。最早 WAL 要到 2026-08-13 19:13:55 CST 后才开始达到 7 天条件。

## 未来候选计算规则

每个候选必须同时满足：

1. 文件名是完整 24 位 WAL 段，而不是正在写入的临时文件；
2. `LastWriteTime < now - 7 days`；
3. 段号早于所有当前保留物理恢复点中最早的起始 WAL 段；
4. 最新正式基础备份仍为 VERIFIED；
5. 最新独立恢复证据仍为 PASS；
6. PostgreSQL 归档失败数仍为 0；
7. 人工确认本批次的截止时间、文件数、首尾段和字节数。

达到条件后，建议先移动到带日期的 WAL 隔离目录，重新执行恢复演练，再由第二次人工确认执行不可逆删除。计划任务不得直接删除 WAL。

## 第一次 attempt 对边界的影响

如果未来用户明确批准删除 `2026-08-10-unverified-attempt1`，并再次确认只保留正式 `...3E` 恢复点，生命周期边界才可前移到 `00000001000000190000003E`。这会使额外 2 个段（约 0.031 GiB）进入“边界之前”集合，但仍必须分别满足 7 天条件。

本报告不构成删除第一次 attempt 或任何 WAL 的授权。

## 长期风险

当前只有“监控 + 人工确认”的生命周期机制，没有自动清理。它避免误删恢复链，但如果告警无人处理，WAL 仍会增长。当前已经非常接近 80 GiB CRITICAL；进入 CRITICAL 后应优先人工审查达到 7 天的严格候选，而不是绕过恢复门槛。
