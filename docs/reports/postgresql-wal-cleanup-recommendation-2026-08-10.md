# PostgreSQL WAL 清理建议（仅建议，未执行）

> 状态更新：本报告已由 `postgresql-wal-cleanup-recommendation-v2.md`
> 取代。V2 识别到第一次物理备份 attempt 仍是有效的更早恢复点，因此采用
> 更保守的 `...3C` 生命周期边界；任何后续判断应以 V2 为准。

生成时间：2026-08-10 10:52 CST  
当前决策：**不删除**  
`deletionPerformed`：`false`

## 当前库存

- 归档目录：`D:\PostgreSQLBackups\wal`
- 加密 WAL 段：5,107 个
- 总分配：85,681,517,890 bytes（79.797 GiB）
- 最早段：`00000001000000050000004C.aes256gcm`
- 最新已归档段：`00000001000000190000003E.aes256gcm`
- 最早修改时间：2026-08-06 19:13:55 CST
- 最新修改时间：2026-08-10 10:42:45 CST
- D 盘可用：33.812 GiB
- 阈值状态：WARNING（50 GiB），距离 CRITICAL（80 GiB）约 0.203 GiB

## 已验证的恢复边界

- 正式基础备份：`D:\PostgreSQLBackups\base_backup\2026-08-10`
- 清单验证：PASS
- 独立恢复启动：PASS
- 业务一致性：PASS
- 清单 WAL：`19/3E000028` → `19/3E000158`
- 起始 WAL 段：`00000001000000190000003E`

按段号观察，起始段之前共有 5,106 个文件、79.782 GiB；对“仅从 2026-08-10 正式基础备份恢复”而言，它们属于前置历史段。但是这一集合目前不能直接删除，因为 7 天保留条件尚未满足。

## 7 天策略计算

- 本次截止点：2026-08-03 10:52 CST
- 修改时间早于截止点的文件：**0 个**
- 当前严格策略候选：**0 bytes / 0 GiB**
- 当前批准删除：**0 个文件**

因此，虽然前置历史段占用很大，“恢复边界之前”与“已满 7 天”两个集合的交集目前为空。最早一批文件要到 2026-08-13 19:13:55 CST 后才开始达到 7 天条件。

## 建议的未来人工流程

1. 由 `check_wal.ps1` 重新生成当时的 7 天候选数量和大小。
2. 再次确认最新正式基础备份与对应独立恢复证据仍为 PASS。
3. 候选取交集：`LastWriteTime < now-7d` 且段号早于最新 VERIFIED 基础备份起始段。
4. 人工确认具体批次、文件数、字节数和截止时间。
5. 优先移动到 `D:\PostgreSQLBackups\wal-quarantine\YYYY-MM-DD`，不要直接永久删除。
6. 隔离后再次执行恢复演练；第二次人工确认后才永久删除隔离副本。

## 不建议的做法

- 不要按“总量太大”直接删除 79.782 GiB 前置段。
- 不要只凭 `archive_command` 成功率判断可恢复性。
- 不要将逻辑 `pg_dump` 当成 WAL 恢复链的物理基础备份。
- 不要让计划任务自动清空 WAL 或隔离目录。

本报告是候选生成结果，不是删除授权。等待用户对未来达到 7 天条件的具体批次另行确认。
