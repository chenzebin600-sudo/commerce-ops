# 每日进度记录

用于持续记录项目每天完成的工作、实际验证结果、风险和下一步计划。新记录放在最上方；不得写入账号密码、访问令牌、Cookie 或完整个人信息。

## 2026-07-30

### 今日完成

- 将自动发货功能分支同步到最新远程主分支，经专项测试后提交 PR，并确认无代码冲突后合并到 `master`。
- 新增统一开发入口 `npm.cmd run dev`，一次启动主系统、主系统调度器和自动发货服务；主系统与发货服务支持代码变更自动重载。
- 将 `npm.cmd start` 改为统一正式启动入口，由顶层守护程序分别管理主系统、主系统调度器和现有发货守护程序。
- 统一守护程序增加单实例锁、健康服务复用、重复进程保护、单服务独立重启、最长 60 秒退避和统一日志轮转。
- Windows 登录启动从仅托管 `3112` 的 `ZNWX Mabang Fulfillment` 升级为同时托管整套系统的 `ZNWX Commerce Ops`；安装器先创建新任务，成功后才移除旧任务。
- 新增整套系统停止、重启和运行状态检查入口；状态窗口会同时检查 `3101` 和 `3112`。
- 保留旧的 `run-fulfillment-supervisor.cmd` 作为兼容入口，旧快捷方式调用时也会转入统一守护程序。
- 更新项目 README 和后台运行说明。
- 完善自动发货异常雷达和异常中心，统一识别库存不足、多仓、运单号延迟和马帮登录异常，并按严重程度优先展示。
- 异常记录增加店铺、订单、发现时间、持续时长、问题说明和建议动作；支持按四类异常筛选，并对可复核订单提供只读重新核对入口。
- 增加持久化预警去重：登录异常 30 分钟冷却，库存、多仓和运单延迟 6 小时冷却，服务重启后不会对同一问题重复弹窗。
- 扫描记录持久化失败分类和详情，使登录失效能够在看板持续显示，而不只存在于临时日志中。
- 修复发货看板每 20 秒自动刷新时整页闪烁：首次进入才显示骨架加载，后续改为后台静默刷新；店铺筛选项仅在配置变化时重建，避免刷新打断正在进行的筛选操作。
- 接入马帮“最后发货期限”：按页面明确的 UTC+8 口径解析，扫描候选与预览优先保留最早到期订单，期限缺失时回退到原付款时间顺序。
- 增加发货时效四级预警：剩余 24 小时提示、6 小时关注、2 小时紧急、超时紧急；预警进入异常雷达、异常中心、待处理订单表和 Windows 通知。
- 时效预警按订单与阶段持久化去重，只有从 24 小时升级到 6 小时、2 小时或超时时才产生新阶段提醒；库存、多仓、状态、运单号和物流渠道安全检查保持不变。
- 修复 Windows 停止脚本隐藏“Access denied”导致后台服务未停止的问题：新脚本自动请求管理员权限、等待扫描或真实批次安全结束、按守护锁精确停止进程并核验 3101/3112 已释放；新增一键进入自动重载开发模式入口。
- 修复未返回“最后发货期限”的订单被 `Number(null)` 误判为“超时 0 分钟”的问题；空期限现在明确显示“期限未知”且不产生时效预警。在线接口复核确认错误时效预警已归零，数据库未留下错误通知冷却记录。
- 定位并接入马帮订单列表的真实时效字段 `closeDateType / closeDateText / closeDay`；该字段组与页面“剩余发货”列使用同一数据源，不再依赖 Shopee 订单导出中为空的“最后发货期限”。
- 抓单时将马帮剩余时间按 UTC+8 换算为绝对截止时间，并合并到同一订单的全部 SKU 明细；自动排序、看板和 Windows 分级通知继续使用统一截止时间口径。

### 实际验证

- 统一启动专项、运行环境兼容和自动发货回归测试共 86 项，全部通过。
- 前端构建和可移植路径检查通过。
- 当前 `3112` 正在由旧后台任务运行时，`npm.cmd run dev` 能识别端口占用并拒绝启动重复发货进程；现有自动发货服务未被停止或重启。
- 在无扫描、无活动批次的安全窗口执行在线迁移；本机无权新建计划任务，安装器按设计自动回退到当前用户“启动”文件夹并启动统一守护。
- 在线日志确认统一守护成功启动主系统和主系统调度器，并识别已有健康的 `3112` 后只监控、不启动重复发货服务。
- 在线检查确认 `3101/api/health` 与 `3112/health` 均正常，自动发货无扫描、无活动批次且并发仍为 2。
- 首次在线检查发现主系统健康路径误写为 `/health`，已修正为实际的 `/api/health`，并重新通过 86 项测试和构建检查。
- 异常预警专项 57 项测试全部通过，覆盖四类异常聚合、登录预警冷却、库存与多仓合并通知、运单延迟通知及原有发货安全规则；前端构建与语法检查通过。
- 发货时效功能加入候选保留、期限排序、UTC+8 解析、预览分级、看板聚合和通知升级测试；发货模块测试增加到 60 项并全部通过，前端构建通过。
- 真实只读对照通过：Toko Penguin 订单页面显示约 7 小时剩余，接口返回 426 分钟并判定为 `due_soon`，看板生成“剩余 425 分钟”时效预警；JOJO Mall 订单页面约 1 天 7 小时，接口返回 1866 分钟。两次均只生成预览，未确认、未提交发货。
- 新增 2 项 Python 时效转换与多 SKU 合并测试，马帮安全测试 18 项、发货模块测试 60 项全部通过。项目全量 Node 测试另有 4 项既存环境失败和 1 项取消，与本次时效改动无关。
- 识别新的 Shopee 留言异常路径：当前待审核列表有 2 笔 JOJO Mall 订单，页面确认均为“异常分类仅 1 项且为有留言”、库存标志 0/0、单仓；马帮转待处理实际接口为 `order.doProcessAbnormalOrders`，留言分类 ID 为 `3`。
- 增加默认关闭的“待审核留言恢复”流程：只读候选检查与单笔确认写接口分离；必须同时满足仅留言异常、已配置店铺、平台一致、空运单号、库存充足和单仓才可转回待处理。转状态后当轮绝不发货，从下一轮重新走完整推单安全检查。

### 下一步

1. 在无扫描、无活动发货批次的安全窗口重启一次整套系统，使健康路径修正和异常预警数据库迁移进入当前运行进程。
2. 用一条库存不足、一条多仓和一次受控登录失效进行在线验收，确认看板、筛选、重新核对和 Windows 通知均符合业务口径。
3. 用状态脚本再次确认 `3101`、`3112` 和统一日志均正常，再验证关闭终端后两个服务持续运行。
4. 继续跟进运单恢复订单和自然批次运行情况。

### 风险与注意事项

- 安装统一计划任务前必须确认没有正在扫描或执行的真实发货批次。
- 开发模式与后台正式模式不能同时占用 3101 或 3112；统一开发入口会主动拒绝重复启动。
- 本次没有自动停止或重启当前生产发货服务。

## 2026-07-29

### 今日完成

- 检查自动发货服务实时状态：定时扫描正常、并发配置为 2、检查时没有活动批次且服务由守护程序运行。
- 汇总 20 个真实批次、28 笔真实订单；20 个批次全部成功，28 单全部取得运单号并回查为“配货中”。
- 验证 2 单和 5 单真实批次的并发效果：多单批次相对顺序执行估算提速 1.62—2.00 倍。
- 定位当前主要瓶颈为等待马帮生成运单号：平均 20.272 秒，占单订单工作进程耗时约 67.5%；真实提交请求平均仅 1.878 秒。
- 复核每单提交前的店铺、平台、待处理状态、空运单号、库存、物流渠道和既有交运记录检查；复核提交后的运单号、渠道及配货状态回查。
- 补充自动化测试，证明并发配置不能超过 2，并证明第二波必须等待第一波两单全部结束后才会开始。
- 更新自动发货性能文档和持续监控规则。
- 因服务没有代码或配置变更且运行正常，本次没有重启，避免干扰定时扫描。
- 完成多店铺扫描提速：由 5 个店铺重复登录、重复导出，改为一次账号级只读采集后按店铺隔离生成预览。
- 增加共享采集失败时的逐店扫描自动回退；回退不改变真实提交和安全检查。
- 同一安全窗口对照旧单店读取与新共享读取，JOJO Mall 均返回 5 行明细，匿名差异集合为 0。
- 新版本只在无扫描、无活动批次且距离下轮扫描足够远时由守护程序安全重启。
- 第一轮自然定时扫描整轮耗时 14.351 秒，共享采集耗时 14.298 秒；旧版约 70—90 秒，约提速 5 倍。
- 优化后的首轮扫描自动发现 Toko Penguin 1 单；该单真实批次成功，已取得运单号并回查为“配货中”，无失败或人工处理项。
- 发货模块测试由 35 项增加到 41 项，覆盖共享扫描、店铺隔离、库存规则、失败回退和库存未知人工处理锁，41 项全部通过。
- Table Trove Mall.ID 出现首笔真实订单后完成指定订单预览与深度预检：待处理、信息完整、库存 4/需求 1、固定物流渠道匹配、无既有运单号、无申报或必填参数缺失。
- 经人工明确确认，对 Table Trove Mall.ID 订单 `26072900GFM4VT` 完成单笔真实验证；批次成功取得运单号并从“待处理”转为“配货中”，无失败或人工处理项，总耗时 45.890 秒。
- Table Trove Mall.ID 单笔真实验证成功后，经人工明确确认加入自动发货白名单；服务仅在无扫描、无活动批次的安全窗口重启，重启后健康检查确认该店铺自动开关已开启。
- 当前自动发货白名单共 4 个店铺：Arca Woods、JOJO Mall、Table Trove Mall.ID、Toko Penguin；CONOCO.ID 继续只扫描。
- 定位 JOJO Mall 两笔重复失败订单：马帮返回订单级 `hasGoods=3`、商品级 `orderItemHasGoods=0`，系统只确认 `0=有货`、`2=缺货`，因此按库存未知安全停止；两单均未提交交运、无运单号。
- 增加 `INVENTORY_UNKNOWN_BEFORE_SUBMIT` 人工处理状态：以后此类订单进入 `needs_attention` 并由幂等锁阻止重复排队，不影响同店铺其他订单。
- 安全部署后将当前两笔 JOJO 订单一次性转为人工处理，运行状态和日志均确认成功，后续不再每 5 分钟重复提醒。
- 根据真实业务确认 `hasGoods=3` 的含义为同订单 SKU 分属不同仓库；系统现可在预览和提交前主动识别多仓订单，并以 `MULTI_WAREHOUSE_REQUIRES_REVIEW` 阻止交运。
- 增加多仓人工处理闭环：人工在马帮完成换仓并恢复“待处理”后，可从异常中心重新核对；系统只有在单一仓库、库存、状态、空运单号和固定渠道全部通过后才解除本地锁，且该操作本身绝不提交发货。
- 异常中心增加“重新核对并解除”入口、操作确认、执行中状态及明确的“不立即发货”提示；历史同订单异常合并显示，避免重复干扰。
- 接口文档新增人工复核接口；发货模块自动化测试增加到 45 项，覆盖多仓预览拦截、修复后安全解锁以及仍为多仓时保留锁，45 项全部通过。
- 真实换仓验证发现马帮在 SKU 已归并到同一仓库后仍保留订单级 `hasGoods=3`；系统调整为以最新 SKU 明细的实际仓库作为人工修复证明，同时要求商品级有货、库存正常、订单待处理。没有单仓证明时，深度预检和真实执行器仍会阻止提交。
- 修复马帮商品级库存标志以数字 `0` 返回时被误判为空值的问题；两笔 JOJO 多仓订单深度预检均已确认同仓、库存正常、无运单号且固定渠道可用。
- 增加人工锁安全自动恢复：仅处理多仓和库存未知，连续两轮完整复核通过后自动解锁；任一检查失败即清零，解锁当轮不发货，下一轮才重新进入候选。
- 运单号获取中、交运结果不确定和重启恢复锁明确排除在自动解锁之外，继续防止重复交运；调度状态新增最近自动恢复结果。
- 自动发货测试增加到 48 项，新增连续两轮、失败清零、解锁不在同轮发货的时序覆盖，48 项全部通过。
- 完成 Shopee 运单号审批超时恢复机制：交运成功但无运单号的订单进入持久化队列，每 5 分钟逐单回查；30 分钟后按马帮“批量修改订单—设置空物流渠道”清空渠道并仅重新交运一次，24 小时后转人工。
- 恢复动作增加店铺、平台、待处理状态、空运单号、库存有货和既有交运记录复检；清空前再次读取订单，防止运单号恰好审批成功时误清空。
- 增加 `ready_to_resubmit`、`resubmitting`、`waiting_after_reset` 不可重入状态。清空成功先落库再重新交运，服务中断或写操作结果不确定时禁止盲目重试并转人工。
- 新增恢复队列查询与立即回查接口，并补充 OpenAPI、环境变量和使用说明；发货模块 50 项 Node 测试及 15 项马帮安全测试全部通过。
- 自动清空与重新交运使用独立开关 `FULFILLMENT_TRACKING_RECOVERY_RESET_ENABLED`，默认关闭；代码部署后先做一笔受控验证，再决定是否开启无人值守执行。
- 完成 Arca Woods 订单 `26072905HDE2JF` 的单笔受控真实恢复：确认其为待处理、有货、空运单号且马帮显示“运单号获取中”后，清空物流渠道一次并按固定 J&T 渠道重新交运一次；马帮已接受重新交运，当前进入 `waiting_after_reset` 持续回查，`resetCount=1`，禁止再次清空或重复交运。
- 将国家、平台、店铺、物流渠道、仓库范围和店铺级自动开关从代码硬编码迁移到 `config/fulfillment-shops.json`；现有 5 家印尼店铺行为保持不变。
- 增加配置启动校验：重复店铺、未知渠道、国家/平台不匹配、无效渠道组合或错误仓库结构会阻止服务启动，避免部分错误配置进入真实发货。
- 增加店铺配置开关与 `.env` 白名单双重授权；新增店铺默认只扫描，必须完成单笔验证后才同时放行。
- 增加仓库白名单规则，在预览和真实提交前分别复检；接口文档店铺列表与国家数量改为按配置动态生成。
- 主系统“发货任务”页新增只读运单恢复队列，展示店铺、订单、恢复阶段、提交与下次回查时间、渠道清空次数和最后异常；前端不提供真实恢复按钮，避免误操作。
- 主系统增加恢复队列的固定本机只读代理，并限制查询上限为 100；履约服务令牌不暴露给浏览器。
- 实际页面已显示 Arca Woods 订单 `26072905HDE2JF` 为“重新交运后等待”、渠道清空 `1 / 1` 次；页面无脚本报错。
- 发货模块自动化测试增加到 54 项，全部通过；覆盖配置校验、仓库范围、运单恢复和主系统只读代理。
- 主系统“发货总览”升级为第一版运营看板：展示今日真实发货订单、成功量、成功率、执行中、预检异常、平均处理耗时、等待运单号耗时、近 7 日趋势和店铺表现。
- 新增完整数据库口径的只读看板接口，按北京时间统计并支持 1—30 日趋势；统计不依赖“最近 30 个批次”，可以继续扩展店铺和订单量。
- 看板按稳定订单键去重，同一订单多次尝试只采用最新结果；运单审批中的订单归入执行中，已完成订单不计为异常。
- 店铺、国家和平台筛选会同步更新指标、趋势、异常构成、店铺排名与队列数量；旧版服务尚未重启时前端会使用最近批次临时统计，不会整页失效。
- 看板统计接口、只读代理、去重口径和恢复队列分类已加入自动化测试；发货模块 55 项测试全部通过，前端构建检查通过。
- 重启后完成在线验收：完整数据库看板显示今日 67 单、成功 66 单、成功率 99%、运单恢复中 1 单，真实批次异常为 0；近 7 日趋势返回 7 个日期。
- 页面筛选联动验证通过：选择 JOJO Mall 后显示 20 单、成功 20 单、成功率 100%、库存不足 8 项且仅保留该店铺；恢复全部店铺后回到 67 单和 5 家店铺。
- 默认 1280×720 页面无横向溢出，5 个核心指标等宽，趋势与异常区域保持清晰；浏览器控制台无错误或警告。

### 实际结论

- 并发 2 已完成真实环境验收，不再提高并发上限。
- 当前速度主要由马帮侧运单号生成时间决定，迁移到云服务器可能改善网络或进程启动开销，但不会消除这段主要等待。
- 安全提速有效，未通过减少库存、状态、渠道或幂等检查换取速度。

### 下一步

1. 继续积累自然产生的多单批次，重点观察失败率和等待运单号的波动。
2. 分析定时扫描 5 个店铺约 70—80 秒的只读开销，寻找不影响订单覆盖率的优化点。
3. 评估复用只读登录会话或降低重复登录开销；真实提交仍保持独立检查和禁止盲目重试。
4. 新店铺出现真实订单后，先深度预检和单笔验证，再加入自动发货白名单。
5. 用真实多仓订单验证人工换仓后的重新核对入口；确认无误后，再设计“系统给出换仓建议、人工确认执行”的半自动换仓第二阶段。

多店铺扫描提速已经完成，第 2 项后续改为持续观察自然扫描耗时和回退次数，不再作为待实现事项。

### 风险与注意事项

- 当前真实多单样本数量仍有限，提速比例应随着后续样本持续更新。
- 不主动制造失败批次验证故障路径；失败停止后续波次的行为由自动化测试覆盖。
- 只有无扫描且无活动批次时才允许安全重启。

## 2026-07-28

### 今日完成

- 将自动发货从单店铺方案扩展为同一马帮账号下的 5 个 Shopee 印尼店铺配置。
- Arca Woods、JOJO Mall、Toko Penguin 纳入自动发货白名单；无真实订单可验证的 CONOCO.ID、Table Trove Mall.ID 保持“只扫描、不自动提交”。
- 增加按店铺独立扫描、固定店铺与物流渠道绑定，以及跨店铺批次互斥保护。
- 定时扫描可以自动识别待处理订单，完成库存判断、安全复检、获取运单号并转入“配货中”。
- 批次处理优化为最多 2 单并行、按双单波次推进；任一波异常后停止后续波次。
- 增加批次和单订单阶段耗时记录，为后续定位速度瓶颈提供数据。
- 完成 Windows 登录后自动启动、运行状态检查、日志轮转和异常重启机制。
- 完成 Windows 桌面通知测试，可提示自动发货启动和扫描异常。
- 主系统前端已接入自动发货状态页面，可查看扫描、批次、订单结果及店铺物流配置。
- 增加主系统开发模式 `npm.cmd run dev:main`，后续调整前端时无需反复手动重启。
- 新增项目总 README 和每日进度记录规范。

### 当前状态

- 主系统入口：`http://127.0.0.1:3101/`
- 发货接口文档：`http://127.0.0.1:3112/docs`
- 单批上限：10 单
- 单批并发：最多 2 单
- 当前国家与平台：印度尼西亚 / Shopee
- 当前物流渠道：按现有印尼店铺固定渠道配置

### 下一步

1. 使用真实订单继续记录 5—10 单批次各阶段耗时，确定主要瓶颈是马帮响应、等待运单号还是本机处理。
2. CONOCO.ID 和 Table Trove Mall.ID 出现订单后，先做深度预检和单笔真实验证，再决定是否加入自动发货白名单。
3. 将“国家—店铺—物流渠道—仓库—自动发货开关”从固定代码逐步改为可维护配置，为后续其他国家店铺做准备。
4. 完成公司局域网访问、安全令牌和防火墙范围验证。
5. 持续完善前端的异常提醒、批次详情和人工处理入口。

### 风险与注意事项

- 电脑关机、睡眠、退出 Windows 登录或网络中断会影响自动任务。
- 马帮验证码、风控或页面结构变化仍可能需要人工处理。
- 新店铺或新国家不能直接开启真实自动发货，必须先确认店铺 ID、物流渠道和状态转换规则，并完成单笔验证。

## 2026-07-27

### 已完成

- 建立独立的马帮自动发货接口服务和交互式 API 测试文档。
- 实现指定订单或最多 10 单批量预览、人工确认、异步批次执行及批次状态查询。
- 支持待处理状态、店铺、平台、仓库、空运单号、库存和物流渠道的提交前复检。
- 明确库存状态：有货、缺货、库存未知；缺货或库存未知均禁止提交。
- 支持多 SKU 订单整单汇总和整单库存判断。
- 增加“深度预检但绝不提交”接口，用于真实发货前验证。
- 完成单笔真实订单验证：线上发货、获取运单号、逐单回查并转入“配货中”。
- 将耗时操作改为异步批次，接口先返回运行状态，避免多订单请求长时间等待。
- 增加登录状态检查与重新登录机制，并对真实提交采用禁止盲目重试策略，降低重复发货风险。
- 完成有货单、多 SKU 单、缺货单与马帮页面数据一致性验证。

### 阶段结论

单店铺、固定物流渠道、最多 10 单的安全发货闭环已经跑通，可以在保留安全检查和人工兜底的前提下逐步扩展到更多店铺。

## 每日记录模板

```markdown
## YYYY-MM-DD

### 今日完成

-

### 实际验证

-

### 遇到的问题

-

### 下一步

1.

### 风险与注意事项

-
```
